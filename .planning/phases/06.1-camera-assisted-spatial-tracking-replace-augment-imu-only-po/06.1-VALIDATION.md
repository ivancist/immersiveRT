---
phase: 06.1
slug: camera-assisted-spatial-tracking-replace-augment-imu-only-po
status: approved
nyquist_compliant: true
wave_0_complete: true
created: 2026-07-11
---

# Phase 06.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest ^3.0.0 (configured via `client/vite.config.ts`, `environment: 'jsdom'`) |
| **Config file** | `client/vite.config.ts` (`test:` block) |
| **Quick run command** | `npm --prefix client test -- <file>` (targeted single-file run) |
| **Full suite command** | `npm --prefix client test` |
| **Estimated runtime** | ~10-20 seconds (existing zupt/kalman/encode suites are small unit tests) |

---

## Sampling Rate

- **After every task commit:** Run `npx vitest run client/tests/webxr.test.ts` (plus any other touched file's suite)
- **After every plan wave:** Run `npm --prefix client test` (full suite) + `npm --prefix client run typecheck`
- **Before `/gsd-verify-work`:** Full suite must be green; on-device manual verification on a real Android/ARCore phone is required before this phase is feature-complete (jsdom cannot exercise real WebXR/ARCore tracking quality)
- **Max feedback latency:** ~20 seconds

---

## Per-Task Verification Map

Planner assigns exact Task IDs/Plan/Wave — this table maps each phase requirement/decision to its test obligation; the planner must place these into concrete tasks with matching `<acceptance_criteria>`.

| Requirement / Decision | Secure/Correct Behavior | Test Type | Automated Command | File Exists | Status |
|---|---|---|---|---|---|
| SENS-V2-03 / D-02 (tracking-state → driftConfidence) | `getViewerPose()` null / `emulatedPosition=true` / `emulatedPosition=false` → driftConfidence 0 / 0.5 / 1 | unit | `npx vitest run client/tests/webxr.test.ts -t "driftConfidence"` | ❌ Wave 0 | ⬜ pending |
| SENS-V2-03 / D-03 (rolling gestureDisplacement) | ~300ms-window delta computation matches expected dx/dy/dz from a sequence of timestamped positions | unit | `npx vitest run client/tests/webxr.test.ts -t "gestureDisplacement"` | ❌ Wave 0 | ⬜ pending |
| SENS-V2-03 (freeze-on-lost, Pitfall 5 / D-06) | When pose read returns null, last-known-good position is retained (not reset to 0/NaN) and driftConfidence reports 0 | unit | `npx vitest run client/tests/webxr.test.ts -t "freeze"` | ❌ Wave 0 | ⬜ pending |
| D-04 (fallback branch unchanged) | Existing ZUPT/Kalman test suite still passes unmodified after this phase's changes | regression | `npx vitest run client/tests/zupt.test.ts client/tests/kalman.test.ts client/tests/encode.test.ts` | ✅ already exists | ⬜ pending |
| D-05 (feature detection safe guard, Pitfall 7) | `detectWebXrSupport()` returns `false` (not throw) when `navigator.xr` is absent | unit | `npx vitest run client/tests/webxr.test.ts -t "isSessionSupported"` | ❌ Wave 0 | ⬜ pending |
| D-01 (wire format unchanged) | `encodePacket()`/`decodePacket()` round-trip produces the same 36-byte layout when camera-derived values are used as px/py/pz input | regression | `npx vitest run client/tests/encode.test.ts client/tests/decode.test.ts` | ✅ exists — extend with camera-sourced case | ⬜ pending |
| WebXR session lifecycle / real ARCore pose accuracy | On-device "feels right" verification of actual camera-assisted position tracking | manual-only | N/A | — | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `client/tests/webxr.test.ts` — new file covering driftConfidence mapping (D-02), rolling gestureDisplacement (D-03), freeze-on-lost (D-06/Pitfall 5), and safe feature-detection (D-05/Pitfall 7). All four are pure-function-testable in jsdom by mocking minimal `XRFrame`/`XRPose`-shaped objects — no real WebXR needed.
- [ ] Ambient WebXR TypeScript declarations (`.d.ts`, hand-written to match project's existing type style — not `@types/webxr`) so `webxr.ts` typechecks against `navigator.xr`/`XRSession`/`XRFrame`.
- [ ] `client/tests/encode.test.ts` — extend with a case asserting camera-sourced px/py/pz values pass through `safeFloat()`/clamp identically to Kalman-sourced values (Pitfall 4).

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Real ARCore camera-assisted position tracking quality/drift | SENS-V2-03 | jsdom cannot exercise `navigator.xr`/ARCore hardware pose tracking; no headless/CI environment can emulate it | On a physical WebXR-capable Android (ARCore) device: start calibration, confirm "Camera Tracking" badge shows, move phone through a known physical path, confirm on-desktop position tracking follows without the drift documented in `.planning/debug/huge-position-drift-makes-pho.md` |
| Session-drop mid-game recovery (D-06) | D-06 | Requires physically backgrounding the app / losing tracking on a real device | On a physical device: trigger tracking loss (cover camera, background app), confirm phone reports `driftConfidence=0` and desktop freezes position rather than snapping/resetting; confirm no live hand-off to Kalman/ZUPT occurs mid-session |
| Battery/thermal escape-hatch condition | Phase goal (escape hatch) | Requires extended real-device runtime to observe heat/battery drain | Run a WebXR immersive-ar session on a real Android device for 15+ minutes, observe device temperature/battery drain; confirms whether the escape-hatch fallback condition is a real concern for this phase |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references (webxr.test.ts, ambient .d.ts, extended encode.test.ts delivered in Plan 06.1-01/02, consumed by Plan 06.1-03)
- [x] No watch-mode flags
- [x] Feedback latency < 20s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved 2026-07-11 (gsd-plan-checker VERIFICATION PASSED)
