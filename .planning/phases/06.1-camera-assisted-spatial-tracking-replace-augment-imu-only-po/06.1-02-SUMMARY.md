---
phase: 06.1-camera-assisted-spatial-tracking-replace-augment-imu-only-po
plan: 02
subsystem: testing
tags: [vitest, float16, encode, regression-testing, webxr]

# Dependency graph
requires:
  - phase: 05-phone-client-sensor-pipeline
    provides: encode.ts 36-byte schema-v1 packet encoder with safeFloat() input sanitisation
provides:
  - Regression proof that camera-sourced px/py/pz values pass through the same safeFloat() guard as Kalman-sourced values
  - Regression proof that encodePacket is source-agnostic (byte-identical output for identical numeric position fields)
  - Confirmation that D-04 fallback suites (zupt, kalman, encode) remain green
affects: [06.1-03-wire-webxr-into-phone-ts]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Encoder regression tests describe camera vs. kalman position source without any production code branching — the wire format is source-agnostic by construction"

key-files:
  created: []
  modified: [client/tests/encode.test.ts]

key-decisions:
  - "Compared byte-identical output using two separate ArrayBuffer instances (not the shared _packetBuf) to avoid the second encodePacket call overwriting the first result before comparison"

patterns-established:
  - "Source-agnostic encoder regression pattern: construct two SensorPacket fixtures labelled by data-source intent (camera-sourced / kalman-sourced) with identical numeric fields, encode into separate buffers, assert byte-for-byte equality"

requirements-completed: [SENS-03, SENS-04]

coverage:
  - id: D1
    description: "Camera-sourced NaN/Infinity/-Infinity px/py/pz sanitise to finite float16 zero at offsets 21/23/25 (Pitfall 4 / V5)"
    requirement: "SENS-03"
    verification:
      - kind: unit
        ref: "client/tests/encode.test.ts#encodePacket — camera-sourced position (Pitfall 4 / V5)"
        status: pass
    human_judgment: false
  - id: D2
    description: "Camera-sourced px at POSITION_MAX bound (100m) round-trips through the 36-byte encoder within float16 tolerance (D-01)"
    requirement: "SENS-04"
    verification:
      - kind: unit
        ref: "client/tests/encode.test.ts#camera-sourced px at POSITION_MAX bound (100 m) round-trips within float16 tolerance (D-01)"
        status: pass
    human_judgment: false
  - id: D3
    description: "Encoder is source-agnostic: camera-sourced and kalman-sourced packets with identical numeric px/py/pz produce byte-identical 36-byte output"
    requirement: "SENS-04"
    verification:
      - kind: unit
        ref: "client/tests/encode.test.ts#camera-sourced and kalman-sourced packets with identical numeric px/py/pz encode byte-identical output (source-agnostic encoder, D-01)"
        status: pass
    human_judgment: false
  - id: D4
    description: "D-04 fallback regression suites (encode, zupt, kalman) remain green after this change"
    verification:
      - kind: unit
        ref: "npm --prefix client test -- encode zupt kalman (49 tests passed)"
        status: pass
    human_judgment: false

duration: 3min
completed: 2026-07-11
status: complete
---

# Phase 06.1 Plan 02: Camera-Sourced Encoder Regression Tests Summary

**Extended encode.test.ts with 5 new tests proving camera-derived px/py/pz pass through the same safeFloat() guard as Kalman-derived values, and that encodePacket never branches on position source (D-01) — zero production code changed.**

## Performance

- **Duration:** 3 min
- **Started:** 2026-07-11T23:44:11Z
- **Completed:** 2026-07-11T23:46:15Z
- **Tasks:** 1 completed
- **Files modified:** 1

## Accomplishments
- Added `describe('encodePacket — camera-sourced position (Pitfall 4 / V5)', ...)` block with 5 new `it` cases to `client/tests/encode.test.ts`
- Proved NaN/Infinity/-Infinity camera-sourced px/py/pz sanitise to finite float16 zero at offsets 21/23/25 (mirrors the existing NaN qw precedent at offset 7)
- Proved px at the POSITION_MAX bound (100m) round-trips within float16 tolerance
- Proved the encoder is source-agnostic: camera-sourced and kalman-sourced packets with identical numeric px/py/pz produce byte-identical 36-byte output
- Confirmed encode.ts, zupt, and kalman regression suites (49 tests total) remain green — D-04 fallback safety net intact

## Task Commits

Each task was committed atomically:

1. **Task 1: Add camera-sourced position cases to encode.test.ts (Pitfall 4 / V5)** - `11d69c4` (test)

**Plan metadata:** pending (docs: complete plan)

## Files Created/Modified
- `client/tests/encode.test.ts` - Added camera-sourced position regression test block (5 new `it` cases); no other changes

## Decisions Made
- Used two separate `ArrayBuffer` instances (not the shared module-level `_packetBuf`) when comparing camera-sourced vs. kalman-sourced encoded output, since `encodePacket` writes into the buffer in place and a second call against the default buffer would overwrite the first result before comparison could happen.

## Deviations from Plan

None - plan executed exactly as written. No production code (`client/src/sensor/encode.ts`) was touched; only `client/tests/encode.test.ts` changed, matching the plan's acceptance criteria (`git diff --name-only` shows only the test file).

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- The wire-format safety net for camera-sourced positions is now proven independent of and in parallel with the WebXR module built in Plan 01.
- Plan 03 can safely wire the WebXR branch into `phone.ts` knowing `encodePacket`'s `safeFloat()` guard already sanitises any camera-derived NaN/Infinity/out-of-range px/py/pz values identically to Kalman-derived ones — no encoder changes are required for that plan.
- No blockers.

---
*Phase: 06.1-camera-assisted-spatial-tracking-replace-augment-imu-only-po*
*Completed: 2026-07-11*

## Self-Check: PASSED

- FOUND: client/tests/encode.test.ts
- FOUND: .planning/phases/06.1-camera-assisted-spatial-tracking-replace-augment-imu-only-po/06.1-02-SUMMARY.md
- FOUND: 11d69c4 (task commit)
