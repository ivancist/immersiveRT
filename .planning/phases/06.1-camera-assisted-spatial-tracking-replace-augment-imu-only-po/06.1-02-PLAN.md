---
phase: 06.1-camera-assisted-spatial-tracking-replace-augment-imu-only-po
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - client/tests/encode.test.ts
autonomous: true
requirements: [SENS-03, SENS-04]
must_haves:
  truths:
    - "A SensorPacket with NaN / Infinity / -Infinity px/py/pz encodes to finite float16 bytes (0) at offsets 21/23/25 (safeFloat guard, Pitfall 4 / V5)"
    - "A camera-sourced px at the POSITION_MAX bound (100 m) round-trips through the 36-byte encoder within float16 tolerance (D-01)"
    - "A camera-sourced packet and a Kalman-sourced packet with identical numeric px/py/pz produce byte-identical output (source-agnostic encoder, D-01)"
    - "Existing zupt / kalman / encode regression suites remain green after this phase (D-04)"
  artifacts:
    - client/tests/encode.test.ts
  key_links:
    - "encode.ts safeFloat guard applies identically to camera-sourced and Kalman-sourced position fields — the boundary Plan 03 relies on"
---

<objective>
Extend the existing encoder test suite to prove that camera-derived position values are handled by the exact same input-validation path (`safeFloat()` on offsets 21/23/25) as Kalman-derived values, and that the 36-byte schema-v1 wire format is unchanged (D-01). This closes the Pitfall-4 / V5 input-validation gap for the new camera position source without touching production code — encode.ts is source-agnostic, so this is a regression guard that must hold both today and after Plan 03 wires the WebXR branch. Also (re)confirms the D-04 fallback suites stay green.

Purpose: Establish the wire-format safety net independent of, and in parallel with, the new WebXR module (Plan 01).
Output: extended `client/tests/encode.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.1-camera-assisted-spatial-tracking-replace-augment-imu-only-po/06.1-RESEARCH.md
@.planning/phases/06.1-camera-assisted-spatial-tracking-replace-augment-imu-only-po/06.1-PATTERNS.md
</context>

## Artifacts this phase produces

New symbols/files introduced across Phase 06.1 (net-new; exclude from source-drift verification):

- (Plan 01) `client/src/sensor/webxr.ts`, `client/src/webxr.d.ts`, `client/tests/webxr.test.ts`
- (This plan) new `describe('encodePacket — camera-sourced position ...')` block in `client/tests/encode.test.ts`
- (Plan 03) `client/phone.html` `#tracking-mode-badge`; `client/src/phone.ts` `useWebXr` branch wiring

<tasks>

<task type="auto">
  <name>Task 1: Add camera-sourced position cases to encode.test.ts (Pitfall 4 / V5)</name>
  <files>client/tests/encode.test.ts</files>
  <read_first>
    - client/src/sensor/encode.ts (safeFloat() lines 44-47; the D-14 byte layout — px=offset 21, py=23, pz=25 float16; note encode.ts applies safeFloat but does NOT clamp — the POSITION_MAX clamp lives in phone.ts)
    - client/tests/encode.test.ts (existing style: basePkt fixture, `getFloat16(view, offset, true)`, `new DataView(result.buffer, result.byteOffset)`, existing `NaN qw` precedent to mirror for px/py/pz)
    - .planning/phases/06.1-camera-assisted-spatial-tracking-replace-augment-imu-only-po/06.1-RESEARCH.md Pitfall 4 and Security Domain V5
    - .planning/phases/06.1-camera-assisted-spatial-tracking-replace-augment-imu-only-po/06.1-VALIDATION.md (Wave 0 gap: extend encode.test.ts with a camera-sourced case)
  </read_first>
  <action>
    Append a new `describe('encodePacket — camera-sourced position (Pitfall 4 / V5)', ...)` block to `client/tests/encode.test.ts`, reusing the existing `basePkt` fixture and `getFloat16`/DataView read helpers. Add `it` cases asserting: (1) a packet with `px: NaN` encodes to a finite float16 value 0 at offset 21; (2) `py: Infinity` encodes to finite 0 at offset 23; (3) `pz: -Infinity` encodes to finite 0 at offset 25 — mirroring the existing `NaN qw` test at offset 7. Add a bound case: (4) `px: 100` (the POSITION_MAX clamp bound applied upstream in phone.ts) round-trips at offset 21 within tolerance (`Math.abs(recovered - 100) < 0.1`). Add a source-agnostic case: (5) build two packets — one labelled "camera-sourced" and one "kalman-sourced" — with identical numeric `px/py/pz` (e.g. 1.5 / -2.25 / 0.75) and assert the two encoded Uint8Arrays are byte-identical (compare all 36 bytes), proving the encoder does not branch on position source (D-01). Do NOT modify `client/src/sensor/encode.ts` — this is a test-only regression guard.
  </action>
  <acceptance_criteria>
    - `npm --prefix client test -- encode -t "camera-sourced"` passes all new cases.
    - `git diff --name-only` shows only `client/tests/encode.test.ts` changed (encode.ts untouched).
    - `npm --prefix client test -- encode zupt kalman` is green (D-04 fallback regression suites intact).
    - `grep -c "camera-sourced" client/tests/encode.test.ts` returns at least 1.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix client test -- encode zupt kalman</automated>
  </verify>
  <done>encode.test.ts proves NaN/Infinity/out-of-range camera px/py/pz are sanitised to finite bytes and that encoding is byte-identical regardless of position source; fallback suites remain green; no production code changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| camera-derived px/py/pz → SensorPacket encoder | The V5 input-validation boundary this plan pins down with regression tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.1-02 | Denial of Service / Tampering | encode.ts safeFloat on px/py/pz | high | mitigate | Regression tests assert NaN/Infinity/out-of-range camera position values encode to finite bytes via safeFloat (Pitfall 4 / V5); production clamp is added in Plan 03 |
| T-06.1-SC | Tampering | npm/pip/cargo installs | low | accept | No packages installed this phase — test-only change (RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `npm --prefix client test -- encode zupt kalman` green.
- Only the test file changed (production encode.ts unchanged — D-01/D-04).
</verification>

<success_criteria>
- Camera-sourced NaN/Infinity/out-of-range px/py/pz proven sanitised to finite float16 bytes.
- Encoder proven source-agnostic (byte-identical output for identical numeric fields).
- D-04 fallback regression suites confirmed green.
</success_criteria>

<output>
Create `.planning/phases/06.1-camera-assisted-spatial-tracking-replace-augment-imu-only-po/06.1-02-SUMMARY.md` when done.
</output>
