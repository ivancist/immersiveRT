---
phase: 01-server-and-transport-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - .gitignore
  - server/Cargo.toml
  - server/src/main.rs
  - server/src/echo.rs
autonomous: true
requirements:
  - INFRA-01
must_haves:
  truths:
    - "`cargo build -p immersive-rt-server` succeeds with zero warnings"
    - "Cargo workspace defines members = [\"server\"] with resolver = \"2\""
    - "server/src/main.rs compiles to a runnable binary (even if it exits immediately)"
    - "`certs/` is listed in `.gitignore` before any cert files exist"
    - "`echo.rs` exports `now_ms() -> u64` and `EchoMessage` struct used by both transport handlers"
  artifacts:
    - Cargo.toml (workspace root)
    - server/Cargo.toml (package manifest with all dependencies)
    - server/src/main.rs (tokio::main entry point with placeholder spawns)
    - server/src/echo.rs (shared echo logic module)
    - .gitignore (certs/ entry present)
  key_links:
    - server/Cargo.toml declares wtransport = \"0.7\", tokio, tokio-tungstenite, futures-util, anyhow, tracing, tracing-subscriber, serde, serde_json
    - main.rs references echo.rs module via `mod echo`
    - .gitignore contains `certs/` to prevent private key commits
---

## Phase Goal

**As a** developer setting up ImmersiveRT for the first time, **I want to** run `cargo build`
and get a compiling Rust binary with the WebTransport and WebSocket dependencies declared,
**so that** I can begin implementing transport listeners without dependency-resolution surprises.

<objective>
Scaffold the Rust Cargo workspace and server package with all required dependencies declared
and verified. Establish the shared echo module used by both transport listeners. Produce a
binary that compiles clean — the entry point exists but listeners are not yet active.

Purpose: Eliminate dependency-resolution and workspace-structure problems before transport
implementation begins. A clean build at Wave 1 ensures Wave 2 can focus entirely on
wtransport logic without fighting the toolchain.

Output:
- Cargo.toml (workspace root)
- server/Cargo.toml (all Phase 1 deps declared)
- server/src/main.rs (tokio::main entry, placeholder for listener spawns)
- server/src/echo.rs (now_ms, EchoMessage, echo handler signatures)
- .gitignore with certs/ entry
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-server-and-transport-foundation/SKELETON.md
@.planning/phases/01-server-and-transport-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cargo workspace root and server package manifest</name>
  <files>Cargo.toml, server/Cargo.toml</files>
  <read_first>
    - .planning/phases/01-server-and-transport-foundation/01-RESEARCH.md (Standard Stack section, Cargo.toml code example)
    - .planning/phases/01-server-and-transport-foundation/SKELETON.md (Framework table)
  </read_first>
  <behavior>
    - Cargo.toml workspace root: `[workspace]` with `members = ["server"]` and `resolver = "2"`
    - server/Cargo.toml: `[package]` name = "immersive-rt-server", version = "0.1.0", edition = "2021"
    - Dependencies in server/Cargo.toml: wtransport = "0.7", tokio with features ["macros","rt-multi-thread","fs","io-util","net","sync"], tokio-tungstenite = "0.29", futures-util = "0.3", anyhow = "1", tracing = "0.1", tracing-subscriber with features ["env-filter"], serde with features ["derive"], serde_json = "1"
    - No dev-dependencies block yet (added in Task 3)
    - `cargo metadata --no-deps` succeeds and lists immersive-rt-server
  </behavior>
  <action>
    Create `Cargo.toml` at the project root with a `[workspace]` table: members pointing to "server", resolver = "2". No `[package]` section in the workspace root.

    Create `server/Cargo.toml` with `[package]` (name = "immersive-rt-server", version = "0.1.0", edition = "2021") and `[dependencies]` per the RESEARCH.md code example (Standard Stack section). Exact versions: wtransport = "0.7", tokio = { version = "1", features = [...] }, tokio-tungstenite = "0.29", futures-util = "0.3", anyhow = "1", tracing = "0.1", tracing-subscriber = { version = "0.3", features = ["env-filter"] }, serde = { version = "1", features = ["derive"] }, serde_json = "1". Do NOT pin patch versions — use semver minor ranges so cargo can resolve.
  </action>
  <verify>
    <automated>cd /home/ivancist/Documents/immersiveRT && cargo metadata --no-deps --manifest-path Cargo.toml 2>&1 | grep immersive-rt-server</automated>
  </verify>
  <done>
    `cargo metadata` outputs a JSON line containing "immersive-rt-server". Both Cargo.toml files parse without error. `cargo fetch` (or first build) resolves all dependencies without version conflicts.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: server/src/main.rs and server/src/echo.rs scaffolds</name>
  <files>server/src/main.rs, server/src/echo.rs, .gitignore</files>
  <read_first>
    - .planning/phases/01-server-and-transport-foundation/01-RESEARCH.md (Pattern 3: Dual-listener main(), Pattern 4: Latency echo handler, Architecture Patterns section)
    - .planning/phases/01-server-and-transport-foundation/SKELETON.md (Network Architecture table, Latency Echo Probe table)
  </read_first>
  <behavior>
    - `echo.rs` exports: `pub fn now_ms() -> u64` returning SystemTime epoch milliseconds, and `pub struct EchoMessage` with fields `msg_type: String` (serialized as "type"), `client_ts: u64`, and `server_ts: Option<u64>` — all serde Serialize/Deserialize
    - `main.rs` declares `mod echo; mod wt_server; mod ws_server;` (the wt_ and ws_ modules can be stubs returning `Ok(())` at this stage)
    - `main.rs` has `#[tokio::main] async fn main() -> anyhow::Result<()>` that calls `tracing_subscriber::fmt::init()` and reads env vars `CERT_PATH`, `KEY_PATH`, `WT_PORT`, `WS_PORT` with defaults matching SKELETON.md
    - `cargo build -p immersive-rt-server` produces zero warnings (use `#[allow(dead_code)]` on stub module functions if needed until Wave 2 implements them)
    - `.gitignore` contains at minimum the lines `certs/` and `target/`
  </behavior>
  <action>
    Create `server/src/` directory. Create `server/src/echo.rs` with `now_ms()` using `std::time::{SystemTime, UNIX_EPOCH}` and `EchoMessage` struct deriving `serde::Serialize, serde::Deserialize, Debug`. Use `#[serde(rename = "type")]` on the `msg_type` field to match the JSON key "type".

    Create `server/src/main.rs` with `mod echo; mod wt_server; mod ws_server;` declarations, `#[tokio::main] async fn main() -> anyhow::Result<()>`, `tracing_subscriber::fmt::init()`, and `std::env::var()` reads for CERT_PATH (default "certs/localhost+2.pem"), KEY_PATH (default "certs/localhost+2-key.pem"), WT_PORT (default "4433"), WS_PORT (default "8080"). The body ends with `Ok(())` at this stage — listener spawns are added in Plan 02 and 03.

    Create `server/src/wt_server.rs` and `server/src/ws_server.rs` as empty stubs: each exports `pub async fn run(...) -> anyhow::Result<()> { Ok(()) }`.

    Create or update `.gitignore` at the repo root to include `certs/` and `target/` on their own lines. Never commit certs — the private key must never appear in git history.
  </action>
  <verify>
    <automated>cd /home/ivancist/Documents/immersiveRT && cargo build -p immersive-rt-server 2>&1 | grep -E "^error|warning\[" | grep -v "unused import" | wc -l | xargs -I{} test {} -eq 0 && echo "BUILD_CLEAN"</automated>
  </verify>
  <done>
    `cargo build -p immersive-rt-server` exits 0 with no error lines. The binary artifact exists at `target/debug/immersive-rt-server`. `echo.rs` contains `now_ms` and `EchoMessage`. `.gitignore` has `certs/` entry.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: echo unit tests + cargo test baseline</name>
  <files>server/src/echo.rs, server/Cargo.toml</files>
  <read_first>
    - server/src/echo.rs (just written in Task 2)
    - .planning/phases/01-server-and-transport-foundation/01-RESEARCH.md (Validation Architecture section)
  </read_first>
  <behavior>
    - A `#[cfg(test)] mod tests` block in `echo.rs` contains at least two tests:
      1. `test_now_ms_nonzero`: calls `now_ms()` and asserts the result is greater than 1_700_000_000_000 (a plausible lower bound for 2024+ Unix ms)
      2. `test_echo_message_round_trip`: constructs an `EchoMessage { msg_type: "ping".into(), client_ts: 12345, server_ts: None }`, serializes to JSON with `serde_json::to_string`, deserializes back, asserts `client_ts == 12345` and `msg_type == "ping"` and `server_ts == None`
    - `cargo test -p immersive-rt-server` exits 0 and prints "test result: ok. 2 passed"
    - Zero warnings on `cargo test` (same zero-warning gate as build)
  </behavior>
  <action>
    Add a `#[cfg(test)] mod tests` block at the bottom of `server/src/echo.rs`. Write `test_now_ms_nonzero` and `test_echo_message_round_trip` as described in the behavior block above. Use `serde_json::to_string` and `serde_json::from_str` for the round-trip test — no extra test dependencies needed since serde_json is already a runtime dependency.

    No new dependencies are needed in server/Cargo.toml for these tests. If `cargo test` reports unused-import warnings about test-only code, suppress with `#[allow(unused_imports)]` in the test module only.
  </action>
  <verify>
    <automated>cd /home/ivancist/Documents/immersiveRT && cargo test -p immersive-rt-server 2>&1 | tail -5</automated>
  </verify>
  <done>
    `cargo test -p immersive-rt-server` exits 0. Output contains "test result: ok" with at least 2 tests passed. No "FAILED" lines in output. No error-level compiler diagnostics.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer → git repo | Developer must not accidentally commit cert private key |
| build system → crates.io | cargo fetch pulls from crates.io registry; supply-chain attack surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Information Disclosure | certs/localhost+2-key.pem | high | mitigate | Add `certs/` to `.gitignore` in Task 2 before any cert files exist; verify with `git check-ignore -v certs/localhost+2-key.pem` after gitignore is written |
| T-01-02 | Tampering | crates.io dependency fetch | medium | mitigate | All packages passed Package Legitimacy Audit in RESEARCH.md (wtransport, tokio, tokio-tungstenite all VERIFIED); `Cargo.lock` committed to repo pins exact resolved versions after first fetch |
| T-01-03 | DoS | `cargo build` in CI | low | accept | Build-time only threat; no runtime exposure; acceptable for Phase 1 scaffold |
</threat_model>

<verification>
After all three tasks complete:

1. `cargo build -p immersive-rt-server` exits 0 with zero error lines
2. `cargo test -p immersive-rt-server` exits 0 with "test result: ok. 2 passed" (or more)
3. `cat .gitignore | grep certs/` outputs `certs/`
4. `cargo metadata --no-deps | grep immersive-rt-server` returns a result
5. `git check-ignore -v certs/` outputs a match (if git is initialized)
</verification>

<success_criteria>
- Cargo workspace builds cleanly from a fresh `cargo build`
- Both echo unit tests pass
- `.gitignore` protects `certs/` from accidental commit
- All dependency versions match RESEARCH.md Package Legitimacy Audit (no new packages introduced)
</success_criteria>

## Artifacts This Phase Produces

| Symbol | Kind | File | Used By |
|--------|------|------|---------|
| `now_ms` | pub fn | server/src/echo.rs | wt_server.rs, ws_server.rs (Plans 02 + 03) |
| `EchoMessage` | pub struct | server/src/echo.rs | wt_server.rs, ws_server.rs (Plans 02 + 03) |
| `immersive-rt-server` | binary | target/debug/immersive-rt-server | Plans 02 + 03 (cargo run) |
| `wt_server::run` | pub async fn stub | server/src/wt_server.rs | main.rs (Plan 02 will fill) |
| `ws_server::run` | pub async fn stub | server/src/ws_server.rs | main.rs (Plan 03 will fill) |

<output>
Create `.planning/phases/01-server-and-transport-foundation/01-01-SUMMARY.md` when done.
</output>
