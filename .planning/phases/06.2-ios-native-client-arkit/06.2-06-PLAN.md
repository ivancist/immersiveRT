---
phase: 06.2-ios-native-client-arkit
plan: 06
type: execute
wave: 2
depends_on: [06.2-01]
files_modified:
  - mobile/ios-app/immersiveRT.xcodeproj/project.pbxproj
  - mobile/ios-app/immersiveRT/WebRTC/PeerConnectionManager.swift
  - mobile/ios-app/immersiveRT/WebRTC/ICEConfig.swift
  - mobile/ios-app/immersiveRTTests/PeerConnectionManagerTests.swift
  - mobile/ios-app/immersiveRTTests/ICEConfigTests.swift
autonomous: false
requirements: [PHONE-03]
user_setup:
  - service: "stasel/WebRTC (SPM)"
    why: "Prebuilt WebRTC.xcframework for RTCPeerConnection + RTCDataChannel on iOS"
    dashboard_config:
      - task: "Resolve the exact current stasel/WebRTC release tag via Xcode 'Add Package Dependencies' or `swift package resolve`, then confirm the tag before it is pinned in the project"
        location: "Xcode → File → Add Package Dependencies → https://github.com/stasel/WebRTC.git"
must_haves:
  truths:
    - "The stasel/WebRTC package is added to the Xcode project pinned to a registry-verified real release tag (not the unverified 149.0.0 from research)."
    - "PeerConnectionManager creates one RTCDataChannel per peer with isOrdered=false, maxRetransmits=0 (never maxPacketLifeTime), and self-heals a failed peer by reopening."
    - "ICEConfig maps pair-ack ice_servers JSON into RTCIceServer[]."
  artifacts:
    - mobile/ios-app/immersiveRT/WebRTC/PeerConnectionManager.swift
    - mobile/ios-app/immersiveRT/WebRTC/ICEConfig.swift
  key_links:
    - "Data channel config isOrdered=false + maxRetransmits=0 is the project-wide locked contract (Phase 4) — must match exactly."
    - "SDP offer/answer + ICE candidates flow through the active SignalingTransport (Plan 01), not a hardcoded transport."
---

<objective>
Add the WebRTC dependency and implement the peer-connection fan-out — one unreliable data channel per desktop in the room (PHONE-03) — as a Swift port of `phone.ts`'s `openChannelToPeer()`.

The data channel config (`isOrdered=false, maxRetransmits=0`) is the project-wide locked contract enforced since Phase 4; it must match exactly (never `maxPacketLifeTime`). The `stasel/WebRTC` SPM package version is `[ASSUMED]` in research (found via WebSearch, not registry-verified) — a blocking-human legitimacy checkpoint MUST verify the package and resolve the real current tag before it is pinned. No ARKit (D-01).

Purpose: Deliver encoded sensor packets peer-to-peer to every desktop, byte-identical to the web client, with the same self-healing reconnect-on-failure behavior.
Output: verified SPM dependency, `PeerConnectionManager.swift`, `ICEConfig.swift`, and tests for config + ICE mapping.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md

**Environment:** SPM resolution + the real WebRTC DTLS/data-channel behavior require the Mac + Xcode (no Xcode in sandbox). Real peer connection to a desktop is an on-device verification (Plan 09). What IS unit-testable: `RTCDataChannelConfiguration` field values (constructed in isolation) and the `ICEConfig` JSON→`RTCIceServer` mapping. `swiftc -parse` cannot resolve the WebRTC module in the sandbox — rely on `xcodebuild` (Mac) for compile verification of WebRTC-importing files.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.2-ios-native-client-arkit/06.2-RESEARCH.md
@.planning/phases/06.2-ios-native-client-arkit/06.2-PATTERNS.md
@client/src/phone.ts
@client/src/room.ts
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 0: Package legitimacy checkpoint — verify stasel/WebRTC + resolve real tag</name>
  <action>Blocking-human package-legitimacy checkpoint — no automated action, never auto-approved (legitimacy checkpoints ignore auto_advance). A human verifies the package identity and resolves the real current release tag per &lt;how-to-verify&gt; before the SPM pin is written in Task 1. This gates T-06.2-SC (supply-chain).</action>
  <what-built>Nothing yet — this checkpoint gates the SPM package install. `stasel/WebRTC`'s version tag is `[ASSUMED]` in RESEARCH (WebSearch, not registry-verified). Per the Package Legitimacy Protocol, the package must be human-verified and its real tag resolved before pinning.</what-built>
  <how-to-verify>
    1. Open https://github.com/stasel/WebRTC and https://swiftpackageindex.com/stasel/WebRTC — confirm the package is real, actively maintained, and is the community-standard WebRTC xcframework (not a typosquat).
    2. On the Mac in Xcode: File → Add Package Dependencies → `https://github.com/stasel/WebRTC.git` — note the actual current resolved release tag Xcode offers (do NOT trust the research figure `149.0.0`).
    3. Confirm the resolved tag to the executor so it is pinned in project.pbxproj.
  </how-to-verify>
  <resume-signal>Reply with the verified package identity + the exact resolved release tag to pin (e.g. "verified, pin 149.x.y"), or "reject" with an alternative.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Add stasel/WebRTC SPM dependency (pinned to the verified tag)</name>
  <files>mobile/ios-app/immersiveRT.xcodeproj/project.pbxproj</files>
  <read_first>
    - mobile/ios-app/immersiveRT.xcodeproj/project.pbxproj (locate PBXProject packageReferences + the app target's frameworks build phase / packageProductDependencies)
    - .planning/phases/06.2-ios-native-client-arkit/06.2-RESEARCH.md ("Standard Stack → Core" WebRTC row + "Installation")
  </read_first>
  <action>
    Add an `XCRemoteSwiftPackageReference` for `https://github.com/stasel/WebRTC.git` pinned to the tag confirmed by the preceding checkpoint (`upToNextMajor` from the verified tag), and a `XCSwiftPackageProductDependency` on the `WebRTC` product wired into the `immersiveRT` app target's `packageProductDependencies` + Frameworks build phase. Do NOT add it to the test target unless a test imports WebRTC. Use the exact verified tag from the checkpoint — never the research `149.0.0` placeholder.
  </action>
  <verify>
    <automated>cd mobile/ios-app && xcodebuild -resolvePackageDependencies -project immersiveRT.xcodeproj && xcodebuild build -scheme immersiveRT -destination 'platform=iOS Simulator,name=iPhone 17'</automated>
  </verify>
  <acceptance_criteria>
    - On the Mac, `xcodebuild -resolvePackageDependencies` resolves `stasel/WebRTC` at the verified tag with no error.
    - The app target builds with `import WebRTC` available.
    - The pinned tag matches the checkpoint-confirmed tag (not `149.0.0` unless that was the verified value).
  </acceptance_criteria>
  <done>WebRTC is a resolvable, pinned, human-verified dependency of the app target.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ICEConfig — pair-ack ice_servers → RTCIceServer[]</name>
  <files>mobile/ios-app/immersiveRT/WebRTC/ICEConfig.swift, mobile/ios-app/immersiveRTTests/ICEConfigTests.swift</files>
  <read_first>
    - client/src/phone.ts line 471 (`iceServers` extraction from `pair-ack` payload) and its usage in `openChannelToPeer`
    - .planning/phases/06.2-ios-native-client-arkit/06.2-PATTERNS.md ("ICEConfig.swift")
    - mobile/ios-app/immersiveRT/Transport/SignalingEnvelope.swift (the `iceServers` accessor from Plan 01)
  </read_first>
  <behavior>
    - A pair-ack `ice_servers` array of `{urls, username?, credential?}` objects maps to `[RTCIceServer]` preserving urls + optional username/credential.
    - An entry with only `urls` (STUN) maps to an `RTCIceServer` with nil credentials.
    - An empty/missing `ice_servers` maps to an empty array (no crash).
  </behavior>
  <action>
    Implement `ICEConfig` mapping the decoded `ice_servers` payload (from `SignalingEnvelope`'s accessor, Plan 01) into `[RTCIceServer]` (init `RTCIceServer(urlStrings:username:credential:)`), preserving TURN username/credential and handling STUN-only entries. Write `ICEConfigTests.swift` covering the behavior cases (this test imports WebRTC → runs on the Mac).
  </action>
  <verify>
    <automated>cd mobile/ios-app && xcodebuild test -scheme immersiveRT -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:immersiveRTTests/ICEConfigTests</automated>
  </verify>
  <acceptance_criteria>
    - `ICEConfigTests` green on the Mac: TURN entry maps with username+credential; STUN-only entry maps with nil credentials; empty input → empty array.
  </acceptance_criteria>
  <done>Server-provided ICE servers are correctly mapped for peer connections.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: PeerConnectionManager — unreliable data-channel fan-out</name>
  <files>mobile/ios-app/immersiveRT/WebRTC/PeerConnectionManager.swift, mobile/ios-app/immersiveRTTests/PeerConnectionManagerTests.swift</files>
  <read_first>
    - client/src/phone.ts lines 634-713 (`openChannelToPeer` — full event wiring)
    - client/src/room.ts (`handleOffer` DTLS `a=setup:active`→`passive` patch — for awareness, server-side)
    - .planning/phases/06.2-ios-native-client-arkit/06.2-PATTERNS.md ("PeerConnectionManager.swift" + "Shared Patterns → data channel config")
    - .planning/phases/06.2-ios-native-client-arkit/06.2-RESEARCH.md ("Architecture Patterns → Pattern 2", Anti-Patterns re maxPacketLifeTime)
  </read_first>
  <behavior>
    - The data channel is created with `isOrdered=false` and `maxRetransmits=0`, and `maxPacketLifeTime` is left untouched (a test inspects the `RTCDataChannelConfiguration`).
    - Given a peers roster, one peer connection + one "sensor" data channel is opened per peer.
    - On a peer connection entering `failed`, the manager tears down that peer and re-invokes open for the same peer (self-heal), while still registered.
    - SDP offers and ICE candidates are dispatched through the injected SignalingTransport, not a hardcoded path.
  </behavior>
  <action>
    Implement `PeerConnectionManager` porting `openChannelToPeer()`: an `RTCPeerConnectionFactory`, and per peer an `RTCPeerConnection` (config from `ICEConfig`) + one `RTCDataChannel(forLabel:"sensor", configuration:)` where `configuration.isOrdered = false; configuration.maxRetransmits = 0` (leave `maxPacketLifeTime` default — mutually exclusive per spec; explicit anti-pattern to substitute it). Wire the `RTCPeerConnectionDelegate` equivalents of phone.ts's handlers: connection-state `failed` → if channel was open decrement the open-channel counter + update UI, and if still registered delete the stale peer entry, close the old pc, and re-invoke open for the same peer (self-healing, collect-then-reopen to avoid mutating the map mid-iteration); negotiation → `setLocalDescription` then send an `offer` envelope via the injected transport (log the SDP `a=setup:` value — DTLS passive-role quirk awareness, server patches it in `room.ts`); ICE candidate → send `ice-candidate` envelopes; data-channel open → mark open, increment counter, send `rtc-channel-ready` (and a `phone-state` `channel-recovered` on a recovery reopen); data-channel close → unless an intentional-close flag was set, decrement counter + send `phone-state` `channel-lost`. Store per-peer state in a dictionary keyed by peer id: `{ pc, dc, channelOpen, flagClose }`. Expose `openChannel(toPeer:)`, `applyRemoteAnswer(_:for:)`, `addRemoteCandidate(_:for:)`, and a way to iterate/re-open channels for the reconnect path (Plan 07). Structure the config construction as a separately-callable function (`makeDataChannelConfig()`) so a unit test can inspect its fields without a real network. Write `PeerConnectionManagerTests.swift` asserting `makeDataChannelConfig()` has `isOrdered=false`, `maxRetransmits=0`, and that `maxPacketLifeTime` was not set (PHONE-03).
  </action>
  <verify>
    <automated>cd mobile/ios-app && xcodebuild test -scheme immersiveRT -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:immersiveRTTests/PeerConnectionManagerTests</automated>
  </verify>
  <acceptance_criteria>
    - `PeerConnectionManagerTests` green on the Mac: `makeDataChannelConfig()` yields `isOrdered=false` and `maxRetransmits=0`; a test asserts `maxPacketLifeTime` is the default/unset (not substituted).
    - Code review confirms: one pc + one "sensor" channel per peer; self-heal reopen on `failed`; offer/ICE sent via injected transport; per-peer state dictionary with the four fields.
    - Real multi-desktop fan-out + DTLS behavior is deferred to the Plan 09 on-device checkpoint (referenced in code comment), not claimed here.
  </acceptance_criteria>
  <done>Per-desktop unreliable data channels are created with the exact locked config and self-heal on failure; the config is unit-verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build pipeline ← SPM package registry | A third-party WebRTC binary is compiled into the app. |
| iOS client ↔ peer desktops (P2P DTLS data channel) | Encoded sensor bytes cross a DTLS-secured peer connection. |

## STRIDE Threat Register

ASVS Level 1; block on high severity.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.2-SC | Tampering | SPM install of `stasel/WebRTC` ([ASSUMED] tag) | high | mitigate | Blocking-human legitimacy checkpoint verifies the package on GitHub/Swift Package Index and resolves the real current tag before pinning; never auto-approved (legitimacy checkpoints ignore auto_advance). |
| T-06.2-08 | Information Disclosure | SDP/ICE candidates carrying local IPs | low | accept | Standard WebRTC behavior brokered by the existing server; dev/LAN threat model (D-06). No client-side control added. |
</threat_model>

<verification>
- Mac: `xcodebuild -resolvePackageDependencies` resolves the verified WebRTC tag; `PeerConnectionManagerTests` + `ICEConfigTests` green; app builds with `import WebRTC`.
- On-device (Plan 09): real fan-out to a desktop + DTLS passive-role behavior.
</verification>

<success_criteria>
- WebRTC pinned to a human-verified tag; data channel config is exactly `isOrdered=false`/`maxRetransmits=0`; per-peer fan-out + self-heal ported; ICE mapping correct.
</success_criteria>

<artifacts_produced>
## Artifacts this plan produces
- **SPM dependency:** `stasel/WebRTC` (verified tag) wired to the app target
- **Files:** `WebRTC/PeerConnectionManager.swift`, `WebRTC/ICEConfig.swift`, `immersiveRTTests/PeerConnectionManagerTests.swift`, `immersiveRTTests/ICEConfigTests.swift`
- **Symbols:** `final class PeerConnectionManager` (`openChannel(toPeer:)`, `applyRemoteAnswer(_:for:)`, `addRemoteCandidate(_:for:)`, `makeDataChannelConfig()`, per-peer state struct), `enum/struct ICEConfig` (`iceServers(from:)`)
</artifacts_produced>

<output>
Create `.planning/phases/06.2-ios-native-client-arkit/06.2-06-SUMMARY.md` when done.
</output>
