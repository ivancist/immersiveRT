---
phase: 06.2-ios-native-client-arkit
plan: 04
type: execute
wave: 2
depends_on: [06.2-01]
files_modified:
  - mobile/ios-app/immersiveRT/Transport/WebSocketSignaling.swift
  - mobile/ios-app/immersiveRTTests/WebSocketSignalingTests.swift
autonomous: true
requirements: [PHONE-03, PHONE-06]
must_haves:
  truths:
    - "A WebSocket transport conforming to SignalingTransport connects to `wss://{scannedHost}:9090`, registers with a persistent myId, and round-trips pair/reconnect requests via continuations."
    - "The close handler rejects any in-flight pair/reconnect request with a reason string and suppresses the 'ended' state while a managed reconnect is active."
    - "This is the fallback-of-last-resort transport (D-05), fully functional and zero-dependency (URLSessionWebSocketTask)."
  artifacts:
    - mobile/ios-app/immersiveRT/Transport/WebSocketSignaling.swift
  key_links:
    - "URL host comes from QRTokenParser.host(from:) (Plan 02) — never location.hostname, never hardcoded (Pitfall 5)."
    - "myId is generated once (UUID) and reused across reconnects (Shared Pattern)."
---

<objective>
Implement the WebSocket signaling transport — the zero-dependency, always-available fallback (D-05 fallback-of-last-resort) — as a `SignalingTransport` conformance ported from `phone.ts`'s `connectPhoneWS`/`sendWsMsg`/`onPhoneWsMessage`.

This is a straight cross-language port of a proven web-client path onto first-party `URLSessionWebSocketTask` (iOS 13+, no SPM dependency, the safest part of the whole transport stack per RESEARCH). It carries signaling (register/pair/reconnect) AND ongoing communication (offer/answer/ICE, heartbeat, phone-state) — matching D-04's requirement that transport is not scoped to signaling alone. No ARKit (D-01).

Purpose: Guarantee a working transport regardless of the WebTransport spike outcome (Plan 05); this path must always work.
Output: `WebSocketSignaling.swift` and its tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md

**Environment:** `URLSessionWebSocketTask` against the real dev server needs the Mac + running server + mkcert trust (Plan 09), so full round-trip is an on-device verification. Message-dispatch/continuation logic IS unit-testable by driving the message-handling function with synthetic envelopes (no real socket). `swiftc -parse` is a sandbox syntax pre-check.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.2-ios-native-client-arkit/06.2-RESEARCH.md
@.planning/phases/06.2-ios-native-client-arkit/06.2-PATTERNS.md
@client/src/phone.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WebSocketSignaling conformance</name>
  <files>mobile/ios-app/immersiveRT/Transport/WebSocketSignaling.swift, mobile/ios-app/immersiveRTTests/WebSocketSignalingTests.swift</files>
  <read_first>
    - client/src/phone.ts lines 251-313 (`connectPhoneWS`, `sendWsMsg`, `onPhoneWsMessage`)
    - .planning/phases/06.2-ios-native-client-arkit/06.2-PATTERNS.md ("WebSocketSignaling.swift" section — URL construction, connect+register, message dispatch)
    - mobile/ios-app/immersiveRT/Transport/SignalingTransport.swift, SignalingEnvelope.swift (Plan 01)
    - mobile/ios-app/immersiveRT/QRTokenParser.swift (Plan 02 host extraction)
  </read_first>
  <behavior>
    - On connect, sends a `register` envelope with the persistent myId immediately after the socket opens.
    - `request(pair)` resolves when a `pair-ack` arrives and throws with the server reason when `pair-error` arrives.
    - `request(reconnect)` resolves on `join-ack`, throws on `join-error`.
    - Any non-request message routes to `onServerPush`.
    - On close, an in-flight pair/reconnect request is failed with reason `ws-closed`; `onClosed("ws-closed")` fires.
  </behavior>
  <action>
    Implement `final class WebSocketSignaling: SignalingTransport` on `URLSessionWebSocketTask`. Construct the URL as `wss://\(scannedHost):9090` (host from `QRTokenParser.host`, Plan 02 — replaces `location.hostname`, Pitfall 5). `connect()`: open the task, on open send `SignalingEnvelope(type: .register, from: myId, to: "", payload: [:])` (generate myId once via `UUID().uuidString` and reuse across reconnects — Shared Pattern), start a receive loop that JSON-decodes each text message and dispatches. `send(_:)`: JSON-encode + `send(.string(...))` (fire-and-forget). `request(_:)`: store a `CheckedContinuation` in an optional ivar keyed by kind (pair vs reconnect, mirroring `_pairResolve/_pairReject`/`_reconnectResolve/_reconnectReject`), send the envelope, and resolve/reject it from the receive loop on `pair-ack`/`pair-error`/`join-ack`/`join-error`. Everything else → `onServerPush`. `close()`/close-observed: reject any in-flight continuation with `ws-closed`, fire `onClosed("ws-closed")`, and (per phone.ts) suppress the terminal "ended" state when a managed reconnect is in progress — expose this via a flag the manager sets. Set `isWebTransport = false`. Structure the message-handling as a testable `handle(_ envelope:)` function. Write `WebSocketSignalingTests.swift` driving `handle(_:)` with synthetic `pair-ack`/`pair-error`/`join-ack`/`join-error`/push envelopes and asserting continuation resolution/rejection + push routing (no real socket).
  </action>
  <verify>
    <automated>cd mobile/ios-app && xcodebuild test -scheme immersiveRT -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:immersiveRTTests/WebSocketSignalingTests</automated>
  </verify>
  <acceptance_criteria>
    - Sandbox pre-check: `swiftc -parse` of WebSocketSignaling + Transport deps succeeds.
    - `WebSocketSignalingTests` green: pair-ack resolves the pair request; pair-error rejects with the server reason; join-ack/join-error resolve/reject the reconnect request; unknown message routes to `onServerPush`; simulated close rejects an in-flight request with `ws-closed`.
    - URL built from injected host (test asserts `wss://<host>:9090`), never a hardcoded IP.
    - myId reused across a simulated reconnect (test asserts same UUID on second register).
  </acceptance_criteria>
  <done>The WebSocket fallback transport is complete and its dispatch/continuation logic is deterministically tested; the phase has a guaranteed-working transport.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| iOS client ↔ Rust signaling server (WSS) | Signaling + game-state envelopes cross the network over TLS. |

## STRIDE Threat Register

ASVS Level 1; block on high severity.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.2-04 | Tampering/Spoofing | WSS TLS trust chain | medium | mitigate | Use system TLS trust via `URLSessionWebSocketTask` (never disable validation, never hand-roll). Dev-server uses mkcert CA which must be trusted on the device (Plan 09, Pitfall 2). Cert pinning out of scope for dev-only distribution (D-06). |
| T-06.2-02 | Spoofing | `from` field / myId | low | accept | Server-side `from != my_id` check (V4); client consistently reuses its server-issued UUID. |
</threat_model>

<verification>
- Sandbox: `swiftc -parse` passes.
- Mac: `WebSocketSignalingTests` green.
</verification>

<success_criteria>
- WebSocketSignaling conforms to SignalingTransport, registers with persistent myId, round-trips pair/reconnect, routes pushes, handles close with `ws-closed`, and builds URLs from the scanned host.
</success_criteria>

<artifacts_produced>
## Artifacts this plan produces
- **Files:** `Transport/WebSocketSignaling.swift`, `immersiveRTTests/WebSocketSignalingTests.swift`
- **Symbols:** `final class WebSocketSignaling: SignalingTransport` (with testable `handle(_:)` dispatch), reuse of `myId` generation pattern
</artifacts_produced>

<output>
Create `.planning/phases/06.2-ios-native-client-arkit/06.2-04-SUMMARY.md` when done.
</output>
