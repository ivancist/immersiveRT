---
phase: 06.2-ios-native-client-arkit
plan: 09
type: execute
wave: 5
depends_on: [06.2-02, 06.2-06, 06.2-07, 06.2-08]
files_modified:
  - mobile/ios-app/DEVICE-SETUP.md
  - .planning/phases/06.2-ios-native-client-arkit/06.2-SPIKE-FINDINGS.md
autonomous: false
requirements: [PHONE-03, PHONE-04, PHONE-05, PHONE-07]
user_setup:
  - service: "Physical iPhone (D-07) + running dev server + mkcert CA"
    why: "On-device verification of WebTransport handshake, CoreMotion axis mapping, WebRTC fan-out, and Wake Lock — none are simulator-testable"
    dashboard_config:
      - task: "Trust mkcert root CA on the iPhone: export via `mkcert -CAROOT`, transfer (AirDrop/hosted URL), install profile (Settings → General → VPN & Device Management), enable full trust (Settings → General → About → Certificate Trust Settings)"
        location: "iPhone Settings + dev Mac"
      - task: "Run the dev server reachable from the iPhone's network; deploy the app via Xcode-tethered debug build (D-06)"
        location: "Xcode on the dev Mac"
must_haves:
  truths:
    - "The full XCTest suite is green on the Mac before any on-device sign-off."
    - "mkcert CA is trusted on the physical iPhone so TLS connections to the dev server succeed."
    - "The WebTransport spike outcome is decided within its time-box and documented (WT works, or WS-only fallback with findings) — D-05."
    - "CoreMotion axis mapping is verified on-device: the desktop cube rotates correctly for known 90° rotations (Pitfall 1, D-09)."
    - "One iPhone drives one-or-more real desktops via WebRTC data channels with packets decoding correctly (PHONE-03)."
  artifacts:
    - mobile/ios-app/DEVICE-SETUP.md
    - .planning/phases/06.2-ios-native-client-arkit/06.2-SPIKE-FINDINGS.md
  key_links:
    - "This is the phase's go-signal for transport parity; the ARKit go/no-go gate is Phase 06.3, not here (D-01)."
    - "A failed WebTransport spike degrades to WS-only per D-05 — it does NOT fail the phase."
---

<objective>
Run the on-device verification gates that cannot be automated in a simulator (D-06/D-07): mkcert CA trust setup, full-suite green, the time-boxed WebTransport spike decision (D-05), CoreMotion axis correctness (Pitfall 1, D-09), WebRTC fan-out to a real desktop (PHONE-03), and Wake Lock behavior (PHONE-07).

This closes transport parity for the native client. It explicitly does NOT include any ARKit tracking or the ARKit precision go/no-go — that is Phase 06.3 (D-01). A failed WebTransport spike is an acceptable, documented outcome (WS-only fallback per D-05), not a phase failure.

Purpose: Prove the native app reaches feature parity with the browser phone client on real hardware before ARKit is layered on in 06.3.
Output: `DEVICE-SETUP.md` (repeatable device-trust + run steps) and `06.2-SPIKE-FINDINGS.md` (WebTransport spike result + decision).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md

**Environment:** Every task here runs on the user's Mac + physical iPhone (no Xcode/device in sandbox). The executor writes the docs and drives the checkpoints; the user performs the device steps and reports outcomes.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.2-ios-native-client-arkit/06.2-RESEARCH.md
@.planning/phases/06.2-ios-native-client-arkit/06.2-VALIDATION.md
@.planning/phases/06.2-ios-native-client-arkit/06.2-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write DEVICE-SETUP.md and run the full XCTest suite</name>
  <files>mobile/ios-app/DEVICE-SETUP.md</files>
  <read_first>
    - .planning/phases/06.2-ios-native-client-arkit/06.2-RESEARCH.md (Pitfall 2 — mkcert CA trust steps; Environment Availability)
    - .planning/phases/06.2-ios-native-client-arkit/06.2-VALIDATION.md ("Manual-Only Verifications" + "Full suite command")
  </read_first>
  <action>
    Write `mobile/ios-app/DEVICE-SETUP.md` documenting the repeatable on-device prerequisites: (1) mkcert CA export + transfer + install-profile + enable-full-trust steps (both steps — profile install alone is insufficient, Pitfall 2); (2) running the dev server reachable from the iPhone's LAN; (3) Xcode-tethered debug build/run (D-06, note the 7-day free-Apple-ID expiry); (4) the full XCTest command. Then run the full suite on the Mac to confirm all unit tests across Plans 01-08 are green before on-device sign-off.
  </action>
  <verify>
    <automated>cd mobile/ios-app && xcodebuild test -scheme immersiveRT -destination 'platform=iOS Simulator,name=iPhone 17'</automated>
  </verify>
  <acceptance_criteria>
    - `DEVICE-SETUP.md` exists with the mkcert full-trust steps (both profile install AND certificate-trust toggle), server-reachability, Xcode-tethered run, and full-suite command.
    - The full XCTest suite (all Plan 01-08 test classes) is green on the Mac.
  </acceptance_criteria>
  <done>Device setup is documented and reproducible; the automated suite is fully green as the precondition for on-device checkpoints.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Trust mkcert CA on the physical iPhone</name>
  <action>Blocking human-action checkpoint — no automated path (TLS trust has no click-through for Network.framework/URLSession). A human performs the mkcert CA install + full-trust steps per &lt;how-to-verify&gt; and confirms server reachability.</action>
  <what-built>The app builds and the unit suite is green; the dev server exists (prior phases). The one thing only a human can do: install and fully-trust the mkcert root CA on the physical iPhone (no CLI/API path; TLS validation has no click-through for Network.framework/URLSession).</what-built>
  <how-to-verify>
    Follow `mobile/ios-app/DEVICE-SETUP.md`: export mkcert root CA (`mkcert -CAROOT`), transfer to the iPhone (AirDrop or hosted URL), install the configuration profile (Settings → General → VPN & Device Management), THEN enable full trust (Settings → General → About → Certificate Trust Settings). Confirm the dev server is running and reachable from the iPhone's network.
  </how-to-verify>
  <resume-signal>Reply "CA trusted" once the profile is installed AND full trust is enabled and the server is reachable, or describe the blocker.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: WebTransport spike on-device decision (D-05 time-box)</name>
  <action>Blocking human-verify checkpoint — no automated action (QUIC/h3 over a real network path + TLS trust + live server is not simulator-testable). A human runs the on-device spike session per &lt;how-to-verify&gt; and returns whether WT connected or the app fell back to WS. This is the second half of the D-05 time-box.</action>
  <what-built>The `WebTransportSignaling` client (Plan 05) with the hand-rolled RFC 9220 handshake, and the `TransportManager` (Plan 07) that tries WT first then falls back to WS. This is the time-boxed spike decision point (D-05).</what-built>
  <how-to-verify>
    On the trusted iPhone with the app deployed via Xcode: launch, scan the desktop QR, and observe whether the WebTransport path connects (Xcode console: QUIC/h3 connection opens, extended-CONNECT gets 200, a `register` envelope is sent, bidi stream opens — or the connection stays open matching `wt_server.rs`'s no-response-on-register behavior). This is the single on-device debug session that constitutes the second half of the spike time-box. If WT does not complete the handshake within this session, confirm the app automatically falls back to WebSocket and pairs successfully over WS.
  </how-to-verify>
  <resume-signal>Reply "WT works" (handshake succeeded) or "WS fallback" (WT did not land within the time-box; app paired over WebSocket) — either is an acceptable phase outcome per D-05.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Record the WebTransport spike findings + decision</name>
  <files>.planning/phases/06.2-ios-native-client-arkit/06.2-SPIKE-FINDINGS.md</files>
  <read_first>
    - .planning/phases/06.2-ios-native-client-arkit/06.2-RESEARCH.md ("Architecture Patterns → Pattern 1", Open Questions 2 & 3)
  </read_first>
  <action>
    Based on the preceding checkpoint outcome, write `06.2-SPIKE-FINDINGS.md` documenting the D-05 decision: whether the native WebTransport handshake succeeded, what failed if it did not (which handshake step, any server-side QUIC/h3 log detail), and the resulting decision (WT primary retained, or WS-only fallback adopted for 06.2 with the spike preserved behind runtime fallback for a future revisit). Record the time-box that was spent (one impl cycle + one on-device session) so a future phase can decide whether to re-attempt.
  </action>
  <verify>
    <automated>test -f .planning/phases/06.2-ios-native-client-arkit/06.2-SPIKE-FINDINGS.md && grep -qiE 'WT works|WS fallback|WebSocket' .planning/phases/06.2-ios-native-client-arkit/06.2-SPIKE-FINDINGS.md</automated>
  </verify>
  <acceptance_criteria>
    - `06.2-SPIKE-FINDINGS.md` exists and records the spike outcome, the concrete failure point (if any), the D-05 decision (WT-primary vs WS-only), and the time-box spent.
  </acceptance_criteria>
  <done>The WebTransport spike is closed with a documented, defensible decision per D-05.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: CoreMotion axis-mapping on-device verification (Pitfall 1, D-09)</name>
  <action>Blocking human-verify checkpoint — no automated action (CoreMotion sensors are unavailable in the Simulator; requires the physical iPhone, D-07). A human rotates the phone through known 90° yaw/pitch/roll rotations per &lt;how-to-verify&gt; and confirms the desktop cube rotates correctly, reporting any wrong axis/direction.</action>
  <what-built>`CoreMotionSource` (Plan 03) mapping `CMQuaternion(x,y,z,w)` → packet `(qw,qx,qy,qz)` with `.xArbitraryZVertical`, the byte-identical encoder (Plan 02), and the full send path (Plan 07). The axis mapping is RESEARCH's reasoned starting point, NOT verified (Pitfall 1, D-09).</what-built>
  <how-to-verify>
    With the iPhone paired to a real desktop: rotate the phone through known reference rotations — 90° yaw (turn left/right), 90° pitch (tilt forward/back), 90° roll (tilt side to side). Confirm the desktop cube rotates in the MATCHING direction and axis for each. Where possible, compare directly against the web phone client's behavior on the same physical device. Watch for the warning signs in Pitfall 1: cube rotating around the wrong axis, opposite direction, or yaw drifting/snapping (would indicate a wrong reference frame). If the mapping is wrong, report which axis/direction is off so the encoder mapping (or reference frame) can be corrected.
  </how-to-verify>
  <resume-signal>Reply "axis correct" if all three rotations match, or describe exactly which axis/direction is wrong.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: WebRTC fan-out + Wake Lock on-device verification (PHONE-03/04/07)</name>
  <action>Blocking human-verify checkpoint — no automated action (native DTLS stack + real multi-desktop peer connections are not simulator-testable). A human pairs the iPhone to one then two desktops per &lt;how-to-verify&gt;, confirms channels open and packets decode, and confirms Wake Lock behavior + send rate.</action>
  <what-built>`PeerConnectionManager` (Plan 06) unreliable data-channel fan-out (`isOrdered=false, maxRetransmits=0`), `TransportManager` (Plan 07) fan-out + reconnect, Wake Lock (Plan 08), on the real `stasel/WebRTC` iOS DTLS stack.</what-built>
  <how-to-verify>
    1. Pair the iPhone to a room with one desktop: confirm the `RTCDataChannel` opens (status shows 1/1 channels) and the desktop decodes/renders the phone's orientation packets (cube moves). 2. Add a second desktop to the room: confirm the phone fans out a second channel (2/2) and both desktops receive the phone's motion. 3. Confirm the DTLS handshake completes on the native stack (the server's `a=setup:active`→`passive` patch applies to native offers too — watch for any DTLS ClientHello issue). 4. Wake Lock: confirm the screen does not auto-lock during an active session, and that after backgrounding the app the idle-disable is released (screen locks normally). 5. Sample the send rate is ~55-60Hz (PHONE-04).
  </how-to-verify>
  <resume-signal>Reply "fan-out verified" if channels open to all desktops and packets decode + Wake Lock behaves, or describe the failure (which desktop, DTLS error, rate, or Wake Lock issue).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| iPhone ↔ dev server (TLS via mkcert CA) | All transport traffic depends on the device trusting the local CA. |
| iPhone ↔ peer desktops (DTLS) | Real peer connections established on the native WebRTC stack. |

## STRIDE Threat Register

ASVS Level 1; block on high severity.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.2-04 | Tampering/Spoofing | mkcert CA trust on device | medium | mitigate | Trust is scoped to the dev CA on a dev device (D-06); documented one-time setup (DEVICE-SETUP.md). Production cert pinning out of scope for Xcode-tethered debug distribution. |
| T-06.2-01 | Information Disclosure | reconnect token in Xcode console during debug | medium | mitigate | Confirm during the on-device session that the token value is never printed to the console (T-03-09 / V3). |
</threat_model>

<verification>
- Mac: full XCTest suite green (Task 1).
- On-device: mkcert trust established; WebTransport spike decided + documented; CoreMotion axis correct; WebRTC fan-out to ≥1 desktop with packets decoding; Wake Lock behaves.
</verification>

<success_criteria>
- Full unit suite green; mkcert CA trusted on device; WT spike decision documented (D-05); axis mapping verified (D-09); WebRTC fan-out + Wake Lock verified on real hardware (PHONE-03/04/07). Transport parity with the web client is demonstrated; ARKit remains Phase 06.3 (D-01).
</success_criteria>

<artifacts_produced>
## Artifacts this plan produces
- **Files:** `mobile/ios-app/DEVICE-SETUP.md`, `.planning/phases/06.2-ios-native-client-arkit/06.2-SPIKE-FINDINGS.md`
- **Outcomes:** documented WebTransport spike decision (D-05), verified CoreMotion axis mapping (D-09), verified WebRTC fan-out + Wake Lock on device
</artifacts_produced>

<output>
Create `.planning/phases/06.2-ios-native-client-arkit/06.2-09-SUMMARY.md` when done.
</output>
