---
phase: 02-signaling-turn-and-deployment
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - server/src/wt_server.rs
  - server/src/ws_server.rs
  - server/src/main.rs
  - server/tests/ws_echo.rs
  - server/tests/broker_relay.rs
autonomous: true
requirements:
  - INFRA-02
  - INFRA-03

must_haves:
  truths:
    - "Both WS and WT handlers register clients in the shared SignalingBroker and forward JSON envelopes to target client IDs via broker::route per D-03"
    - "ws_server::run_with_listener accepts Arc<SignalingBroker> and Option<TlsAcceptor> — tests pass None for TLS, prod passes Some(acceptor)"
    - "wt_server::run accepts Arc<SignalingBroker> — accept_bi streams carry inbound messages; conn.open_bi() carries outbound messages pushed from broker"
    - "WS default port default in main.rs is '9090' not '8080' per D-02"
    - "rustls::crypto::aws_lc_rs::default_provider().install_default().ok() is called before any TLS code in main() per RESEARCH.md Pitfall 3"
    - "test_broker_relay_ws passes: client A registers as 'phone-1', client B registers as 'desktop-1'; A sends offer to B; B receives it via broker relay"
  artifacts:
    - "server/src/wt_server.rs — modified: add broker param, select! relay loop with open_bi push"
    - "server/src/ws_server.rs — modified: add broker + Option<TlsAcceptor> params, WSS TLS support, select! relay loop"
    - "server/src/main.rs — modified: broker construction, WS port 9090, crypto provider init, pass broker+TLS to both listeners, new env vars"
    - "server/tests/ws_echo.rs — modified: updated signature call with broker + None TLS"
    - "server/tests/broker_relay.rs — new: integration test for cross-client WS relay"
  key_links:
    - "ws_server::run(port, broker, cert_path, key_path) loads TlsAcceptor and passes Some(acceptor) to run_with_listener — run_with_listener accepts Option<TlsAcceptor> to keep existing tests working with None"
    - "wt_server::run receives broker via Arc<SignalingBroker>; the broker is cloned into each tokio::spawn"
    - "broker_relay.rs imports immersive_rt_server::ws_server::run_with_listener and immersive_rt_server::broker::SignalingBroker via lib.rs (set up in Plan 01)"
    - "The 'register' message type is handled specially in both handlers: extract from.clone() as my_id, call broker.register(my_id) to get the outbound receiver, then enter the relay loop"
---

## Phase Goal

**As a** developer, **I want to** verify WebRTC signaling brokered by the Rust server with ephemeral TURN credentials, **so that** phones and desktops can establish P2P data channels via the complete `docker compose up` stack.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-signaling-turn-and-deployment/02-CONTEXT.md
@.planning/phases/02-signaling-turn-and-deployment/02-RESEARCH.md
@.planning/phases/02-signaling-turn-and-deployment/02-PATTERNS.md
@.planning/phases/02-signaling-turn-and-deployment/02-01-SUMMARY.md
</context>

<objective>
Wire the SignalingBroker (created in Plan 01) into both transport handlers and update the server entry point. This is the plan that activates signaling relay: both WebTransport and WebSocket connections can now register client IDs, route offer/answer/ICE messages to each other, and use WSS TLS for the WebSocket fallback path.

Purpose: Satisfy INFRA-02 (phone↔desktop relay) and INFRA-03 (desktop↔desktop relay) — the same broker handles both since routing is purely by client ID with no room semantics (Phase 3 adds those). Verified by an automated integration test that routes an offer between two WS clients.

Output: Both transport handlers converted from echo to signaling relay; main.rs updated with broker + correct defaults; integration test proving WS-path relay end-to-end.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: wt_server.rs — broker integration + select! relay loop (D-01, D-03, INFRA-02)</name>
  <files>server/src/wt_server.rs</files>
  <read_first>
    server/src/wt_server.rs — read the ENTIRE current file; understand the three-step handshake (lines 41-54), the accept_bi loop (lines 56-113), and the buffer-size guard pattern (lines 67-78)
    server/src/broker.rs — SignalingBroker::register, unregister, route signatures created in Plan 01
    server/src/signaling.rs — parse_envelope signature and SignalingEnvelope fields created in Plan 01
    .planning/phases/02-signaling-turn-and-deployment/02-PATTERNS.md section "wt_server.rs" — full change specification including the accept_bi loop structure and imports to replace
    .planning/phases/02-signaling-turn-and-deployment/02-RESEARCH.md "Pattern 2" (select! loop shape) and "Pitfall 5" (accept_bi vs open_bi) — critical for understanding the concurrency restructuring
  </read_first>
  <behavior>
    - Server accepts WT connection, awaits first client-opened stream (accept_bi), reads "register" message, extracts from field as my_id, registers my_id with broker
    - Main relay loop: tokio::select! with two arms — (1) accept_bi for client-opened inbound streams: parse SignalingEnvelope, if route fails log tracing::warn!(to=%envelope.to, "signaling target not connected, dropping") per D-05; (2) broker_rx.recv() for outbound: open_bi, write_all the payload, finish the send stream
    - Connection teardown: broker.unregister(&my_id) on loop exit regardless of error path
    - Malformed envelope: parse_envelope returns None → tracing::warn! and continue (never panic, T-01-06)
  </behavior>
  <action>
    Modify server/src/wt_server.rs. Replace `use crate::echo::{now_ms, EchoMessage};` with `use crate::broker::SignalingBroker; use crate::signaling::parse_envelope; use std::sync::Arc;`. Add `broker: Arc<SignalingBroker>` parameter to both `run()` and `handle_wt_connection()`. In `run()` clone broker into the tokio::spawn closure. Keep the three-step handshake (incoming.await → request.accept()) unchanged.

    In handle_wt_connection: after accepting the connection, read the first client-opened stream via accept_bi to get the "register" message. Extract my_id = envelope.from.clone(). Call let mut broker_rx = broker.register(my_id.clone()); . Then enter the relay loop with tokio::select!:

    Arm 1 (inbound): conn.accept_bi().await on success: read all bytes from recv using the same buf accumulation pattern (4096-byte chunks, loop until None/FIN, 65_536-byte overflow guard with buf.clear()). Parse with parse_envelope; if None log warn and continue. If envelope.msg_type is "register" skip routing (already registered). For all other types: if !broker.route(&envelope.to, serde_json::to_vec(&envelope)?) log tracing::warn!(to=%envelope.to, "signaling target not connected, dropping") per D-05.

    Arm 2 (outbound): Some(payload) = broker_rx.recv(): open a new bidirectional stream via conn.open_bi().await; write payload via send.write_all(&payload).await; close the stream via send.finish().await. Any error on open_bi or write logs tracing::warn! and continues (does not abort the relay loop — one failed push should not kill the connection).

    After the loop exits: broker.unregister(&my_id);. Keep all existing tracing::error!/tracing::info! call sites for connection-level events.
  </action>
  <verify>
    <automated>cd /home/ivancist/Documents/immersiveRT && RUSTFLAGS="-D warnings" cargo check -p immersive-rt-server 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - server/src/wt_server.rs compiles without errors or warnings when run() and handle_wt_connection() receive an Arc<SignalingBroker> parameter
    - wt_server.rs does NOT contain references to EchoMessage or echo::now_ms (those imports are removed)
    - wt_server.rs contains broker.register(...) and broker.unregister(...) calls
    - wt_server.rs contains tokio::select! with both accept_bi and broker_rx.recv() arms
    - broker.route returns false case logs tracing::warn! with the target client ID (D-05 compliance)
    - cargo check -p immersive-rt-server exits 0 with zero warnings (note: main.rs still references the old wt_server::run signature at this point, so cargo check may warn about unused params — acceptable until Task 3 updates main.rs)
  </acceptance_criteria>
  <done>wt_server.rs contains the full broker-integrated select! relay loop; compiles; echo behavior is removed</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ws_server.rs — broker + WSS TLS + select! relay loop (D-01, D-02, INFRA-02)</name>
  <files>server/src/ws_server.rs</files>
  <read_first>
    server/src/ws_server.rs — read the ENTIRE current file; understand run/run_with_listener split (lines 15-19), semaphore pattern (lines 22-28), handle_ws_connection echo loop (lines 48-84)
    server/src/broker.rs — SignalingBroker::register, unregister, route signatures
    server/src/signaling.rs — parse_envelope signature
    .planning/phases/02-signaling-turn-and-deployment/02-PATTERNS.md section "ws_server.rs" — WSS TLS insertion point, select! relay loop, control-frame filter to preserve
    .planning/phases/02-signaling-turn-and-deployment/02-RESEARCH.md Code Examples "tokio-rustls server-side TLS acceptor" — exact load_tls_acceptor implementation using rustls_pemfile 2.x API
    .planning/phases/02-signaling-turn-and-deployment/02-RESEARCH.md "Pitfall 3" — must NOT enable ring feature; TlsAcceptor must use the aws_lc_rs provider
  </read_first>
  <behavior>
    - run_with_listener(listener, broker, None) → plain WS upgrade (for existing tests: ws_echo.rs passes None, broker_relay.rs passes None)
    - run_with_listener(listener, broker, Some(acceptor)) → TLS wraps TcpStream before accept_async_with_config (WSS path for production)
    - run(port, broker, cert_path, key_path) → loads TlsAcceptor from cert_path/key_path, passes Some(acceptor) to run_with_listener
    - Connection handler relay loop: tokio::select! — inbound frames from ws sink/stream go to broker.route; outbound broker_rx payloads are sent as WS Text frames
    - First inbound Text frame containing {"type":"register","from":"<id>",...}: extract id, call broker.register(id) → broker_rx; subsequent frames route to their "to" targets
    - D-05 compliance: broker.route returns false → tracing::warn!(to=%envelope.to, "signaling target not connected, dropping")
    - Control frames (Ping, Pong, Close) still skipped via the existing match guard — tungstenite handles them internally
  </behavior>
  <action>
    Modify server/src/ws_server.rs. Add imports: use tokio_rustls::{TlsAcceptor, rustls::ServerConfig}; use rustls_pemfile::{certs, private_key}; use std::io::BufReader; use crate::broker::SignalingBroker; use crate::signaling::parse_envelope; use std::sync::Arc;.

    Add a module-level function load_tls_acceptor(cert_path: &str, key_path: &str) → anyhow::Result<TlsAcceptor>: open cert_path with BufReader, collect via rustls_pemfile::certs into Vec<CertificateDer>, open key_path with BufReader, extract via rustls_pemfile::private_key (return error if None). Build ServerConfig::builder().with_no_client_auth().with_single_cert(cert_chain, key)?, wrap in TlsAcceptor::from(Arc::new(config)).

    Change run signature to: pub async fn run(port: u16, broker: Arc<SignalingBroker>, cert_path: &str, key_path: &str) → anyhow::Result<()>. Inside run: let tls = load_tls_acceptor(cert_path, key_path).ok().map(|a| a); pass to run_with_listener(listener, broker, tls).await. If load_tls_acceptor fails, log tracing::warn! and pass None (plain WS fallback for dev environments without certs).

    Change run_with_listener signature to: pub async fn run_with_listener(listener: TcpListener, broker: Arc<SignalingBroker>, tls: Option<TlsAcceptor>) → anyhow::Result<()>. In the accept loop, clone broker and tls into each tokio::spawn. In handle_ws_connection add Arc<SignalingBroker> and Option<TlsAcceptor> params.

    In handle_ws_connection: if let Some(ref acceptor) = tls, wrap tcp_stream with acceptor.accept(stream).await before passing to accept_async_with_config. Replace the echo loop with a tokio::select! relay loop: split the WS stream (split into write half and read half); loop on select!: (1) msg = read.next().await — on None break; parse as SignalingEnvelope via parse_envelope on Text/Binary bytes; "register" message → broker.register(from) → broker_rx; other types → broker.route; (2) Some(payload) = broker_rx.recv() → write.send(Message::Text(String::from_utf8_lossy(&payload).into_owned())).await. Keep the existing control-frame match guard (Message::Text | Message::Binary only). On loop exit: broker.unregister(&my_id).

    Handle the "not yet registered" state: broker_rx starts as None (Option); after receiving the first "register" message it becomes Some. Before registration, inbound messages that are not "register" type log tracing::warn! and are dropped (cannot route without an ID).
  </action>
  <verify>
    <automated>cd /home/ivancist/Documents/immersiveRT && RUSTFLAGS="-D warnings" cargo check -p immersive-rt-server 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - server/src/ws_server.rs compiles without errors or warnings with the new function signatures
    - ws_server.rs run() signature is pub async fn run(port: u16, broker: Arc<SignalingBroker>, cert_path: &str, key_path: &str) → anyhow::Result<()>
    - ws_server.rs run_with_listener() signature is pub async fn run_with_listener(listener: TcpListener, broker: Arc<SignalingBroker>, tls: Option<TlsAcceptor>) → anyhow::Result<()>
    - ws_server.rs contains load_tls_acceptor function using rustls_pemfile::certs and rustls_pemfile::private_key (not the deprecated free-function API)
    - ws_server.rs does NOT reference EchoMessage — the echo loop is fully replaced by the relay loop
    - ws_server.rs contains broker.register and broker.unregister calls
    - cargo check exits 0 (main.rs signature mismatch warnings are acceptable at this checkpoint since Task 3 updates main.rs)
  </acceptance_criteria>
  <done>ws_server.rs contains the full broker-integrated select! relay loop with optional WSS TLS; echo behavior is removed; TLS loading function is in place</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: main.rs wiring + ws_echo.rs update + broker_relay.rs integration test (D-02, D-03, INFRA-02, INFRA-03)</name>
  <files>server/src/main.rs, server/tests/ws_echo.rs, server/tests/broker_relay.rs</files>
  <read_first>
    server/src/main.rs — read the ENTIRE current file; understand env var config pattern (lines 9-20), tokio::try_join! call (lines 26-29)
    server/tests/ws_echo.rs — read the ENTIRE current test; understand how run_with_listener is called (line 14) — must be updated for new signature
    .planning/phases/02-signaling-turn-and-deployment/02-PATTERNS.md sections "main.rs" and "broker_relay.rs" — exact env var pattern, broker insertion point, CryptoProvider fix, and two-client relay test structure
    .planning/phases/02-signaling-turn-and-deployment/02-RESEARCH.md "Pitfall 3" — rustls crypto provider conflict fix (install_default before any TLS code)
    server/src/broker.rs — SignalingBroker::new() to construct the shared broker
  </read_first>
  <behavior>
    - broker_relay test: two WS clients connect to the same server; client A sends {"type":"register","from":"phone-1"}; client B sends {"type":"register","from":"desktop-1"}; client A sends {"type":"offer","from":"phone-1","to":"desktop-1","payload":{}}; client B receives the offer with msg_type="offer" and from="phone-1"
    - WS_PORT default in main.rs is "9090" not "8080" (D-02)
    - main.rs constructs Arc<SignalingBroker> once, clones into both wt_server::run and ws_server::run calls
    - rustls CryptoProvider is installed before any listener starts — prevents runtime panic if TLS is initialized concurrently
  </behavior>
  <action>
    Modify server/src/main.rs: add mod broker; mod signaling; mod turn_creds; declarations alongside the existing mod echo; mod wt_server; mod ws_server; lines. Change WS_PORT default from "8080" to "9090" (D-02). After tracing_subscriber::fmt::init(); add: let _ = rustls::crypto::aws_lc_rs::default_provider().install_default(); (RESEARCH.md Pitfall 3 — idempotent, .ok() is not needed since we don't care about "already installed" error; use let _ = to silence unused-result warning). Construct let broker = Arc::new(broker::SignalingBroker::new()); before tokio::try_join!. Update the try_join! to pass broker.clone() and cert_path/key_path to both listeners: wt_server::run(&cert_path, &key_path, wt_port, broker.clone()) and ws_server::run(ws_port, broker.clone(), &cert_path, &key_path). Add use std::sync::Arc; import.

    Modify server/tests/ws_echo.rs: update the tokio::spawn line to pass the new run_with_listener signature. Create a broker: let broker = Arc::new(immersive_rt_server::broker::SignalingBroker::new()); and pass broker and None (for TLS): tokio::spawn(immersive_rt_server::ws_server::run_with_listener(listener, broker, None)). Add use std::sync::Arc; to imports.

    Create server/tests/broker_relay.rs: #[tokio::test] async fn test_broker_relay_ws(). Bind a TcpListener at 127.0.0.1:0 (port 0 for OS-assigned port, same as ws_echo.rs). Construct broker. Spawn server: tokio::spawn(immersive_rt_server::ws_server::run_with_listener(listener, broker, None)). Connect two WS clients (url = format!("ws://{}", addr)): client_a and client_b. client_a sends {"type":"register","from":"phone-1","to":"","payload":null}. client_b sends {"type":"register","from":"desktop-1","to":"","payload":null}. Allow a brief yield (tokio::task::yield_now().await) for registration to complete. client_a sends {"type":"offer","from":"phone-1","to":"desktop-1","payload":{}}. Read client_b's next message: parse as SignalingEnvelope, assert msg_type == "offer" and from == "phone-1". Use the same exhaustive match style as ws_echo.rs (Message::Text → assert, other → panic!).
  </action>
  <verify>
    <automated>cd /home/ivancist/Documents/immersiveRT && RUSTFLAGS="-D warnings" cargo test --workspace 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - server/src/main.rs WS_PORT default value is "9090" (grep: `unwrap_or_else(|_| "9090".into())`)
    - server/src/main.rs contains rustls::crypto::aws_lc_rs::default_provider().install_default() call before try_join!
    - server/src/main.rs constructs Arc<broker::SignalingBroker::new()> and passes broker.clone() to both wt_server::run and ws_server::run
    - server/tests/ws_echo.rs test_ws_echo passes with the updated run_with_listener signature (broker + None TLS)
    - server/tests/broker_relay.rs exists with test_broker_relay_ws
    - RUSTFLAGS="-D warnings" cargo test --workspace exits 0: test_ws_echo, test_broker_relay_ws, test_now_ms_nonzero, test_echo_message_round_trip, broker unit tests, test_turn_credential_known_vector all pass; zero warnings
  </acceptance_criteria>
  <done>Full signaling relay is active on both transports; broker_relay integration test proves WS offer routing end-to-end; all existing tests still pass; zero warnings</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| network → ws_server / wt_server | Untrusted JSON arrives from any connected client — malformed or oversized payloads must not crash the handler |
| broker → target client | Broker routes to any registered ID with no ownership check in Phase 2 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-03 | Denial of Service | Malformed JSON signaling envelope in ws_server / wt_server handler | high | mitigate | parse_envelope returns None on error; handler logs tracing::warn! and continues — handler never panics on network input (T-01-06 precedent) |
| T-02-04 | Tampering | rustls CryptoProvider conflict — ring vs aws_lc_rs loaded by different crates causes runtime panic | high | mitigate | rustls::crypto::aws_lc_rs::default_provider().install_default() called once at the top of main() before any TLS listener starts (RESEARCH.md Pitfall 3) |
| T-02-05 | Elevation of Privilege | Broker forwards signaling messages to any registered client ID with no pairing or room ownership check | medium | accept | Acceptable for Phase 2 scope per CONTEXT.md security contribution; Phase 3 adds pairing/room ownership boundaries (SESS-01..06) |
| T-02-06 | Denial of Service | Oversized WT stream (>65,536 bytes) consuming memory | medium | mitigate | Buffer size guard preserved from Phase 1 echo code — buf.clear() + continue on overflow |
</threat_model>

<verification>
After this plan: RUSTFLAGS="-D warnings" cargo test --workspace exits 0 — test_ws_echo, test_broker_relay_ws, all unit tests pass with zero warnings. The WS relay path is proven automated. The WT relay path is structurally complete but verified manually (browser-based) as part of Phase 2 Success Criterion 1.
</verification>

<success_criteria>
- wt_server.rs: broker-integrated select! relay loop; no echo code
- ws_server.rs: broker-integrated select! relay loop + WSS TLS (Option<TlsAcceptor>); no echo code
- main.rs: WS default port 9090 (D-02); rustls provider installed; broker constructed and passed to both listeners
- test_broker_relay_ws passes: offer routed from phone-1 to desktop-1 via WS signaling broker
- cargo test --workspace exits 0, zero warnings
</success_criteria>

## Artifacts This Plan Produces

| Symbol | Kind | File |
|--------|------|------|
| wt_server::run | modified pub fn (added broker param) | server/src/wt_server.rs |
| ws_server::run | modified pub fn (added broker, cert_path, key_path params) | server/src/ws_server.rs |
| ws_server::run_with_listener | modified pub fn (added broker, tls: Option<TlsAcceptor> params) | server/src/ws_server.rs |
| ws_server::load_tls_acceptor | new pub(crate) fn → anyhow::Result<TlsAcceptor> | server/src/ws_server.rs |
| test_broker_relay_ws | integration test | server/tests/broker_relay.rs |

<output>
Create `.planning/phases/02-signaling-turn-and-deployment/02-02-SUMMARY.md` when done
</output>
