---
phase: 02-signaling-turn-and-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/Cargo.toml
  - server/src/broker.rs
  - server/src/signaling.rs
  - server/src/turn_creds.rs
  - server/src/lib.rs
autonomous: true
requirements:
  - INFRA-02
  - INFRA-03
  - INFRA-04

must_haves:
  truths:
    - "SignalingBroker::route returns false for unknown client IDs; the broker itself does not log — caller must log the warning per D-05"
    - "SignalingBroker::register returns a distinct UnboundedReceiver per caller — two registrations for different IDs produce two independent channels"
    - "generate_turn_credentials username = '{expiry}:{userid}' where expiry = now + ttl_seconds (NOT now alone — Pitfall 1); password = base64(HMAC-SHA1(secret, username))"
    - "SignalingEnvelope round-trips through serde_json with wire key 'type' not 'msg_type' per D-04"
    - "cargo test --workspace exits 0 with zero warnings after this plan"
  artifacts:
    - "server/src/broker.rs — SignalingBroker struct, ClientId type alias"
    - "server/src/signaling.rs — SignalingEnvelope struct, parse_envelope fn"
    - "server/src/turn_creds.rs — TurnCredentials struct, generate_turn_credentials fn"
    - "server/src/lib.rs — pub mod broker; pub mod signaling; pub mod turn_creds added"
  key_links:
    - "SignalingBroker internally wraps Arc<DashMap> — derive Clone on the struct so handlers clone the broker handle, not the DashMap"
    - "turn_creds: expiry = SystemTime::now().as_secs() + ttl_seconds; HMAC key input = shared_secret.as_bytes(); message input = username.as_bytes()"
    - "lib.rs must export all three new modules so integration tests in server/tests/ can import via immersive_rt_server::"
---

## Phase Goal

> Note: The ROADMAP **Goal** for Phase 2 is not in user-story format. Run `/gsd mvp-phase 02` to formalize. Derived story used here:

**As a** developer, **I want to** verify WebRTC signaling brokered by the Rust server with ephemeral TURN credentials, **so that** phones and desktops can establish P2P data channels via the complete `docker compose up` stack.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-signaling-turn-and-deployment/02-CONTEXT.md
@.planning/phases/02-signaling-turn-and-deployment/02-RESEARCH.md
@.planning/phases/02-signaling-turn-and-deployment/02-PATTERNS.md
</context>

<objective>
Create three new server-side modules (broker, signaling, turn_creds) and update Cargo.toml with all Phase 2 dependencies. No existing transport files are modified in this plan — only Cargo.toml and lib.rs are touched alongside the three new source files. The signaling relay is inert until Plan 02 wires it into the transports.

Purpose: Establish core data structures and algorithms (D-03, D-04, D-06) proven via unit tests before touching transport code. The known-vector test for HMAC-SHA1 is the only early warning for the silent-failure mode where coturn rejects 100% of credential requests with 401.

Output:
- server/src/broker.rs — SignalingBroker (DashMap-backed in-process relay registry, per D-03)
- server/src/signaling.rs — SignalingEnvelope (JSON envelope struct per D-04)
- server/src/turn_creds.rs — generate_turn_credentials (HMAC-SHA1 ephemeral TURN credentials per INFRA-04, D-06)
- server/Cargo.toml — 7 new deps (dashmap 6.2, hmac 0.13, sha1 0.11, base64 0.22, tokio-rustls 0.26, rustls-pemfile 2.2, axum 0.8)
- server/src/lib.rs — updated to export all new modules
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cargo.toml + broker.rs + signaling.rs (D-03, D-04, INFRA-02, INFRA-03)</name>
  <files>server/Cargo.toml, server/src/broker.rs, server/src/signaling.rs</files>
  <read_first>
    server/Cargo.toml — current deps; append new entries in the same bare-version-string style (no inline comments unless a pitfall note is essential)
    server/src/echo.rs lines 18-25 — serde rename = "type" pattern and #[cfg(test)] mod tests skeleton to mirror in both new files
    server/src/ws_server.rs lines 2-4 — Arc pattern for shared-state injection (Arc::new wrapping the DashMap, .clone() into spawned tasks)
    .planning/phases/02-signaling-turn-and-deployment/02-PATTERNS.md sections "broker.rs" and "signaling.rs" — exact struct implementations and critical warnings
    .planning/phases/02-signaling-turn-and-deployment/02-RESEARCH.md "Package Legitimacy Audit" table — all 7 packages Approved/VERIFIED, no blocking checkpoint needed
    .planning/phases/02-signaling-turn-and-deployment/02-RESEARCH.md "Pattern 1" and "Anti-Patterns to Avoid" — DashMap guard lifetime rules
  </read_first>
  <behavior>
    - broker: register("id-A") → unique UnboundedReceiver; register("id-B") → second independent receiver; payloads sent to id-A never reach id-B's receiver
    - broker: route("id-A", payload) → true; payload arrives on id-A's receiver channel
    - broker: route("id-unknown", payload) → returns false; broker does not call tracing::warn! — the caller is responsible for logging per D-05
    - broker: unregister("id-A"); route("id-A", payload) → returns false
    - signaling: JSON round-trip — SignalingEnvelope with msg_type="offer" serializes as {"type":"offer",...}, not {"msg_type":"offer",...}
    - signaling: parse_envelope on invalid bytes returns None (never panics)
  </behavior>
  <action>
    In server/Cargo.toml append to [dependencies]: dashmap = "6.2", hmac = "0.13", sha1 = "0.11", base64 = "0.22", tokio-rustls = "0.26", rustls-pemfile = "2.2", axum = "0.8". Do NOT add a ring feature flag to any rustls-related crate — the workspace must rely on the aws_lc_rs provider already active via wtransport → quinn (RESEARCH.md Pitfall 3; adding ring alongside aws_lc_rs panics at runtime).

    Create server/src/broker.rs: pub type ClientId = String; #[derive(Clone)] pub struct SignalingBroker with a single private field clients: std::sync::Arc<dashmap::DashMap<ClientId, tokio::sync::mpsc::UnboundedSender<Vec<u8>>>>. Implement: pub fn new() → Self; pub fn register(id: ClientId) → mpsc::UnboundedReceiver<Vec<u8>> (creates unbounded channel, inserts tx into the DashMap, returns rx); pub fn unregister(id: &str) (removes from map); pub fn route(to: &str, payload: Vec<u8>) → bool (match on DashMap::get(to): if Some(sender) call sender.send(payload).is_ok(), if None return false — do NOT call tracing::warn! here, callers must log per D-05). Critical: do not hold the DashMap Ref guard across any .await — mpsc::UnboundedSender::send is synchronous so this is safe by construction for unbounded channels, but be explicit. Add #[cfg(test)] mod tests covering all four behavior cases listed above using #[tokio::test].

    Create server/src/signaling.rs: #[derive(Debug, Serialize, Deserialize)] pub struct SignalingEnvelope with fields: #[serde(rename = "type")] pub msg_type: String, pub from: String, #[serde(default)] pub to: String, #[serde(default)] pub payload: serde_json::Value. Add pub fn parse_envelope(bytes: &[u8]) → Option<SignalingEnvelope> that returns None on serde_json::from_slice error (never panics — T-01-06 precedent). Add #[cfg(test)] test asserting {"type":"offer",...} round-trips correctly and that the wire key is "type" not "msg_type".
  </action>
  <verify>
    <automated>cd /home/ivancist/Documents/immersiveRT && RUSTFLAGS="-D warnings" cargo test -p immersive-rt-server 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - server/Cargo.toml contains exactly these new lines: dashmap = "6.2", hmac = "0.13", sha1 = "0.11", base64 = "0.22", tokio-rustls = "0.26", rustls-pemfile = "2.2", axum = "0.8"
    - server/Cargo.toml does NOT contain the string "ring" in any tokio-rustls or rustls dependency entry
    - server/src/broker.rs exists with: pub type ClientId, pub struct SignalingBroker (#[derive(Clone)]), pub fn new, pub fn register, pub fn unregister, pub fn route → bool
    - server/src/signaling.rs exists with: pub struct SignalingEnvelope, #[serde(rename = "type")] on the msg_type field, pub fn parse_envelope returning Option
    - RUSTFLAGS="-D warnings" cargo test -p immersive-rt-server exits 0: all existing tests (test_ws_echo, test_now_ms_nonzero, test_echo_message_round_trip) still pass AND new broker + signaling unit tests pass
  </acceptance_criteria>
  <done>broker.rs and signaling.rs compile, all broker behavior unit tests pass, signaling round-trip test passes, zero warnings</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: turn_creds.rs + lib.rs export (D-06, INFRA-04)</name>
  <files>server/src/turn_creds.rs, server/src/lib.rs</files>
  <read_first>
    server/src/lib.rs — current pub mod declarations; must add pub mod broker; pub mod signaling; pub mod turn_creds
    server/src/echo.rs lines 5-12 — utility function shape with /// doc comment, SystemTime + UNIX_EPOCH pattern (reuse as_secs() not as_millis())
    .planning/phases/02-signaling-turn-and-deployment/02-PATTERNS.md section "turn_creds.rs" — exact implementation and known-vector test discipline
    .planning/phases/02-signaling-turn-and-deployment/02-RESEARCH.md "Pattern 3" — verified HMAC-SHA1 algorithm; "Pitfall 1" — expiry not issue timestamp; exact imports (base64::engine::general_purpose::STANDARD, hmac::Hmac, sha1::Sha1)
  </read_first>
  <behavior>
    - generate_turn_credentials("mysecret", "user1", 300): username = "{now+300}:user1" where now is current unix seconds; password = base64_standard_encode(HMAC-SHA1("mysecret", username_as_utf8_bytes))
    - Known-vector test: with shared_secret="turn-secret", userid="testuser", and expiry timestamp fixed at 1_720_000_300u64 → username = "1720000300:testuser"; test must assert the exact expected_password string computed from HMAC-SHA1("turn-secret", "1720000300:testuser") using the same algorithm
    - Two sequential calls with the same inputs (but real clock) produce different usernames because the expiry timestamps differ — credentials are never cached
  </behavior>
  <action>
    Create server/src/turn_creds.rs: use base64::{engine::general_purpose::STANDARD, Engine}; use hmac::{Hmac, Mac}; use sha1::Sha1; use std::time::{SystemTime, UNIX_EPOCH}; type HmacSha1 = Hmac<Sha1>. Define pub struct TurnCredentials { pub username: String, pub password: String, pub ttl_seconds: u64 }. Implement pub fn generate_turn_credentials(shared_secret: &str, userid: &str, ttl_seconds: u64) → anyhow::Result<TurnCredentials>: compute expiry = SystemTime::now().duration_since(UNIX_EPOCH)?.as_secs() + ttl_seconds; username = format!("{expiry}:{userid}"); create HmacSha1::new_from_slice(shared_secret.as_bytes()), call mac.update(username.as_bytes()), password = STANDARD.encode(mac.finalize().into_bytes()). Return Ok(TurnCredentials { username, password, ttl_seconds }). This is the exact coturn use-auth-secret algorithm — do not deviate.

    Add #[cfg(test)] mod tests: test_turn_credential_known_vector uses a fixed expiry value (bypass the clock by constructing username manually: let username = format!("{expiry}:{userid}") with expiry=1_720_000_300u64, then runs only the HMAC portion of the algorithm against that fixed username to compute expected_password, then calls generate_turn_credentials and checks the username portion ends with ":testuser" and the password matches the known expected_password). Do NOT use range assertions — HMAC is deterministic and must be tested with a fixed vector. Also add test_turn_credentials_not_cached asserting two sequential calls return different username strings (different expiry seconds).

    Update server/src/lib.rs: append pub mod broker; pub mod signaling; pub mod turn_creds; after the existing three pub mod lines. This exposes all new modules via the library crate so integration tests in server/tests/ can import via immersive_rt_server::broker::SignalingBroker etc.
  </action>
  <verify>
    <automated>cd /home/ivancist/Documents/immersiveRT && RUSTFLAGS="-D warnings" cargo test --workspace 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - server/src/turn_creds.rs exists with: pub struct TurnCredentials, pub fn generate_turn_credentials
    - generate_turn_credentials uses expiry = now + ttl_seconds (verify: the username format string uses {expiry}:{userid} where expiry is computed as now + ttl, not just now)
    - server/src/lib.rs contains all six pub mod lines: echo, ws_server, wt_server, broker, signaling, turn_creds
    - RUSTFLAGS="-D warnings" cargo test --workspace exits 0 — all existing tests pass AND test_turn_credential_known_vector AND test_turn_credentials_not_cached pass
    - Zero compiler warnings across the entire workspace
  </acceptance_criteria>
  <done>turn_creds.rs compiles; known-vector test proves HMAC algorithm correctness; lib.rs exports all three new modules; full workspace test suite is green with zero warnings</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| crates.io → cargo | 7 new crate downloads; all audited in RESEARCH.md Package Legitimacy Audit before this plan was written |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-SC | Tampering | dashmap/hmac/sha1/base64/tokio-rustls/rustls-pemfile/axum installs via cargo | high | mitigate | Package Legitimacy Audit completed in 02-RESEARCH.md — all 7 packages classified VERIFIED with source repos confirmed; no ASSUMED/SUS packages; no blocking checkpoint required |
| T-02-01 | Tampering | turn_creds::generate_turn_credentials HMAC-SHA1 implementation | high | mitigate | test_turn_credential_known_vector with fixed inputs proves exact algorithm before any network code runs — coturn silently rejects HMAC mismatches with 401, making this test the only automated early warning |
| T-02-02 | Tampering | broker::route — DashMap Ref guard held across .await | medium | mitigate | UnboundedSender::send is synchronous (no .await); document explicitly in code that the guard is safe here, but must be cloned out before any blocking .await if the channel type changes to bounded |
</threat_model>

<verification>
After this plan: RUSTFLAGS="-D warnings" cargo test --workspace exits 0. Existing tests (test_ws_echo, test_now_ms_nonzero, test_echo_message_round_trip) still pass alongside the new broker unit tests, signaling round-trip test, test_turn_credential_known_vector, and test_turn_credentials_not_cached.
</verification>

<success_criteria>
- server/Cargo.toml has 7 new deps: dashmap 6.2, hmac 0.13, sha1 0.11, base64 0.22, tokio-rustls 0.26, rustls-pemfile 2.2, axum 0.8
- SignalingBroker::route returns bool; false for unknown IDs; caller (not broker) logs the warning per D-05
- generate_turn_credentials: username includes expiry = now + ttl (not now); HMAC-SHA1 known-vector test passes
- lib.rs exports broker, signaling, turn_creds alongside existing modules
- cargo test --workspace exits 0, zero warnings
</success_criteria>

## Artifacts This Plan Produces

| Symbol | Kind | File |
|--------|------|------|
| ClientId | pub type alias (= String) | server/src/broker.rs |
| SignalingBroker | pub struct | server/src/broker.rs |
| SignalingBroker::new | pub fn | server/src/broker.rs |
| SignalingBroker::register | pub fn → mpsc::UnboundedReceiver | server/src/broker.rs |
| SignalingBroker::unregister | pub fn | server/src/broker.rs |
| SignalingBroker::route | pub fn → bool | server/src/broker.rs |
| SignalingEnvelope | pub struct | server/src/signaling.rs |
| parse_envelope | pub fn → Option | server/src/signaling.rs |
| TurnCredentials | pub struct | server/src/turn_creds.rs |
| generate_turn_credentials | pub fn → anyhow::Result | server/src/turn_creds.rs |

<output>
Create `.planning/phases/02-signaling-turn-and-deployment/02-01-SUMMARY.md` when done
</output>
