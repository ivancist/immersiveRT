---
phase: 02-signaling-turn-and-deployment
plan: 05
type: execute
wave: 4
depends_on:
  - 02-03
  - 02-04
files_modified: []
autonomous: false
requirements:
  - INFRA-02
  - INFRA-03
  - INFRA-04
  - INFRA-06
  - INFRA-07

must_haves:
  truths:
    - "cargo test --workspace exits 0 with zero warnings — all Phase 2 unit and integration tests pass"
    - "docker compose up brings up exactly 3 healthy containers: server, coturn, static-files"
    - "turnutils_uclient -u test -w test <host>:3478 reports STUN binding + TURN allocation success (INFRA-06 success criterion 2)"
    - "A phone and desktop complete a WebRTC ICE handshake brokered through the Rust server — an unreliable data channel opens between them (INFRA-02 success criterion 1)"
    - "GET http://localhost:8081/turn-credentials returns JSON with non-empty username and password; a second call returns a different username (INFRA-04 success criterion 4)"
  artifacts: []
  key_links:
    - "turnutils_uclient: if not on host, use docker run --rm --network host coturn/coturn:4.6 turnutils_uclient -u test -w test 127.0.0.1 (Open Question 2 fallback from RESEARCH.md)"
    - "Browser ICE test: two tabs, both connect WebTransport to wss://localhost:4433 or ws://localhost:9090, register client IDs, exchange offer/answer via broker, verify RTCPeerConnection.connectionState === 'connected'"
---

## Phase Goal

**As a** developer, **I want to** verify WebRTC signaling brokered by the Rust server with ephemeral TURN credentials, **so that** phones and desktops can establish P2P data channels via the complete `docker compose up` stack.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-signaling-turn-and-deployment/02-CONTEXT.md
@.planning/phases/02-signaling-turn-and-deployment/02-RESEARCH.md
@.planning/phases/02-signaling-turn-and-deployment/02-04-SUMMARY.md
@.planning/phases/02-signaling-turn-and-deployment/02-03-SUMMARY.md
</context>

<objective>
Final validation gate for Phase 2. All automated tests must be green before the human checkpoint. The human checkpoint confirms the three manual-only success criteria that require real Docker, real coturn, and real browsers: STUN/TURN reachability, docker compose cold-start, and the end-to-end ICE handshake.

Purpose: Phase 2 cannot be marked complete without verifying the full stack works together. The automated tests cover the relay logic and HMAC algorithm. The manual steps cover the production-shape deployment and the actual WebRTC peer connection.

Output: Phase 2 verification sign-off. The stack is proven ready for Phase 3 (session and pairing).
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Full workspace test suite gate (all Phase 2 requirements)</name>
  <files></files>
  <read_first>
    .planning/phases/02-signaling-turn-and-deployment/02-VALIDATION.md — test map and per-task verification rows; confirm all rows are green
  </read_first>
  <action>
    Run the full workspace test suite. No code changes. If any test fails, the executor must fix the underlying issue in the relevant plan's files before marking this task done. Do not skip failing tests.

    Expected passing tests (7 minimum): test_now_ms_nonzero, test_echo_message_round_trip, test_ws_echo, broker unit tests (register/route/unregister/unknown-target), test_turn_credential_known_vector, test_turn_credentials_not_cached, test_broker_relay_ws, test_turn_creds_handler_unit.
  </action>
  <verify>
    <automated>cd /home/ivancist/Documents/immersiveRT && RUSTFLAGS="-D warnings" cargo test --workspace 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - RUSTFLAGS="-D warnings" cargo test --workspace exits 0
    - Output contains "test result: ok" for all test suites (lib tests, integration tests)
    - test_broker_relay_ws is listed as passing (integration test proving WS signaling relay)
    - test_turn_credential_known_vector is listed as passing (HMAC algorithm proven correct)
    - Zero compiler warnings
    - Zero test failures
  </acceptance_criteria>
  <done>Full workspace test suite exits 0 with zero warnings; all Phase 2 automated tests pass</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Phase 2 manual validation — docker compose up, STUN/TURN, WebRTC ICE handshake</name>
  <files></files>
  <action>Human validates the full Phase 2 stack: docker compose cold-start, coturn STUN/TURN reachability via turnutils_uclient, TURN credential endpoint, and end-to-end WebRTC ICE handshake between two browser tabs brokered by the Rust server. All 5 checks in how-to-verify must pass before Phase 2 is marked complete.</action>
  <verify>
    <human-check>All 5 manual checks pass: (1) docker compose ps shows 3 running containers; (2) turnutils_uclient exits 0 with TURN allocation success; (3) curl /turn-credentials returns valid JSON with changing username across calls; (4) two browser tabs complete WebRTC ICE handshake (RTCPeerConnection.connectionState === 'connected'); (5) TURN relay-only path succeeds with iceTransportPolicy: 'relay'</human-check>
  </verify>
  <done>All 5 manual checks pass and user types "approved"</done>
  <what-built>
    Plans 01-04 implemented:
    - SignalingBroker (DashMap in-process relay registry) — broker.rs
    - SignalingEnvelope (JSON envelope per D-04) — signaling.rs
    - generate_turn_credentials (HMAC-SHA1 ephemeral credentials per INFRA-04) — turn_creds.rs
    - Both wt_server.rs and ws_server.rs converted from echo to signaling relay with select! loops
    - WSS TLS enabled for the WebSocket fallback path
    - GET /turn-credentials endpoint via axum on port 8081
    - Docker Compose stack: Rust server + coturn + nginx static-files
    - coturn configured with use-auth-secret + lt-cred-mech + fingerprint, network_mode: host
  </what-built>
  <how-to-verify>
    Run these checks in order. Record which pass and which fail.

    **Check 1 — Docker Compose cold start (INFRA-07):**
    1. Copy .env.example to .env: cp .env.example .env
    2. Edit .env: set TURN_SHARED_SECRET to a random 32-char string (e.g. openssl rand -hex 16); set COTURN_EXTERNAL_IP to your LAN IP (ip route get 1 | awk '{print $7}' on Linux)
    3. docker compose up --build -d
    4. docker compose ps — expect: server (running), coturn (running), static-files (running)
    5. Expected: all 3 containers show "running" or "Up" status

    **Check 2 — STUN/TURN reachability (INFRA-06):**
    Option A (if turnutils installed on host): turnutils_uclient -u test -w test 127.0.0.1 -p 3478
    Option B (via Docker): docker run --rm --network host coturn/coturn:4.6 turnutils_uclient -u test -w test 127.0.0.1
    Expected: output shows "STUN binding request sent" and "TURN allocate ok" (or similar success lines). Exit code 0.

    **Check 3 — TURN credential endpoint (INFRA-04):**
    curl -s http://localhost:8081/turn-credentials | python3 -m json.tool
    Run it twice. Expected: both responses are valid JSON with "username" and "password" keys; the "username" values differ between the two calls (different expiry timestamps).

    **Check 4 — WebRTC ICE handshake end-to-end (INFRA-02 Success Criterion 1):**
    Open two browser tabs at http://localhost:8080 (the static-files server). Open DevTools Console in each tab. In each tab run a minimal WebSocket signaling script:
    - Tab A: connects to ws://localhost:9090, sends {"type":"register","from":"desktop-1","to":"","payload":null}, creates RTCPeerConnection, creates offer, sends {"type":"offer","from":"desktop-1","to":"phone-1","payload":{sdp:offer.sdp,type:offer.type}}
    - Tab B: connects to ws://localhost:9090, sends {"type":"register","from":"phone-1","to":"","payload":null}, receives offer from Tab A, creates answer, sends {"type":"answer",...}
    - Both tabs add ICE candidates to each other via {"type":"ice-candidate",...} messages
    Expected: RTCPeerConnection.connectionState === 'connected' in both tabs. An unreliable data channel can be opened and messages sent.

    Note: if coturn is needed for NAT traversal in your test environment, call GET /turn-credentials first and use the returned credentials in the RTCPeerConnection iceServers config.

    **Check 5 — TURN relay-only path (INFRA-02 Success Criterion 5, optional):**
    Add credential-based TURN servers to RTCPeerConnection iceServers, force relay-only transport policy (iceTransportPolicy: 'relay'), and complete the ICE handshake. Expected: connection still succeeds via TURN relay.
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 checks pass, or describe which checks failed and what errors were seen so the issues can be diagnosed and fixed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .env → docker containers | TURN_SHARED_SECRET and COTURN_EXTERNAL_IP flow from .env into all three containers |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-14 | Information Disclosure | .env file with real TURN_SHARED_SECRET accidentally committed | high | mitigate | Verify .env is in .gitignore before running any git add/commit; only .env.example (with placeholder values) is ever committed |
</threat_model>

<verification>
Phase 2 is complete when: cargo test --workspace exits 0 (Task 1), AND all 5 manual checks in Task 2 pass (human approval). Both conditions are required.
</verification>

<success_criteria>
1. cargo test --workspace exits 0, zero warnings — all Phase 2 automated tests pass
2. docker compose up brings up 3 healthy containers from cold start (INFRA-07)
3. turnutils_uclient reports STUN binding + TURN allocation success (INFRA-06)
4. GET /turn-credentials returns different credentials on sequential calls (INFRA-04)
5. Two browser tabs complete WebRTC ICE handshake via the Rust signaling broker — RTCPeerConnection.connectionState === 'connected' (INFRA-02)
</success_criteria>

## Artifacts This Plan Produces

This plan produces no new code artifacts. It validates all artifacts produced by Plans 01-04.

<output>
Create `.planning/phases/02-signaling-turn-and-deployment/02-05-SUMMARY.md` when done
</output>
