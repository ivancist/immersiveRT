---
phase: 06.3-ios-native-client-arkit-world-tracking
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - mobile/ios-app/immersiveRT/Sensor/ARPoseConversion.swift
  - mobile/ios-app/immersiveRTTests/ARPoseConversionTests.swift
autonomous: true
requirements: [SENS-V2-03]
must_haves:
  truths:
    - "A pure function converts an ARKit camera transform (simd_float4x4) into wire px/py/pz + qw/qx/qy/qz packet fields with no ARSession dependency"
    - "driftConfidence maps ARCamera.TrackingState exactly per D-07: .normal→1.0, .limited→0.5, .notAvailable→0.0"
    - "Freeze-on-loss retains last-known-good transform on .notAvailable and updates it through .limited (webxr.ts precedent)"
    - "All conversion/mapping is unit-testable in the iOS Simulator (no live ARSession required)"
  artifacts:
    - mobile/ios-app/immersiveRT/Sensor/ARPoseConversion.swift
    - mobile/ios-app/immersiveRTTests/ARPoseConversionTests.swift
  key_links:
    - "ARPoseConversion is the single isolated axis-conversion seam consumed by ARPoseSource (Plan 02) and flipped/reordered cheaply by the Plan 03 on-device checkpoint"
---

<objective>
Build the isolated, unit-testable ARKit-pose → wire-packet conversion layer (D-02, D-07) as pure functions with no live `ARSession` dependency. This is the single named seam RESEARCH.md mandates so the axis convention can be corrected on-device without touching unrelated code (this project has shipped an analytically-reasoned axis fix that was wrong on-device twice — `260710-w83` and the huge-position-drift debug cycle).

Purpose: De-risk the highest-uncertainty part of the phase (axis convention, D-02) behind a test harness and a starting-point mapping documented as UNVERIFIED, so the Plan 03 checkpoint can validate/flip it cheaply.
Output: `ARPoseConversion.swift` (pure functions + freeze-on-loss tracker) and `ARPoseConversionTests.swift`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.3-ios-native-client-arkit-world-tracking/06.3-RESEARCH.md
@.planning/phases/06.3-ios-native-client-arkit-world-tracking/06.3-PATTERNS.md
@client/src/sensor/webxr.ts
@mobile/ios-app/immersiveRT/Sensor/SensorPacketEncoder.swift
@mobile/ios-app/immersiveRTTests/SensorPacketEncoderTests.swift
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Failing tests for ARKit pose conversion + driftConfidence + freeze-on-loss</name>
  <files>mobile/ios-app/immersiveRTTests/ARPoseConversionTests.swift</files>
  <read_first>
    - mobile/ios-app/immersiveRTTests/SensorPacketEncoderTests.swift (fixture-driven XCTest style to mirror; `@testable import immersiveRT`)
    - client/src/sensor/webxr.ts (readPoseAndConfidence + WebXrPoseTracker freeze-on-loss — the porting source for D-07)
    - .planning/phases/06.3-ios-native-client-arkit-world-tracking/06.3-RESEARCH.md (Validation Architecture section, lines 448-483 — Simulator constraint + pure-function requirement)
  </read_first>
  <behavior>
    - Test: position extraction pulls the translation column (columns.3.x/y/z) of a known simd_float4x4 into px/py/pz through the axis-conversion function.
    - Test: quaternion extraction of a known rotation matrix yields expected qw/qx/qy/qz (normalized) through the axis-conversion function.
    - Test: driftConfidence(for:) returns 1.0 for .normal, 0.5 for .limited(.excessiveMotion) AND .limited(.initializing), 0.0 for .notAvailable (D-07/D-08 flat-0.5).
    - Test: ARPoseTracker.ingest updates last-known-good on .normal and on .limited, but does NOT mutate last-known-good on .notAvailable (freeze), while driftConfidence still drops to 0.0 on .notAvailable.
    - Test: trackingLimitedReasonMessage returns a distinct non-empty string per reason (.initializing/.excessiveMotion/.insufficientFeatures/.relocalizing).
  </behavior>
  <action>Create `ARPoseConversionTests.swift` in the `immersiveRTTests` target with `import XCTest`, `import ARKit`, `@testable import immersiveRT`. Construct `simd_float4x4` fixtures directly (translation-only matrix with columns.3 = SIMD4(1.5, -2.0, 3.25, 1); a known 90-degree rotation matrix) and assert against the pure conversion functions named in Task 2 (`arKitPacketPosition(from:)`, `arKitPacketQuaternion(from:)`, `driftConfidence(for:)`, `trackingLimitedReasonMessage(_:)`, and `ARPoseTracker`). Construct `ARCamera.TrackingState` cases directly (`.normal`, `.limited(.excessiveMotion)`, `.notAvailable`) — these enum values compile and run in the Simulator even though a live `ARSession` does not. Assert freeze-on-loss per D-07 using the webxr.ts precedent (freeze only on driftConfidence 0 / `.notAvailable`; keep updating through `.limited`). Tests MUST fail to compile/run initially (Task 2 file absent) — this is the RED step.</action>
  <verify>
    <automated>xcodebuild test -project mobile/ios-app/immersiveRT.xcodeproj -scheme immersiveRT -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:immersiveRTTests/ARPoseConversionTests 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `ARPoseConversionTests.swift` exists in `mobile/ios-app/immersiveRTTests/` with the five behaviors above expressed as XCTest assertions.
    - Before Task 2, the test build fails because the symbols under test do not yet exist (RED confirmed).
    - Commit: `test(06.3-01): add failing ARKit pose conversion tests`
  </acceptance_criteria>
  <done>RED: test file compiles-references the intended symbols and the target fails to build/run because they are unimplemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement ARPoseConversion pure functions + freeze-on-loss tracker (UNVERIFIED axis mapping)</name>
  <files>mobile/ios-app/immersiveRT/Sensor/ARPoseConversion.swift</files>
  <read_first>
    - mobile/ios-app/immersiveRT/Sensor/SensorPacketEncoder.swift (SensorPacket field names/order + safeFloat convention)
    - mobile/ios-app/immersiveRT/Sensor/CoreMotionSource.swift (the "⚠️ UNVERIFIED (Pitfall 1)" doc-comment convention on lines 62-82 to copy in spirit)
    - .planning/phases/06.3-ios-native-client-arkit-world-tracking/06.3-RESEARCH.md (Code Examples lines 341-395; Anti-Patterns lines 281-286; Don't Hand-Roll lines 288-297)
    - client/src/sensor/webxr.ts (freeze-on-loss last-known-good idiom)
  </read_first>
  <action>Create `ARPoseConversion.swift` with `import ARKit` and `import simd`. Define an `ARPose` value struct in packet-field order (`px, py, pz, qw, qx, qy, qz, driftConfidence: Double`). Implement pure functions: `arKitPacketPosition(from transform: simd_float4x4) -> (px: Double, py: Double, pz: Double)` reading the translation column (`transform.columns.3`); `arKitPacketQuaternion(from transform: simd_float4x4) -> (qw: Double, qx: Double, qy: Double, qz: Double)` using the vetted one-call `simd_quaternion(transform)` (per Don't Hand-Roll — never hand-roll 3x3→quaternion); `driftConfidence(for trackingState: ARCamera.TrackingState) -> Double` implementing D-07 (`.normal`→1.0, `.limited`→0.5 flat per D-08, `.notAvailable`→0.0, `@unknown default`→0.0); and `trackingLimitedReasonMessage(_ reason: ARCamera.TrackingState.Reason) -> String` returning a distinct human string per D-08 sub-reason. Ship a DIRECT starting-point axis mapping (px=t.x, py=t.y, pz=t.z; qw/qx/qy/qz straight from the simd quaternion's real/imag) — do NOT hard-code sign/reorder constants (RESEARCH.md Pitfall 3 forbids analytically guessing final constants). Mark the mapping with a doc comment stating it is UNVERIFIED pending Plan 03's on-device axis checkpoint, mirroring CoreMotionSource.swift's existing warning style. Add an `ARPoseTracker` final class holding `lastGoodTransform: simd_float4x4` and `driftConfidence: Double`, with `ingest(transform:trackingState:)` that updates last-known-good only when `trackingState != .notAvailable` (freeze only on driftConfidence 0, per D-07 resolved against webxr.ts precedent — document this as the deliberate choice), and a `currentPose() -> ARPose` accessor. Make GREEN.</action>
  <verify>
    <automated>xcodebuild test -project mobile/ios-app/immersiveRT.xcodeproj -scheme immersiveRT -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:immersiveRTTests/ARPoseConversionTests 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `ARPoseConversionTests` passes fully (GREEN).
    - `grep -c "UNVERIFIED" mobile/ios-app/immersiveRT/Sensor/ARPoseConversion.swift` returns >= 1 (axis mapping flagged for the Plan 03 checkpoint).
    - The freeze condition keys on `.notAvailable` only (source assertion: `ingest` guards last-known-good update with a `!= .notAvailable` / equivalent check).
    - Commit: `feat(06.3-01): implement ARKit pose conversion + freeze-on-loss tracker`
  </acceptance_criteria>
  <done>GREEN: all conversion + freeze-on-loss + driftConfidence tests pass; axis mapping present but explicitly marked UNVERIFIED.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ARKit VIO → app | ARKit-derived floats (position/rotation) enter app code and may be non-finite in a degenerate VIO state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.3-01 | Tampering | ARPoseConversion float outputs | medium | mitigate | Conversion returns plain Doubles; `safeFloat()` sanitization is applied downstream at every `SensorPacketEncoder` write (Plan 02). Tracker never emits NaN as last-known-good because it only overwrites on a valid tracking state. |

No package installs this phase — ARKit/simd are first-party Apple frameworks (RESEARCH.md Package Legitimacy Audit: not applicable).
</threat_model>

<verification>
- Full suite stays green: `xcodebuild test -project mobile/ios-app/immersiveRT.xcodeproj -scheme immersiveRT -destination 'platform=iOS Simulator,name=iPhone 16'`
- New pure functions require no live ARSession (Simulator-only, per RESEARCH.md constraint).
</verification>

<success_criteria>
ARKit pose conversion + driftConfidence mapping + freeze-on-loss are implemented as pure, Simulator-tested functions; the axis mapping is isolated and marked UNVERIFIED for the Plan 03 checkpoint.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (this plan)
- `mobile/ios-app/immersiveRT/Sensor/ARPoseConversion.swift` — new file. Symbols: `ARPose` struct; `arKitPacketPosition(from:)`; `arKitPacketQuaternion(from:)`; `driftConfidence(for:)`; `trackingLimitedReasonMessage(_:)`; `ARPoseTracker` class (`ingest(transform:trackingState:)`, `currentPose()`, `lastGoodTransform`, `driftConfidence`).
- `mobile/ios-app/immersiveRTTests/ARPoseConversionTests.swift` — new test class `ARPoseConversionTests`.
- No pbxproj edits required — files land under the existing `PBXFileSystemSynchronizedRootGroup` roots (`immersiveRT/`, `immersiveRTTests/`) and auto-join their targets.
</artifacts_produced>

<output>
Create `.planning/phases/06.3-ios-native-client-arkit-world-tracking/06.3-01-SUMMARY.md` when done.
</output>
