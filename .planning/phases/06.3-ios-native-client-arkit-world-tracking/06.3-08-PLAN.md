---
phase: 06.3-ios-native-client-arkit-world-tracking
plan: 08
type: execute
wave: 7
depends_on: ["06.3-05", "06.3-06", "06.3-07"]
files_modified:
  - mobile/ios-app/immersiveRT/ActiveSessionView.swift
  - mobile/ios-app/immersiveRT/Transport/TransportManager.swift
  - mobile/ios-app/immersiveRT/ContentView.swift
autonomous: false
requirements: [SENS-V2-03]
must_haves:
  truths:
    - "The 2-finger corner long-press reveals a small overlay menu containing Recenter and Disconnect/Back buttons (D-12)"
    - "Recenter re-zeros this player's ARKit origin via TransportManager.recenter (D-11)"
    - "Disconnect/Back disconnects when connected, or navigates back to the initial screen when already disconnected/errored (D-13)"
    - "Both controls are hard to trigger accidentally because the reveal requires the hidden gesture (D-12)"
  artifacts:
    - mobile/ios-app/immersiveRT/ActiveSessionView.swift
  key_links:
    - "CornerLongPressOverlay.onReveal → show overlay menu → Recenter (TransportManager.recenter) / Disconnect (TransportManager.disconnect or ContentView return-to-home)"
---

<objective>
Wire the hidden overlay menu (D-12) into `ActiveSessionView`, revealed by the Plan 07 corner long-press recognizer, exposing the Recenter control (D-11) and a Disconnect/Back control (D-13). Both are intentionally hard to reach because the full screen is the touch-capture surface (D-12).

Purpose: Give the user a deliberate, accident-resistant way to re-zero tracking and to leave/disconnect a session.
Output: overlay menu UI + `TransportManager.disconnect()` + D-13 navigation.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.3-ios-native-client-arkit-world-tracking/06.3-RESEARCH.md
@mobile/ios-app/immersiveRT/ActiveSessionView.swift
@mobile/ios-app/immersiveRT/ContentView.swift
@mobile/ios-app/immersiveRT/Transport/TransportManager.swift
@mobile/ios-app/immersiveRT/Gestures/CornerLongPressRecognizer.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: TransportManager.disconnect() (D-13 connected case)</name>
  <files>mobile/ios-app/immersiveRT/Transport/TransportManager.swift</files>
  <read_first>
    - mobile/ios-app/immersiveRT/Transport/TransportManager.swift (state machine ConnectionState lines 32-41; activate/handleTransportClosed lines 296-343; finishReconnectFailure lines 500-505; pauseForBackground lines 567-570)
    - .planning/phases/06.3-ios-native-client-arkit-world-tracking/06.3-CONTEXT.md (D-13)
  </read_first>
  <action>Add a `disconnect()` main-actor method to `TransportManager` implementing the D-13 connected case: stop the sensor loop (`arPoseSource.stop()`) and heartbeat, close the active transport, clear `reconnectToken` so the close does NOT trigger the reconnect loop (mirror `finishReconnectFailure`'s token clearing to prevent auto-reconnect after an intentional disconnect), and transition `state` to `.ended`. Ensure this is idempotent and safe to call from `.reconnecting`/`.error` states. Add a read-only helper (e.g. `var isConnected: Bool`) the view can consult to choose between disconnect vs. navigate-home (D-13).</action>
  <verify>
    <automated>xcodebuild test -project mobile/ios-app/immersiveRT.xcodeproj -scheme immersiveRT -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:immersiveRTTests/TransportManagerTests 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `TransportManagerTests` stays green; `disconnect()` exists and transitions to `.ended` without triggering reconnect (source assertion: clears `reconnectToken` before/with closing).
    - `isConnected` (or equivalent) helper exists for the D-13 branch.
    - Commit: `feat(06.3-08): TransportManager.disconnect intentional teardown (D-13)`
  </acceptance_criteria>
  <done>An intentional disconnect tears down cleanly and does not auto-reconnect.</done>
</task>

<task type="auto">
  <name>Task 2: Hidden overlay menu in ActiveSessionView + Recenter/Disconnect/Back wiring (D-11/D-12/D-13)</name>
  <files>mobile/ios-app/immersiveRT/ActiveSessionView.swift, mobile/ios-app/immersiveRT/ContentView.swift</files>
  <read_first>
    - mobile/ios-app/immersiveRT/ActiveSessionView.swift (body + SessionViewModel; the touch DragGesture added in Plan 04)
    - mobile/ios-app/immersiveRT/ContentView.swift (hasStartedSession @State drives HomeView vs ActiveSessionView, lines 15-55)
    - mobile/ios-app/immersiveRT/Gestures/CornerLongPressRecognizer.swift (CornerLongPressOverlay wrapper + onReveal from Plan 07)
    - mobile/ios-app/immersiveRT/Transport/TransportManager.swift (recenter() from Plan 05; disconnect()/isConnected from Task 1)
  </read_first>
  <action>Layer `CornerLongPressOverlay` (Plan 07) over `ActiveSessionView`'s content so its `onReveal` toggles a `@State private var isMenuRevealed`. When revealed, present a small overlay menu (e.g. a compact card with two buttons) containing: (1) Recenter → call `viewModel`/`TransportManager.recenter()` (D-11), then dismiss the menu and optionally show the `recentered` toast; (2) Disconnect/Back → implement D-13: if `TransportManager.isConnected` is true, call `disconnect()`; otherwise (already disconnected/error/ended) invoke an `onExit` closure that returns to the initial screen. Add an `onExit: () -> Void` parameter to `ActiveSessionView`, supplied by `ContentView` to set `hasStartedSession = false` (return to `HomeView`) — expose `SessionViewModel` accessors as needed. Ensure the overlay menu and its reveal do not interfere with the single-finger touch capture (the corner overlay uses `cancelsTouchesInView = false` from Plan 07); the menu is dismissable (tap-outside or an explicit close). Keep the reveal gesture the ONLY way to surface these controls (D-12 — no always-visible buttons).</action>
  <verify>
    <automated>xcodebuild test -project mobile/ios-app/immersiveRT.xcodeproj -scheme immersiveRT -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - App builds; full suite green.
    - Source assertion: the menu is gated on `isMenuRevealed` toggled by `CornerLongPressOverlay.onReveal` (no always-visible Recenter/Disconnect buttons — D-12).
    - Source assertion: Disconnect/Back branches on `isConnected` (disconnect vs. `onExit` → `hasStartedSession = false`, D-13).
    - Source assertion: Recenter button calls the recenter path (D-11).
    - Commit: `feat(06.3-08): hidden overlay menu with recenter + disconnect/back (D-11/D-12/D-13)`
  </acceptance_criteria>
  <done>The hidden gesture reveals a menu whose Recenter re-zeros tracking and whose Disconnect/Back follows D-13.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: On-device verification of the full hidden gesture + menu behavior (D-12/D-13)</name>
  <action>On a physical iPhone, verify accident-resistance, the 2-finger reveal, Recenter, Disconnect, and Back-to-home per the procedure below (D-12/D-13). Manual-only — two sustained independent touches are not reliably reproducible in the Simulator.</action>
  <what-built>
    The 2-finger corner long-press reveal (Plan 07 recognizer) wired to the overlay menu with Recenter (D-11) and Disconnect/Back (D-13).
  </what-built>
  <how-to-verify>
    1. On a physical iPhone in an active session, use normal single-finger tap/drag across the screen for a couple of minutes — confirm the hidden menu NEVER appears accidentally (D-12 accident-resistance).
    2. Perform the intended gesture: hold one finger in the top-left corner AND one in the top-right corner for the hold duration — confirm the overlay menu appears.
    3. Tap Recenter — confirm this player's rendered position re-zeros on the desktop without a tracking blip (setWorldOrigin, D-11), and normal touch capture still works after dismissing the menu.
    4. While connected, tap Disconnect — confirm the session disconnects (D-13 connected case) and does not auto-reconnect.
    5. Reveal the menu again from the disconnected/ended state and tap Back — confirm it navigates to the initial Home screen (D-13 disconnected case).
  </how-to-verify>
  <resume-signal>Type "menu-verified" once accident-resistance, reveal, Recenter, Disconnect, and Back-to-home all behave per D-12/D-13, or describe the issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user gesture → app (local control) | Menu actions affect local session lifecycle and ARKit origin only; no new wire data |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.3-06 | Tampering (UX safety) | accidental Recenter/Disconnect | low | mitigate | Controls are reachable only via the deliberate 2-finger both-corners hold (D-12); verified accident-resistant on-device (Task 3). |
| T-06.3-07 | Denial of Service (self) | disconnect vs auto-reconnect race | low | mitigate | `disconnect()` clears `reconnectToken` so an intentional teardown cannot be undone by the reconnect loop. |

No package installs this phase — SwiftUI/UIKit are first-party Apple frameworks.
</threat_model>

<verification>
- Full suite green.
- Manual on-device (Task 3): full multi-touch gesture, Recenter, Disconnect, and Back-to-home — not Simulator-reproducible (two sustained independent touches; RESEARCH.md).
</verification>

<success_criteria>
The hidden 2-finger corner long-press reliably reveals a menu (accident-resistant) whose Recenter re-zeros this player's ARKit origin (D-11) and whose Disconnect/Back follows D-13 (disconnect when connected, else return to the initial screen).
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (this plan)
- `mobile/ios-app/immersiveRT/Transport/TransportManager.swift` — modified. New `disconnect()` + `isConnected` helper (D-13).
- `mobile/ios-app/immersiveRT/ActiveSessionView.swift` — modified. `CornerLongPressOverlay` layered in; `isMenuRevealed` @State; overlay menu with Recenter + Disconnect/Back; new `onExit` parameter.
- `mobile/ios-app/immersiveRT/ContentView.swift` — modified. Supplies `onExit` to reset `hasStartedSession` (D-13 return-to-home).
</artifacts_produced>

<output>
Create `.planning/phases/06.3-ios-native-client-arkit-world-tracking/06.3-08-SUMMARY.md` when done.
</output>
