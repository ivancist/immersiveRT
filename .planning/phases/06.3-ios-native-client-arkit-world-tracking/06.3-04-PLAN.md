---
phase: 06.3-ios-native-client-arkit-world-tracking
plan: 04
type: execute
wave: 4
depends_on: ["06.3-03"]
files_modified:
  - mobile/ios-app/immersiveRT/Sensor/TouchCaptureNormalization.swift
  - mobile/ios-app/immersiveRTTests/TouchCaptureNormalizationTests.swift
  - mobile/ios-app/immersiveRT/ActiveSessionView.swift
  - mobile/ios-app/immersiveRT/Transport/TransportManager.swift
autonomous: true
requirements: [SENS-06]
must_haves:
  truths:
    - "A single finger down anywhere on ActiveSessionView continuously sets touchActive=true and updates touchX/touchY while held; lifting sets touchActive=false (D-03)"
    - "Touch is captured across the full ActiveSessionView, not a sub-region (D-04)"
    - "touchX/touchY are normalized [0,1] against the app's explicit landscape-locked bounds, not raw view bounds (D-05)"
    - "The phone shows local visual feedback while a touch is active (D-06)"
  artifacts:
    - mobile/ios-app/immersiveRT/Sensor/TouchCaptureNormalization.swift
    - mobile/ios-app/immersiveRTTests/TouchCaptureNormalizationTests.swift
  key_links:
    - "DragGesture(minimumDistance:0) full-screen → normalize → TransportManager.touchState → handlePose reads it into SensorPacket touch fields"
---

<objective>
Add native touch input capture (SENS-06 native parity) so the already-existing wire fields `touchActive`/`touchX`/`touchY` — currently false/zero-stubbed on iOS — carry real tap+drag data, matching the web client's touch behavior. This is client parity, not a new capability (CONTEXT.md).

Purpose: Bring the native iOS client to touch-input parity with the Phase 5 web client.
Output: a pure normalization function + tests, full-screen gesture capture in `ActiveSessionView`, transport wiring, and local visual feedback.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.3-ios-native-client-arkit-world-tracking/06.3-PATTERNS.md
@client/src/phone.ts
@mobile/ios-app/immersiveRT/ActiveSessionView.swift
@mobile/ios-app/immersiveRT/Transport/TransportManager.swift
@mobile/ios-app/immersiveRT/Sensor/SensorPacketEncoder.swift
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure touch-coordinate normalization against landscape-locked bounds (D-05) + tests</name>
  <files>mobile/ios-app/immersiveRT/Sensor/TouchCaptureNormalization.swift, mobile/ios-app/immersiveRTTests/TouchCaptureNormalizationTests.swift</files>
  <read_first>
    - mobile/ios-app/immersiveRT/Sensor/SensorPacketEncoder.swift (touch fields: touchActive/touchX/touchY, encoder clamps to [0,1] then scales — offsets 31-35)
    - client/src/phone.ts (existing web touch-capture reference: normalization + POSITION_MAX/GESTURE_MAX bounds)
    - mobile/ios-app/immersiveRTTests/SensorPacketEncoderTests.swift (XCTest style)
  </read_first>
  <behavior>
    - normalizedTouch(location:in:) maps a CGPoint at the top-left of the landscape-locked bounds to (0,0), bottom-right to (1,1), center to (0.5,0.5).
    - A location outside the bounds is clamped to [0,1] on each axis.
    - Normalization uses the explicit landscape-locked bounds passed in, not raw device/portrait bounds (D-05).
  </behavior>
  <action>Create `TouchCaptureNormalization.swift` with a pure function `normalizedTouch(location: CGPoint, in bounds: CGRect) -> (x: Double, y: Double)` returning [0,1]-clamped coordinates computed as `(location.x - bounds.minX) / bounds.width` and the equivalent for y, clamped to [0,1]. Document that `bounds` MUST be the app's explicit landscape-locked interface bounds (D-05), which the caller (Task 2) supplies from the active landscape geometry — not the raw UIKit view bounds. Add `TouchCaptureNormalizationTests` covering corners, center, and out-of-bounds clamping. Keep this a pure function so it is Simulator-testable (no gesture runtime needed).</action>
  <verify>
    <automated>xcodebuild test -project mobile/ios-app/immersiveRT.xcodeproj -scheme immersiveRT -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:immersiveRTTests/TouchCaptureNormalizationTests 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `TouchCaptureNormalizationTests` passes (corners map to (0,0)/(1,1), center to (0.5,0.5), out-of-bounds clamps).
    - Source assertion: the function takes an explicit `bounds: CGRect` parameter (D-05 — not reading a global/raw view bounds).
    - Commit: `feat(06.3-04): pure touch normalization against landscape bounds (D-05)`
  </acceptance_criteria>
  <done>Normalization is a pure, tested function keyed on explicit landscape-locked bounds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Full-screen tap+drag capture in ActiveSessionView + transport wiring + local feedback (D-03/D-04/D-06)</name>
  <files>mobile/ios-app/immersiveRT/ActiveSessionView.swift, mobile/ios-app/immersiveRT/Transport/TransportManager.swift</files>
  <read_first>
    - mobile/ios-app/immersiveRT/ActiveSessionView.swift (body VStack lines 151-195; SessionViewModel wiring lines 17-51)
    - mobile/ios-app/immersiveRT/Transport/TransportManager.swift (the touchState placeholder + handlePose added in Plan 02)
    - mobile/ios-app/immersiveRT/Sensor/TouchCaptureNormalization.swift (Task 1)
    - .planning/phases/06.3-ios-native-client-arkit-world-tracking/06.3-PATTERNS.md (ActiveSessionView assignment lines 154-162)
  </read_first>
  <action>Add a full-screen `DragGesture(minimumDistance: 0)` to `ActiveSessionView`'s root content (D-04: entire screen is the capture surface) inside a `GeometryReader` so the current landscape-locked bounds are available. On `.onChanged`, set touchActive=true and compute normalized (x,y) via `normalizedTouch(location:in:)` (Task 1); on `.onEnded`, set touchActive=false. Push this state to `TransportManager` via a new main-actor method `updateTouchState(active:x:y:)` that writes the `touchState` placeholder added in Plan 02 (D-03: continuously updated while the finger is down). Do NOT push per-touch on the 60Hz path — `handlePose` already reads the stored `touchState` each tick, so touch is a state write, not a send trigger. Add local visual feedback (D-06): a lightweight SwiftUI indicator (e.g. a colored border/overlay or a small dot at the touch point) shown while touchActive is true, driven by `@State` in the view. Ensure the drag gesture does not block the hidden 2-finger corner gesture that Plans 07-08 add (leave the single-finger drag as the only gesture here; the corner recognizer is added as a separate observing overlay later).</action>
  <verify>
    <automated>xcodebuild test -project mobile/ios-app/immersiveRT.xcodeproj -scheme immersiveRT -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:immersiveRTTests/TransportManagerTests 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - App builds; `TransportManager.updateTouchState(active:x:y:)` exists and writes the touch state consumed by `handlePose`.
    - Source assertion: `ActiveSessionView` uses `DragGesture(minimumDistance: 0)` and calls `normalizedTouch(location:in:)`.
    - Source assertion: a local-feedback view element is gated on the touch-active `@State` (D-06).
    - Full suite green: `xcodebuild test -project mobile/ios-app/immersiveRT.xcodeproj -scheme immersiveRT -destination 'platform=iOS Simulator,name=iPhone 16'`
    - Commit: `feat(06.3-04): full-screen touch capture + local feedback (SENS-06)`
  </acceptance_criteria>
  <done>Single-finger tap+drag anywhere on ActiveSessionView populates real touch fields on the wire and shows local feedback.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user touch → app → wire | Touch coordinates enter as untrusted floats and cross to the desktop in the packet |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.3-04 | Tampering | touchX/touchY float writes | low | mitigate | `normalizedTouch` clamps to [0,1]; `SensorPacketEncoder` additionally applies `safeFloat()` + clamp01 before the uint16 write (existing V5 control). |

No package installs this phase — SwiftUI/UIKit are first-party Apple frameworks.
</threat_model>

<verification>
- Full suite green including `TouchCaptureNormalizationTests` and `TransportManagerTests`.
- Manual on-device touch verification (real feel of tap+drag + local feedback) is confirmed during the phase's on-device pass; the automated coverage is the pure normalization math (Simulator constraint).
</verification>

<success_criteria>
Native tap+drag across the full ActiveSessionView populates touchActive/touchX/touchY on the wire (normalized to landscape-locked bounds) with local visual feedback — touch parity with the web client (SENS-06).
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (this plan)
- `mobile/ios-app/immersiveRT/Sensor/TouchCaptureNormalization.swift` — new file. `normalizedTouch(location:in:) -> (x: Double, y: Double)`.
- `mobile/ios-app/immersiveRTTests/TouchCaptureNormalizationTests.swift` — new test class.
- `mobile/ios-app/immersiveRT/ActiveSessionView.swift` — modified. Full-screen `DragGesture(minimumDistance:0)`, touch-active `@State`, local feedback overlay.
- `mobile/ios-app/immersiveRT/Transport/TransportManager.swift` — modified. New `updateTouchState(active:x:y:)`.
</artifacts_produced>

<output>
Create `.planning/phases/06.3-ios-native-client-arkit-world-tracking/06.3-04-SUMMARY.md` when done.
</output>
