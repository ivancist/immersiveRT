---
phase: 06.3-ios-native-client-arkit-world-tracking
plan: 06
type: execute
wave: 6
depends_on: ["06.3-05"]
files_modified:
  - mobile/ios-app/immersiveRT/DynamicToast/Toast.swift
  - mobile/ios-app/immersiveRT.xcodeproj/project.pbxproj
  - mobile/ios-app/immersiveRT/Sensor/ARPoseSource.swift
  - mobile/ios-app/immersiveRT/ActiveSessionView.swift
autonomous: false
requirements: [SENS-V2-03]
must_haves:
  truths:
    - "If ARKit is unsupported or camera permission is denied at session start, the session is blocked with a DynamicToast error and never silently falls back to CoreMotion-only (D-09)"
    - "ARKit .limited sub-reasons (initializing/excessiveMotion/insufficientFeatures/relocalizing) are surfaced individually via DynamicToast locally (D-08), while the wire driftConfidence stays a flat 0.5"
    - "All user-facing messages use the existing DynamicToast component (D-15)"
    - "NSCameraUsageDescription copy is accurate and minimal for both QR scanning and ARKit motion tracking"
  artifacts:
    - mobile/ios-app/immersiveRT/DynamicToast/Toast.swift
  key_links:
    - "checkARStartupPreconditions → block session start + Toast (D-09); ARCamera.TrackingState.limited(reason) → trackingLimitedReasonMessage → Toast (D-08)"
---

<objective>
Add the ARKit availability/permission start-time gate (D-09) and local tracking-quality feedback (D-08), all surfaced through the existing DynamicToast component (D-15). D-09 is a hard block, not a soft degrade — the whole point of the phase is ARKit tracking (consistent with D-18). Also correct the camera usage-description copy so it honestly covers ARKit motion tracking, not just QR scanning.

Purpose: Fail loudly and honestly when ARKit cannot run, and give the user actionable local feedback while tracking is degraded.
Output: new Toast cases, an accurate Info.plist string, a session-start precondition gate, and tracking-limited toast wiring.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.3-ios-native-client-arkit-world-tracking/06.3-RESEARCH.md
@mobile/ios-app/immersiveRT/DynamicToast/Toast.swift
@mobile/ios-app/immersiveRT/DynamicToast/ToastView.swift
@mobile/ios-app/immersiveRT/ContentView.swift
@mobile/ios-app/immersiveRT/ActiveSessionView.swift
@mobile/ios-app/immersiveRT/Sensor/ARPoseSource.swift
@mobile/ios-app/immersiveRT/Sensor/ARPoseConversion.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: New DynamicToast cases (D-08/D-09/D-15) + accurate camera usage copy</name>
  <files>mobile/ios-app/immersiveRT/DynamicToast/Toast.swift, mobile/ios-app/immersiveRT.xcodeproj/project.pbxproj</files>
  <read_first>
    - mobile/ios-app/immersiveRT/DynamicToast/Toast.swift (the `.invalidQRCode` static factory shape to copy, lines 20-26)
    - .planning/phases/06.3-ios-native-client-arkit-world-tracking/06.3-RESEARCH.md (Code Examples lines 358-367 tracking-limited reason strings; Security Domain lines 500-506 NSCameraUsageDescription accuracy)
    - mobile/ios-app/immersiveRT.xcodeproj/project.pbxproj (INFOPLIST_KEY_NSCameraUsageDescription on both build configs — currently QR-only copy)
  </read_first>
  <action>Add new `static var` cases to `Toast` following the exact `.invalidQRCode` shape: (a) `arUnavailable` — device does not support ARKit world tracking (D-09, red/error style); (b) `cameraPermissionDenied` — camera access denied, needed for motion tracking (D-09, red/error style); (c) tracking-limited feedback cases for D-08 — either one parameterized factory `trackingLimited(_ message: String)` or discrete cases per sub-reason (initializing/excessiveMotion/insufficientFeatures/relocalizing) using the strings from `trackingLimitedReasonMessage` (Plan 01) with an informational (non-error) symbol/style; (d) optionally `recentered` confirmation (Claude's Discretion per CONTEXT.md). Update `INFOPLIST_KEY_NSCameraUsageDescription` in BOTH build configurations of `project.pbxproj` to an accurate, minimal string covering QR scanning AND ARKit motion tracking (camera used only for motion — never displayed, stored, or transmitted, per D-14) — e.g. "Camera access is used to scan the pairing QR code and to track this device's motion. Camera images are never shown, saved, or sent."</action>
  <verify>
    <automated>xcodebuild build -project mobile/ios-app/immersiveRT.xcodeproj -scheme immersiveRT -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - App builds; `Toast.arUnavailable` and `Toast.cameraPermissionDenied` exist, plus tracking-limited toast factory/cases for the four D-08 reasons.
    - `grep -c "track this device's motion\|motion tracking" mobile/ios-app/immersiveRT.xcodeproj/project.pbxproj` returns >= 2 (both build configs updated).
    - Commit: `feat(06.3-06): ARKit toast cases + accurate camera usage copy (D-08/D-09/D-15)`
  </acceptance_criteria>
  <done>New DynamicToast cases exist for ARKit errors and tracking-limited reasons; camera usage copy honestly covers ARKit.</done>
</task>

<task type="auto">
  <name>Task 2: D-09 session-start precondition gate (block, never degrade) + D-08 tracking-limited toast wiring</name>
  <files>mobile/ios-app/immersiveRT/Sensor/ARPoseSource.swift, mobile/ios-app/immersiveRT/ActiveSessionView.swift</files>
  <read_first>
    - mobile/ios-app/immersiveRT/Sensor/ARPoseSource.swift (start() capability guard; delegate callback where trackingState is available)
    - mobile/ios-app/immersiveRT/ActiveSessionView.swift (SessionViewModel.start lines 45-50; dynamicIslandToast usage pattern in ContentView line 36)
    - .planning/phases/06.3-ios-native-client-arkit-world-tracking/06.3-RESEARCH.md (Pattern 4 lines 252-279 precondition check; D-08 lines 358-367; Pitfall on freeze/limited)
    - mobile/ios-app/immersiveRT/DynamicToast/ToastView.swift (dynamicIslandToast(isPresented:duration:value:) modifier signature)
  </read_first>
  <action>Add a startup precondition check (D-09) exposed from `ARPoseSource` (or a small helper), returning an error kind for: `ARWorldTrackingConfiguration.isSupported == false` → device-unsupported; `AVCaptureDevice.authorizationStatus(for: .video)` denied/restricted → camera-denied (request access when `.notDetermined` via `AVCaptureDevice.requestAccess(for:.video)`) — mirroring RESEARCH.md Pattern 4. Wire this into the session-start flow (`SessionViewModel.start` / the point where ARKit would begin): if preconditions fail, do NOT start the ARSession or fall back to CoreMotion — instead present `Toast.arUnavailable` or `Toast.cameraPermissionDenied` via `dynamicIslandToast` and leave the session in a blocked/error state (D-09; consistent with D-18's no-silent-degrade rule). For D-08: surface the `ARCamera.TrackingState.limited(reason:)` sub-reason locally via a DynamicToast using `trackingLimitedReasonMessage` — expose the current limited reason from `ARPoseSource` (throttled, not once-per-frame) to a `@Published` property the view observes, and present the matching toast. The wire `driftConfidence` stays a flat 0.5 for any `.limited` (unchanged from Plan 01 — D-08 differentiates only locally, never on the wire). Use `import AVFoundation` for the permission API.</action>
  <verify>
    <automated>xcodebuild test -project mobile/ios-app/immersiveRT.xcodeproj -scheme immersiveRT -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - App builds and full suite stays green.
    - Source assertion: the precondition path presents `Toast.arUnavailable`/`Toast.cameraPermissionDenied` and does NOT call `motionSource.start()` / any CoreMotion fallback on failure (grep confirms no CoreMotion start in the failure branch).
    - Source assertion: a limited-reason DynamicToast is presented from a throttled property, not on the 60Hz path.
    - The wire driftConfidence mapping for `.limited` remains flat 0.5 (unchanged; grep in ARPoseConversion still shows single `.limited` → 0.5).
    - Commit: `feat(06.3-06): D-09 start gate + D-08 tracking-limited toast`
  </acceptance_criteria>
  <done>ARKit-unavailable/permission-denied hard-blocks session start with a toast; limited sub-reasons surface locally without changing the wire.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: On-device verification of D-09 block and D-08 toasts</name>
  <action>On a physical iPhone, verify the D-09 permission-denied block and each D-08 tracking-limited toast per the procedure below. Manual-only — these states are not reproducible in the Simulator.</action>
  <what-built>
    The D-09 start-time gate (blocks with a DynamicToast on unsupported/denied) and the D-08 local tracking-limited toasts, both routed through the existing DynamicToast component.
  </what-built>
  <how-to-verify>
    1. On a physical iPhone, revoke camera permission for the app in iOS Settings (or deny at the prompt), then start a session: confirm the session is BLOCKED with the camera-denied DynamicToast and does NOT connect/stream via CoreMotion (D-09).
    2. Re-grant permission; start a normal session.
    3. Induce ARKit `.limited` states and confirm the matching DynamicToast appears: cover the camera or face a blank featureless wall (insufficientFeatures), shake the phone rapidly (excessiveMotion), and observe the initial `initializing` state at startup (D-08).
    4. Confirm the desktop `drift:` HUD shows 0.5 during any `.limited` state (wire stays flat 0.5, unchanged) even though the phone shows the specific sub-reason locally.
  </how-to-verify>
  <resume-signal>Type "gates-verified" once the D-09 block and each D-08 sub-reason toast are confirmed on-device, or describe the mismatch.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user/OS permission → app | Camera permission state governs whether ARKit may run |
| ARKit camera → app | D-14 data-minimization must hold even as usage-description copy changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.3-03 | Info Disclosure / Privacy | NSCameraUsageDescription copy | low | mitigate | Usage string states camera is used only for QR + motion tracking and never shown/saved/sent, matching D-14 reality (accurate, minimal scope). |
| T-06.3-05 | Denial of Service (availability) | D-09 block path | medium | mitigate | Fail-closed by design: no ARKit → blocked with a clear toast, never a silent degraded stream that would mislead the user about tracking quality. |
| T-06.3-02 | Info Disclosure | ARSession/permission code | high | mitigate | D-14 preserved: precondition/permission code reads authorization status only; no camera-buffer access introduced. <!-- planner-discipline-allow: capturedImage --> |

No package installs this phase — ARKit/AVFoundation/SwiftUI are first-party Apple frameworks.
</threat_model>

<verification>
- Full suite green (Simulator).
- Manual on-device: D-09 permission-denied block and D-08 limited-reason toasts confirmed (Task 3) — these are not Simulator-reproducible (RESEARCH.md constraint).
</verification>

<success_criteria>
ARKit-unavailable/permission-denied blocks session start with a DynamicToast (no CoreMotion degrade, D-09); tracking-limited sub-reasons surface locally via DynamicToast (D-08) while the wire stays flat 0.5; camera usage copy is honest for ARKit (D-14).
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (this plan)
- `mobile/ios-app/immersiveRT/DynamicToast/Toast.swift` — modified. New static cases: `arUnavailable`, `cameraPermissionDenied`, tracking-limited factory/cases (D-08 four reasons), optional `recentered`.
- `mobile/ios-app/immersiveRT.xcodeproj/project.pbxproj` — modified. `INFOPLIST_KEY_NSCameraUsageDescription` updated in both build configs (accurate ARKit+QR copy).
- `mobile/ios-app/immersiveRT/Sensor/ARPoseSource.swift` — modified. Startup precondition check + throttled limited-reason exposure.
- `mobile/ios-app/immersiveRT/ActiveSessionView.swift` — modified. D-09 block toast + D-08 limited-reason toast wiring via `dynamicIslandToast`.
</artifacts_produced>

<output>
Create `.planning/phases/06.3-ios-native-client-arkit-world-tracking/06.3-06-SUMMARY.md` when done.
</output>
