---
phase: 05-sensor-fusion-and-packet-encoding
plan: 07
type: execute
wave: 4
depends_on: [05-05, 05-06]
files_modified:
  - client/src/phone.ts
  - client/src/sensor/devOverlay.ts
autonomous: true
requirements: [SENS-03, SENS-04, SENS-05, SENS-06]
user_setup: []
must_haves:
  truths:
    - "SENS-03 (live): the devicemotion loop feeds accelerationIncludingGravity magnitude (safeFloat-guarded) into a single ZUPTDetector each tick; when it returns true after ≥300ms of stillness, all three Kalman axes resetVelocity() and driftConfidence rises toward 1.0 — ZUPT runs on the live stream without adding latency (D-06)"
    - "SENS-04 (live): three Kalman1D instances (x/y/z, constructed with the calibration kalmanQ) predict() every tick from the safeFloat-guarded linear acceleration; their .position values fill the packet's deadReckoning px/py/pz (D-07), clamped to a bounded range so an un-reset drift cannot grow without limit"
    - "SENS-05 (live): a per-gesture displacement (deadReckoning position minus the origin captured at the last ZUPT) fills dx/dy/dz — it is ~0 immediately after a ZUPT reset, grows during a movement, is bounded, and the pre-reset value is saved as the completed-gesture output (SENS-05 is Claude's discretion per CONTEXT.md; a position-delta is used, not an acceleration sum, so dx/dy/dz share px/py/pz units)"
    - "SENS-06 (live): touchstart/touchmove/touchend listeners maintain a module-scope currentTouch { active, x, y }; touchX/touchY are clamp01-normalized so encode's ×65535 uint16 write cannot overflow; touchend clears active but preserves the last coordinates; every packet carries the live touch state"
    - "D-15 (dev-only): the phone screen shows a dev overlay (packet OS-fused quaternion vs live Madgwick quaternion, active ahrs.beta, ZUPT-fired-in-last-500ms, driftConfidence, rolling packet Hz) that is entirely inside `import.meta.env.DEV`, injected by devOverlay.ts, and Vite-dead-code-eliminated from the production bundle — the string `dev-overlay` does not appear in dist/assets/phone-*.js"
    - "D-04 (dev-only): the packet orientation defaults to the OS-fused eulerToQuat quaternion; the dev-only URL param `?orient=madgwick` swaps it to the Madgwick output (updateMadgwick), and rampBeta runs every tick regardless of source so the Madgwick filter stays converged for the overlay comparison"
    - "safeFloat guards every DeviceMotionEvent acceleration value before it reaches ZUPT or Kalman, and the per-tick dt is clamped, so one NaN/null sample or a stalled clock cannot poison accumulated position/velocity state (V5)"
  artifacts:
    - path: client/src/phone.ts
      provides: "live ZUPT+Kalman+gesture wiring, touch listeners + currentTouch, dev overlay call, Madgwick source-select, rampBeta — replacing every Plan 06 zero/inactive placeholder"
      contains: "ZUPTDetector"
    - path: client/src/sensor/devOverlay.ts
      provides: "updateOverlay(pkt, madgwickQuat, zuptFired, hz) — dev-only overlay that lazily injects and updates the #dev-overlay div"
      exports: ["updateOverlay"]
  key_links:
    - from: "client/src/phone.ts startSensorPipeline (devicemotion handler)"
      to: "client/src/sensor/zupt.ts ZUPTDetector.update / client/src/sensor/kalman.ts Kalman1D.predict+resetVelocity"
      via: "zupt.update(mag, Date.now()); kalmans[i].predict(accel, dt); on still → kalmans[i].resetVelocity()"
      pattern: "resetVelocity"
    - from: "client/src/phone.ts touch listeners"
      to: "packet touch fields via module-scope currentTouch"
      via: "touchstart/move set currentTouch.{active,x,y}=clamp01(...); packet reads currentTouch"
      pattern: "currentTouch"
    - from: "client/src/phone.ts devicemotion handler (dev-only)"
      to: "client/src/sensor/devOverlay.ts updateOverlay"
      via: "if (import.meta.env.DEV) updateOverlay(pkt, updateMadgwick(e), isStill, hz)"
      pattern: "updateOverlay"
    - from: "client/src/phone.ts devicemotion handler (dev-only)"
      to: "client/src/sensor/orientation.ts updateMadgwick / rampBeta"
      via: "mq = updateMadgwick(e); rampBeta(quatDelta(mq, prevMq)); if (?orient=madgwick) packet uses mq"
      pattern: "updateMadgwick"
---

<objective>
Deepen the Plan 06 thin slice into the full on-device sensor pipeline. Replace every zero/inactive
placeholder in `startSensorPipeline` with real behavior: ZUPT-gated Kalman dead-reckoning
(px/py/pz + driftConfidence), per-gesture displacement (dx/dy/dz), and live touch (touchActive/X/Y).
Add the dev-only tuning overlay (D-15) and the dev-only Madgwick source-select URL param (D-04),
with the Madgwick beta ramp running every tick so the overlay comparison stays honest. Nothing is
reduced — this is the "fill the placeholders" slice that makes the packet fully real.

Purpose: Satisfy SENS-03, SENS-04, SENS-05, SENS-06 as live, on-device behavior (their exact numeric
math is already unit-tested in Plans 03–05; this plan wires it into the 60Hz packet stream) and stand
up the dual-orientation evaluation surface (D-04/D-15) that Phase 6 uses to lock the winning source.
Output: sensor-pipeline additions to `client/src/phone.ts` and the new dev-only
`client/src/sensor/devOverlay.ts`.
</objective>

## Phase Goal (user story)

**As a** phone player in an active session, **I want to** have my phone's motion and touch fused
on-device (OS-fused orientation, dead-reckoned position, ZUPT-gated gesture displacement) and
streamed to every desktop as a compact 36-byte binary packet at my device's maximum sample rate,
**so that** each desktop can render my movements with low-latency, drift-honest fidelity.

> This plan completes that story: it fills the packet's position, gesture, and touch fields with real
> data and adds the dev overlay + Madgwick source-select used to evaluate orientation quality.

## Artifacts this phase produces

New symbols introduced across Phase 5 (this plan creates the ★ items):

- **phone.ts pipeline symbols:** ★ live `ZUPTDetector`/`Kalman1D` instances inside `startSensorPipeline`,
  ★`gestureOrigin`/`gestureDisplacement`/`lastCompletedGesture` state, ★`currentTouch` module state,
  ★`attachTouchListeners` + named `onTouchStart`/`onTouchMove`/`onTouchEnd` handlers,
  ★`useMadgwick` dev source-select flag, ★`quatDelta` helper, ★ rolling-Hz counter
- **devOverlay.ts:** ★`client/src/sensor/devOverlay.ts`, ★`updateOverlay(pkt, madgwickQuat, zuptFired, hz)`
- **Consumes:** `ZUPTDetector` (Plan 05), `Kalman1D` (Plan 05), `encodePacket`/`_packetBuf`/`safeFloat` (Plan 03),
  `eulerToQuat`/`updateMadgwick`/`ahrs`/`rampBeta` (Plan 04), `broadcastPacket`/`startSensorPipeline`/`primaryQuat` thin slice (Plan 06),
  `SensorPacket`/`Quaternion`/`Vector3` types (Plan 01)
- **Ends Phase 5's client scope:** after this plan, no placeholder fields remain in the packet.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-sensor-fusion-and-packet-encoding/05-CONTEXT.md
@.planning/phases/05-sensor-fusion-and-packet-encoding/05-RESEARCH.md
@.planning/phases/05-sensor-fusion-and-packet-encoding/05-PATTERNS.md
@client/src/phone.ts
@client/src/sensor/zupt.ts
@client/src/sensor/kalman.ts
@client/src/sensor/encode.ts
@client/src/sensor/orientation.ts
@client/src/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire ZUPT + Kalman dead-reckoning + gesture displacement into startSensorPipeline</name>
  <files>client/src/phone.ts</files>
  <read_first>
    - client/src/phone.ts (Plan 06 `startSensorPipeline(zuptThreshold, kalmanQ)` and its `devicemotion` handler — the block with `px/py/pz = 0`, `dx/dy/dz = 0`, `driftConfidence: 0` placeholders and the `// Plan 07: replace` comment; the module-scope `sessionStart`/`seq`/`primaryQuat` state; the `safeFloat` import from `./sensor/encode`; `encodePacket(pkt, _packetBuf)` + `broadcastPacket` calls)
    - client/src/sensor/zupt.ts (ZUPTDetector constructor `(windowMs = 300, threshold)` and `update(accelMag, nowMs): boolean`)
    - client/src/sensor/kalman.ts (Kalman1D constructor `(Q, R)`, `predict(accelMs2, dtSec): number`, `resetVelocity(): void`, `driftConfidence(): number`)
    - client/src/types.ts (SensorPacket field set; Vector3)
    - .planning/phases/05-sensor-fusion-and-packet-encoding/05-PATTERNS.md (sensor pipeline entry lines 261-316 — the gestureOrigin / kalmans / zupt shape this task realizes)
    - .planning/phases/05-sensor-fusion-and-packet-encoding/05-CONTEXT.md (D-06 ZUPT is a background correction, no live latency; D-07 deadReckoningPosition naming + driftConfidence)
  </read_first>
  <action>
    Add imports to phone.ts: `import { ZUPTDetector } from './sensor/zupt'`, `import { Kalman1D } from
    './sensor/kalman'`, and add `Vector3` to the existing `import type { ... } from './types'` line.

    Inside `startSensorPipeline(zuptThreshold, kalmanQ)`, before the listeners, construct the live
    dead-reckoning state (closure-local, so it is reinitialized once per session and cannot leak):
    `const zupt = new ZUPTDetector(300, zuptThreshold)`; `const kalmans = [new Kalman1D(kalmanQ), new
    Kalman1D(kalmanQ), new Kalman1D(kalmanQ)]`; `let gestureOrigin: Vector3 = { x: 0, y: 0, z: 0 }`;
    `let gestureDisplacement: Vector3 = { x: 0, y: 0, z: 0 }`; `let lastCompletedGesture: Vector3 =
    { x: 0, y: 0, z: 0 }`; `let lastTs = performance.now()`. Add two module-scope bound constants for
    magnitude clamping: a position bound (e.g. `POSITION_MAX = 100`) and a gesture bound (e.g.
    `GESTURE_MAX = 100`) — units are meters; the values only need to be finite float16 headroom, they
    exist to bound un-reset drift (see threat model).

    In the `devicemotion` handler, at the top compute a clamped delta time:
    `const now = performance.now()`; `const dtSec = Math.min(0.1, Math.max(0, (now - lastTs) / 1000))`;
    `lastTs = now`. Read the linear acceleration for Kalman (prefer `e.acceleration`, gravity-removed):
    `const ax = safeFloat(e.acceleration?.x)`, `ay`, `az` likewise. Read
    `accelerationIncludingGravity` for the ZUPT magnitude: `const ag = e.accelerationIncludingGravity`;
    `const mag = Math.hypot(safeFloat(ag?.x), safeFloat(ag?.y), safeFloat(ag?.z))`. Every value that
    reaches ZUPT or Kalman MUST pass through `safeFloat` first (V5).

    Compute stillness and integrate: `const isStill = zupt.update(mag, Date.now())`;
    `const rawPx = kalmans[0].predict(ax, dtSec)`, `rawPy`, `rawPz` likewise. Define a local
    `clamp(v, lim)` returning `Math.min(lim, Math.max(-lim, v))`. Build the bounded packet position:
    `px = clamp(rawPx, POSITION_MAX)`, `py`, `pz`. Compute the live gesture displacement as the
    position delta since the last ZUPT origin, bounded:
    `gestureDisplacement = { x: clamp(rawPx - gestureOrigin.x, GESTURE_MAX), y: clamp(rawPy -
    gestureOrigin.y, GESTURE_MAX), z: clamp(rawPz - gestureOrigin.z, GESTURE_MAX) }`.

    When `isStill` is true (ZUPT fired): save the completed gesture output
    `lastCompletedGesture = { ...gestureDisplacement }`; call `kalmans[0].resetVelocity();
    kalmans[1].resetVelocity(); kalmans[2].resetVelocity()`; re-anchor the origin to the current raw
    position `gestureOrigin = { x: rawPx, y: rawPy, z: rawPz }`; and zero the live displacement
    `gestureDisplacement = { x: 0, y: 0, z: 0 }` so the packet sent this tick already reads [0,0,0]
    (SENS-05: resets to zero after ZUPT). `lastCompletedGesture` is intentionally retained for Phase 6/8
    gesture-trigger consumers — do not delete it as unused.

    In the packet object, replace the Plan 06 placeholder assignments: set `dx/dy/dz` from
    `gestureDisplacement`, `px/py/pz` from the bounded position, and `driftConfidence` from the average
    of the three axes `(kalmans[0].driftConfidence() + kalmans[1].driftConfidence() +
    kalmans[2].driftConfidence()) / 3`. Leave `qw/qx/qy/qz` sourced from `primaryQuat` (Plan 06 — Task 3
    adds the dev source-select) and leave the touch fields as the Plan 06 placeholders (Task 2 wires
    them). Continue to encode via `encodePacket(pkt, _packetBuf)` and `broadcastPacket(...)` — do not
    introduce any per-tick buffer allocation; the single module-scope `_packetBuf` from Plan 03 stays
    the only buffer. Remove the `// Plan 07: replace` marker for the fields this task fills.
  </action>
  <verify>
    <automated>cd /home/ivancist/Documents/immersiveRT/client && grep -q "new ZUPTDetector" src/phone.ts && grep -q "new Kalman1D" src/phone.ts && grep -q "\.predict(" src/phone.ts && grep -q "\.resetVelocity()" src/phone.ts && grep -q "\.driftConfidence()" src/phone.ts && grep -q "gestureDisplacement" src/phone.ts && grep -q "gestureOrigin" src/phone.ts && grep -q "_packetBuf" src/phone.ts && npx tsc --noEmit 2>&1 | tail -10 && npm run build >/tmp/vite-build-07-1.log 2>&1 && ls dist/assets/phone-*.js >/dev/null 2>&1 && echo OK-DEADRECKON</automated>
  </verify>
  <acceptance_criteria>
    - `startSensorPipeline` constructs one `ZUPTDetector(300, zuptThreshold)` and three `Kalman1D(kalmanQ)` instances closure-local to the session
    - Each devicemotion tick calls `zupt.update(mag, Date.now())` on a safeFloat-guarded `accelerationIncludingGravity` magnitude and `kalmans[i].predict(safeFloat-guarded accel, dtSec)` with `dtSec` clamped to `[0, 0.1]`
    - On ZUPT true, all three axes `resetVelocity()`, `gestureOrigin` re-anchors to current position, and `gestureDisplacement` zeroes for the current packet
    - Packet `px/py/pz` come from bounded Kalman positions, `dx/dy/dz` from bounded `gestureDisplacement`, `driftConfidence` from the axis-averaged `driftConfidence()` — none are literal `0` anymore
    - `_packetBuf` remains the only buffer; no per-tick allocation added
    - `npx tsc --noEmit` passes and `npm run build` emits `dist/assets/phone-*.js`
    - Automated check prints `OK-DEADRECKON`
  </acceptance_criteria>
  <done>The live packet carries real ZUPT-gated Kalman dead-reckoning position, drift confidence, and per-gesture displacement — SENS-03/04/05 behavior runs on the 60Hz stream with all sensor inputs safeFloat-guarded and drift bounded.</done>
</task>

<task type="auto">
  <name>Task 2: Add live touch input (touchstart/move/end → currentTouch → packet touch fields)</name>
  <files>client/src/phone.ts</files>
  <read_first>
    - client/src/phone.ts (the packet object's `touchActive`/`touchX`/`touchY` Plan 06 placeholders; `startSensorPipeline`; `safeFloat` import; existing session-reset / reconnect paths that may call `startSensorPipeline` more than once)
    - client/src/sensor/encode.ts (offset 31/32/34 touch encoding — `touchActive?1:0`, `round(clamp01(touchX)*65535)`, `round(clamp01(touchY)*65535)`; the SensorPacket.touchX/touchY contract is a normalized [0,1] value, NOT a raw pixel or 0–65535 value — the ×65535 happens inside encode)
    - .planning/phases/05-sensor-fusion-and-packet-encoding/05-CONTEXT.md (D-13 — 1 primary touch point, raw normalized (x,y) every packet as uint16 + 1-byte active flag; gesture derivation happens on desktop)
  </read_first>
  <action>
    Add module-scope touch state at the top of phone.ts alongside the other sensor state:
    `let currentTouch = { active: false, x: 0, y: 0 }` and a guard `let touchListenersAttached = false`.
    Add a local `clamp01(v: number): number` helper (`Math.min(1, Math.max(0, v))`) — reuse the one
    from Task 1 if it is already at module scope.

    Define three named handlers (named, so they can be attached exactly once and removed on teardown —
    they MUST NOT be anonymous inline closures that leak on every session):
    - `onTouchStart(e: TouchEvent)` and `onTouchMove(e: TouchEvent)`: read `const t = e.touches[0]`;
      if `!t` return; set `currentTouch.active = true`; set
      `currentTouch.x = clamp01(safeFloat(t.clientX) / window.innerWidth)` and
      `currentTouch.y = clamp01(safeFloat(t.clientY) / window.innerHeight)`. clamp01 guarantees the
      value stays in [0,1] so encode's `×65535` uint16 write can never overflow or underflow (V5).
    - `onTouchEnd(_e: TouchEvent)`: set `currentTouch.active = false` but DO NOT modify `currentTouch.x`
      / `currentTouch.y` — the last known coordinates are preserved for Phase 6 trajectory use (D-13).

    Add `attachTouchListeners(): void` that is idempotent: `if (touchListenersAttached) return;
    touchListenersAttached = true;` then `document.body.addEventListener('touchstart', onTouchStart, {
    passive: true })`, `'touchmove'` → `onTouchMove` (passive), `'touchend'` → `onTouchEnd` (passive),
    and `'touchcancel'` → `onTouchEnd`. Call `attachTouchListeners()` from inside `startSensorPipeline`
    (or when the active view is shown). Because of the guard, re-entry on reconnect/re-calibration does
    NOT stack duplicate listeners (see threat model — no listener leak across session resets).

    In the packet object, replace the Plan 06 touch placeholders: `touchActive: currentTouch.active`,
    `touchX: currentTouch.x`, `touchY: currentTouch.y`. Remove the `// Plan 07: replace` marker for the
    touch fields.
  </action>
  <verify>
    <automated>cd /home/ivancist/Documents/immersiveRT/client && grep -q "touchstart" src/phone.ts && grep -q "touchmove" src/phone.ts && grep -q "touchend" src/phone.ts && grep -q "currentTouch" src/phone.ts && grep -q "touchListenersAttached" src/phone.ts && grep -q "clamp01" src/phone.ts && grep -q "touchActive: currentTouch.active" src/phone.ts && npx tsc --noEmit 2>&1 | tail -10 && npm run build >/tmp/vite-build-07-2.log 2>&1 && ls dist/assets/phone-*.js >/dev/null 2>&1 && echo OK-TOUCH</automated>
  </verify>
  <acceptance_criteria>
    - Module-scope `currentTouch { active, x, y }` exists; touchstart/touchmove/touchend/touchcancel listeners are attached via an idempotent `attachTouchListeners` guarded by `touchListenersAttached`
    - Handlers are named functions (not inline anonymous closures), so a second `startSensorPipeline` call does not add duplicate listeners
    - `touchX`/`touchY` are `clamp01`-normalized to [0,1] before assignment; `touchend` clears `active` but preserves the last x/y
    - Packet `touchActive`/`touchX`/`touchY` read from `currentTouch` (no placeholders remain)
    - `npx tsc --noEmit` passes and `npm run build` emits `dist/assets/phone-*.js`
    - Automated check prints `OK-TOUCH`
  </acceptance_criteria>
  <done>Every sensor packet carries live, clamped, single-point touch state (SENS-06); touch listeners attach exactly once per session and preserve the last coordinate on release.</done>
</task>

<task type="auto">
  <name>Task 3: Add the dev-only overlay (devOverlay.ts) + Madgwick source-select + per-tick beta ramp</name>
  <files>client/src/sensor/devOverlay.ts, client/src/phone.ts</files>
  <read_first>
    - client/src/phone.ts (Plan 06's dev byte/Hz `phoneLog('pkt ...')` line + its rolling tick counter — this task REPLACES that with the overlay call; the packet build where `qw/qx/qy/qz` come from `primaryQuat`; `phoneLog` itself stays for signaling events)
    - client/src/sensor/orientation.ts (`updateMadgwick(e): Quaternion` returns `{ w, x, y, z }`; `ahrs` instance with mutable `.beta`; `rampBeta(frameDelta): void` lowers beta toward 0.1 when the frame delta is below the convergence threshold)
    - client/src/types.ts (SensorPacket, Quaternion)
    - .planning/phases/05-sensor-fusion-and-packet-encoding/05-CONTEXT.md (D-04 — OS-fused default, `?orient=madgwick` dev-only switch, URL params dead-code-eliminated in prod; D-15 — overlay contents and prod auto-hide)
    - .planning/phases/05-sensor-fusion-and-packet-encoding/05-PATTERNS.md (devOverlay pattern lines 556-601 and dev-mode gating pattern lines 666-681 — note this plan places the file at `client/src/sensor/devOverlay.ts` with a single `updateOverlay` export, not the `ui/` two-function shape)
  </read_first>
  <action>
    Create `client/src/sensor/devOverlay.ts`. Import `import { ahrs } from './orientation'` and
    `import type { SensorPacket, Quaternion } from '../types'`. Keep a module-scope
    `let lastZuptFireTs = 0`. Export a single function
    `updateOverlay(pkt: SensorPacket, madgwickQuat: Quaternion, zuptFired: boolean, hz: number): void`.
    First line of the body: `if (!import.meta.env.DEV) return;` (belt-and-suspenders — the primary
    guard is the DEV-gated call site in phone.ts, which lets Vite tree-shake this whole module out of
    production). Lazily obtain the overlay element: `let el = document.getElementById('dev-overlay')`;
    if absent, create a `<div>` with id `dev-overlay`, set `el.style.cssText` to a fixed bottom-left,
    small-font, low-opacity, `white-space:pre`, `pointer-events:none` style (e.g. bottom/left 0,
    z-index below any modal, `font:10px/1.4 monospace`, `color:#0f0`, `background:rgba(0,0,0,0.6)`,
    `opacity:0.85`, `padding:4px 8px`), and append it to `document.body`. If `zuptFired`, set
    `lastZuptFireTs = performance.now()`. Set `el.textContent` to a multi-line string (3 decimal places
    for quaternion components): line 1 the packet/OS-fused quaternion from `pkt.qw/qx/qy/qz`; line 2 the
    Madgwick quaternion from `madgwickQuat`; line 3 `beta=${ahrs.beta.toFixed(3)}`,
    `ZUPT:${performance.now() - lastZuptFireTs < 500 ? 'YES' : 'no'}` (latched for 500ms),
    `drift=${pkt.driftConfidence.toFixed(2)}`, `hz=${hz.toFixed(0)}`. This file has no top-level side
    effects, so an unused import of it is fully tree-shakeable.

    In `client/src/phone.ts`, add `import { updateMadgwick, rampBeta } from './sensor/orientation'` and
    `import { updateOverlay } from './sensor/devOverlay'` (top-level imports are fine; they are only
    *referenced* inside `import.meta.env.DEV` blocks, so Rollup eliminates them from the prod bundle).
    Add a module-scope dev source-select flag `let useMadgwick = false`. Add a small helper
    `quatDelta(a: Quaternion, b: Quaternion): number` returning the L1 sum
    `Math.abs(a.w-b.w)+Math.abs(a.x-b.x)+Math.abs(a.y-b.y)+Math.abs(a.z-b.z)` (the per-frame convergence
    signal fed to rampBeta).

    Inside `startSensorPipeline`, read the URL param once (dev only):
    `if (import.meta.env.DEV) { useMadgwick = new URLSearchParams(location.search).get('orient') ===
    'madgwick'; }`. Add closure-local `let prevMq: Quaternion = { w: 1, x: 0, y: 0, z: 0 }` and a
    rolling-Hz counter `let hzCount = 0`, `let hzWindowStart = performance.now()`, `let hz = 0`.

    In the `devicemotion` handler, before building the packet, add the dev orientation path:
    `let qSource = primaryQuat`; `let mq: Quaternion = { w: 1, x: 0, y: 0, z: 0 }`; then
    `if (import.meta.env.DEV) { mq = updateMadgwick(e); rampBeta(quatDelta(mq, prevMq)); prevMq = mq; if
    (useMadgwick) qSource = mq; }`. Build the packet's `qw/qx/qy/qz` from `qSource` (default OS-fused,
    D-04). After `encodePacket` + `broadcastPacket`, REPLACE the Plan 06 `phoneLog('pkt ...B @...Hz')`
    dev log with the overlay path:
    `if (import.meta.env.DEV) { hzCount++; if (now - hzWindowStart >= 1000) { hz = hzCount / ((now -
    hzWindowStart) / 1000); hzCount = 0; hzWindowStart = now; } updateOverlay(pkt, mq, isStill, hz); }`.
    Delete Plan 06's rolling tick counter that fed that `phoneLog` line, but KEEP the `phoneLog`
    function and its use for non-sensor (signaling/WebRTC) events.
  </action>
  <verify>
    <automated>cd /home/ivancist/Documents/immersiveRT/client && test -f src/sensor/devOverlay.ts && grep -q "export function updateOverlay" src/sensor/devOverlay.ts && grep -q "import.meta.env.DEV" src/sensor/devOverlay.ts && grep -q "dev-overlay" src/sensor/devOverlay.ts && grep -q "updateOverlay(pkt" src/phone.ts && grep -q "updateMadgwick" src/phone.ts && grep -q "rampBeta" src/phone.ts && grep -q "orient" src/phone.ts && grep -q "import.meta.env.DEV" src/phone.ts && ! grep -q "pkt ' +" src/phone.ts && npx tsc --noEmit 2>&1 | tail -10 && npm run build >/tmp/vite-build-07-3.log 2>&1 && ls dist/assets/phone-*.js >/dev/null 2>&1 && ! grep -q "dev-overlay" dist/assets/phone-*.js && echo OK-DEVOVERLAY</automated>
  </verify>
  <acceptance_criteria>
    - `client/src/sensor/devOverlay.ts` exists, exports `updateOverlay(pkt, madgwickQuat, zuptFired, hz)`, guards the body with `if (!import.meta.env.DEV) return;`, lazily injects a `#dev-overlay` div, and shows OS/packet quaternion vs Madgwick quaternion (3 dp), `ahrs.beta`, a 500ms-latched ZUPT indicator, driftConfidence, and rolling Hz
    - phone.ts computes `mq = updateMadgwick(e)` and `rampBeta(quatDelta(mq, prevMq))` every tick inside `import.meta.env.DEV`; the packet quaternion is `primaryQuat` by default and `mq` only when `?orient=madgwick` (dev-only, D-04)
    - The Plan 06 `phoneLog('pkt ...')` byte/Hz line is removed and replaced by `updateOverlay(...)`; `phoneLog` itself remains for signaling events
    - Production tree-shake verified: after `npm run build`, `dev-overlay` does NOT appear in `dist/assets/phone-*.js`
    - `npx tsc --noEmit` passes and `npm run build` emits `dist/assets/phone-*.js`
    - Automated check prints `OK-DEVOVERLAY`
  </acceptance_criteria>
  <done>The phone shows a dev-only tuning overlay comparing OS-fused vs Madgwick orientation live (D-15), the Madgwick beta ramps every tick, `?orient=madgwick` swaps the packet source in dev builds (D-04), and none of this reaches the production bundle.</done>
</task>

</tasks>

<!-- planner-discipline-allow: dev-overlay -->
<!-- The `dev-overlay` div id must appear verbatim in Task 3's action (it is the element id the -->
<!-- overlay injects) and in the production-tree-shake negative grep, which targets the BUILD -->
<!-- ARTIFACT dist/assets/phone-*.js — not a source file the executor writes. Tree-shaking removes -->
<!-- the whole devOverlay module from production, so the literal is legitimately absent there. -->

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DeviceMotionEvent acceleration → ZUPT/Kalman state | Browser accel values (may be NaN/null) accumulate into position/velocity/variance state every tick |
| TouchEvent clientX/clientY → packet touch fields | Browser touch coordinates cross into the uint16 touch encoding |
| dev overlay + source-select code → production bundle | Dev-only tuning surface must not ship to players (D-04/D-15) |
| phone → desktop (unreliable WebRTC data channel) | 36-byte packets cross to the Phase 6 decoder |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering / DoS | NaN/null DeviceMotionEvent acceleration poisoning ZUPT/Kalman accumulated state | high | mitigate | Every `e.acceleration` / `e.accelerationIncludingGravity` value passes through `safeFloat` before `zupt.update` and `kalmans[i].predict`; `dtSec` is clamped to `[0, 0.1]` so a stalled/backward clock cannot produce an unbounded integration step. Kalman/ZUPT classes also NaN-guard internally (Plan 05), so no single sample can wedge dead-reckoning for the session |
| T-05-11 | Tampering | touchX/touchY overflow/underflow before the uint16 `×65535` normalization | medium | mitigate | phone.ts `clamp01`-normalizes `clientX/innerWidth` and `clientY/innerHeight` to [0,1] before assigning to `currentTouch`; encode.ts additionally clamps before `Math.round(v*65535)` — the uint16 write can never overflow or go negative |
| T-05-15 | Information Disclosure | dev overlay + `?orient=madgwick` source-select reachable in production | medium | mitigate | All overlay/source-select/Madgwick-in-loop code is inside `if (import.meta.env.DEV)`; `updateOverlay` early-returns when not DEV; devOverlay.ts has no top-level side effects so it tree-shakes. Verified by asserting `dev-overlay` is absent from `dist/assets/phone-*.js` after a production build (block-on-HIGH gate; this is medium and mitigated) |
| T-05-16 | DoS | gesture accumulator / Kalman position growing without bound if ZUPT never fires | medium | mitigate | `px/py/pz` and `dx/dy/dz` are clamped to `±POSITION_MAX`/`±GESTURE_MAX` before encoding; the ZUPT window is already bounded (Plan 05); `dtSec` clamp bounds the per-tick integration step. A drifting session produces bounded, float16-safe values rather than runaway magnitudes |
| T-05-17 | DoS / Tampering | touch listeners leaking / stale state across session resets or reconnects | low | mitigate | `attachTouchListeners` is idempotent behind `touchListenersAttached`, and handlers are named functions (not per-call anonymous closures), so re-entry into `startSensorPipeline` on reconnect does not stack duplicate listeners |
| T-05-SC | Tampering | npm installs | low | accept | No package installs in this plan — all dependencies (ahrs, @petamoriken/float16) were approved and installed under Plan 01's legitimacy gate |
</threat_model>

<verification>
- `cd client && npx tsc --noEmit` — strict TypeScript passes
- `cd client && npm run build` — emits `dist/assets/phone-*.js`; `dev-overlay` absent from the bundle (dev overlay tree-shaken, D-15)
- Structural grep gates confirm ZUPT/Kalman/gesture wiring, touch listeners, dev overlay + source-select
- Manual (device, captured in Plan 08): hold phone still ≥300ms → `driftConfidence` rises toward 1.0 and position stabilizes (SENS-03/04); flick → non-zero `gestureDisplacement` that resets after the pause (SENS-05); touch → touch fields populate every packet (SENS-06); `?orient=madgwick` swaps the overlay's active source (D-04); overlay shows OS vs Madgwick side-by-side (D-15)
</verification>

<success_criteria>
- ZUPT gates Kalman dead-reckoning on the live 60Hz stream: driftConfidence rises and position stabilizes after 300ms of stillness, no added latency (SENS-03, SENS-04, D-06)
- `gestureDisplacement` is a bounded per-gesture delta that resets to zero after each ZUPT and grows during movement (SENS-05)
- Every packet carries live, clamped single-point touch state; last coordinate preserved on release (SENS-06, D-13)
- Dev overlay (OS vs Madgwick, beta, ZUPT, drift, Hz) and `?orient=madgwick` are dev-only and dead-code-eliminated from production (D-04, D-15)
- safeFloat + dt clamp + magnitude bounds keep a bad sensor sample or un-reset drift from corrupting or runaway-growing accumulated state (V5)
</success_criteria>

<output>
Create `.planning/phases/05-sensor-fusion-and-packet-encoding/05-07-SUMMARY.md` when done.
</output>