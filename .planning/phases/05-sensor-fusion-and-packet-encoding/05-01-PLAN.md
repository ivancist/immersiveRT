---
phase: 05-sensor-fusion-and-packet-encoding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/package.json
  - client/tsconfig.json
  - client/vite.config.ts
  - client/index.html
  - client/src/types.ts
  - client/src/room.ts
  - client/public/phone.html
  - client/public/phone.js
autonomous: false
requirements: [PHONE-05]
user_setup: []
must_haves:
  truths:
    - "cd client && npm install succeeds and installs ahrs + @petamoriken/float16 (deps) and vite + typescript + vitest (devDeps)"
    - "npm run build emits dist/index.html (Vite-built desktop bundle) plus dist/phone.html and dist/phone.js copied verbatim from client/public/ — both the desktop SPA and the phone page still function identically"
    - "D-01: client/ is a Vite multi-page project (package.json + vite.config.ts + tsconfig.json) that bundles TypeScript entry points to dist/"
    - "D-02: the former room.js is migrated to strict-TypeScript client/src/room.ts with no behavior change; the QRCode CDN global is typed via an ambient declaration so tsc --noEmit is clean"
    - "D-10/D-11/D-14: client/src/types.ts defines the SensorPacket interface with the schema-v1 field set (seq, timestamp, quaternion qw/qx/qy/qz, gesture displacement dx/dy/dz, dead-reckoning position px/py/pz, driftConfidence, touchActive/touchX/touchY)"
  artifacts:
    - path: client/package.json
      provides: "npm manifest with dev/build/test scripts and pinned deps"
      contains: "\"ahrs\""
    - path: client/tsconfig.json
      provides: "strict TypeScript config, target ES2020, DOM lib, noEmit"
      contains: "\"strict\": true"
    - path: client/vite.config.ts
      provides: "Vite MPA config (room entry) + vitest jsdom env"
      contains: "rollupOptions"
    - path: client/src/types.ts
      provides: "Quaternion, Vector3, SensorPacket, TouchState shared types"
      exports: ["Quaternion", "Vector3", "SensorPacket", "TouchState"]
    - path: client/src/room.ts
      provides: "TypeScript desktop SPA (migrated from room.js, behavior-preserving)"
      min_lines: 800
    - path: client/index.html
      provides: "Vite room entry (module script → ./src/room.ts)"
      contains: "./src/room.ts"
  key_links:
    - from: "client/vite.config.ts"
      to: "client/index.html"
      via: "rollupOptions.input.room resolves index.html which loads ./src/room.ts"
      pattern: "input"
    - from: "client/public/phone.html"
      to: "dist/phone.html"
      via: "Vite publicDir copies phone.html + phone.js verbatim to dist root — temporary bridge until Plan 02 migrates the phone entry"
      pattern: "public"
---

<objective>
Stand up the client build system: convert the flat `client/dist/` folder of static files into a
proper Vite + TypeScript project. Create `package.json`, `tsconfig.json`, and `vite.config.ts`;
install `ahrs` and `@petamoriken/float16` (plus vite/typescript/vitest); define the shared sensor
type contract in `src/types.ts`; and migrate the desktop SPA `room.js` to strict-TypeScript
`src/room.ts`. The phone page rides along untouched via Vite's `public/` directory so the whole
app still builds and runs after this plan.

Purpose: Unlock the npm ecosystem (D-01) so later plans can import `ahrs`/`@petamoriken/float16`,
and establish the one-time-shared `SensorPacket` type (D-02) that Phase 6 + Phase 7 will consume.
Output: A working `npm run build` that emits `dist/index.html` (Vite room bundle) + `dist/phone.html`
+ `dist/phone.js` (verbatim), the shared `types.ts`, and the TypeScript `room.ts`.
</objective>

## Phase Goal (user story)

**As a** phone player in an active session, **I want to** have my phone's motion and touch fused
on-device (OS-fused orientation, dead-reckoned position, ZUPT-gated gesture displacement) and
streamed to every desktop as a compact 36-byte binary packet at my device's maximum sample rate,
**so that** each desktop can render my movements with low-latency, drift-honest fidelity.

> This plan delivers the enabling foundation: the Vite/TypeScript build (D-01), the shared
> `SensorPacket` contract (D-02), and the desktop-side migration — a required shared foundation
> before any typed sensor code can be written.

## Artifacts this phase produces

New symbols introduced across Phase 5 (this plan creates the ★ items; later plans add the rest —
listed here so downstream drift verification excludes them):

- **Config files:** ★`client/package.json`, ★`client/tsconfig.json`, ★`client/vite.config.ts`
- **Moved entry files:** ★`client/index.html` (was dist/), `client/phone.html` (Plan 02)
- **Temporary bridge:** ★`client/public/phone.html`, ★`client/public/phone.js` (removed in Plan 02)
- **Types (client/src/types.ts):** ★`Quaternion`, ★`Vector3`, ★`SensorPacket`, ★`TouchState`
- **Migrated sources:** ★`client/src/room.ts`, `client/src/phone.ts` (Plan 02)
- **Sensor modules:** `client/src/sensor/encode.ts` (Plan 03), `client/src/sensor/orientation.ts` (Plan 04), `client/src/sensor/zupt.ts` + `client/src/sensor/kalman.ts` (Plan 05)
- **Sensor symbols:** `encodePacket`/`computeCalibration`/`runCalibration`/`safeFloat`/`SCHEMA_VERSION`/`BUF_SIZE`/`_packetBuf` (Plan 03), `eulerToQuat`/`updateMadgwick`/`ahrs`/`rampBeta` (Plan 04), `ZUPTDetector` (Plan 05), `Kalman1D` (Plan 05)
- **UI:** `client/src/ui/devOverlay.ts` with `initDevOverlay`/`updateDevOverlay` (Plan 07), `#view-calibrating` (Plan 06), `#dev-overlay` div (Plan 07)
- **phone.ts pipeline symbols:** `broadcastPacket`, `startSensorPipeline`, `currentTouch`, `getGestureDisplacement` (Plans 06-07)
- **Tests:** `client/tests/encode.test.ts` (Plan 03), `orientation.test.ts` (Plan 04), `zupt.test.ts` + `kalman.test.ts` (Plan 05)
- **npm scripts:** ★`dev` (vite), ★`build` (vite build), ★`test` (vitest run)

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-sensor-fusion-and-packet-encoding/05-CONTEXT.md
@.planning/phases/05-sensor-fusion-and-packet-encoding/05-RESEARCH.md
@.planning/phases/05-sensor-fusion-and-packet-encoding/05-PATTERNS.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Approve the ahrs package before install (SUS legitimacy gate)</name>
  <what-built>
    Nothing is installed yet. This gate blocks the automated install in Task 2 until a human
    verifies the one flagged dependency. Per the Package Legitimacy Audit in 05-RESEARCH.md:
    - ahrs 1.3.3 — verdict SUS (low downloads: 337/wk), but CLAUDE.md explicitly recommends it as
      the only npm package providing both Madgwick and Mahony algorithms. No postinstall script.
    - @petamoriken/float16 3.9.3 — verdict OK (2.1M/wk), vite + vitest OK — no gate needed for these.
  </what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/ahrs — confirm it is `psiphi75/ahrs`, latest 1.3.3,
       source repo github.com/psiphi75/ahrs, and that the package page shows no install/postinstall
       scripts and no recent ownership-transfer or typosquat warnings.
    2. Optionally open https://github.com/psiphi75/ahrs and confirm the README documents the
       Madgwick/Mahony constructor (`beta`, `sampleInterval`, `algorithm`).
    3. Confirm the package name is spelled exactly `ahrs` (not a look-alike) before approving.
  </how-to-verify>
  <resume-signal>Type "approved" to allow `npm install ahrs`, or describe a concern to stop.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Create the Vite/TypeScript scaffold, install deps, and bridge the phone page via public/</name>
  <files>client/package.json, client/tsconfig.json, client/vite.config.ts, client/index.html, client/public/phone.html, client/public/phone.js</files>
  <read_first>
    - client/dist/index.html (current desktop entry; note the qrcode CDN `<script>` in the head lines 7-11 and `<script src="/room.js"></script>` at line 576 — the CDN script stays, the room.js tag becomes a module tag)
    - client/dist/phone.html (current phone page — moved untouched into public/)
    - .planning/phases/05-sensor-fusion-and-packet-encoding/05-PATTERNS.md (package.json pattern lines 40-60; tsconfig pattern lines 71-83; vite.config pattern lines 94-113; Pitfall 7 note that output HTML name mirrors the source filename)
    - .planning/phases/05-sensor-fusion-and-packet-encoding/05-RESEARCH.md (Standard Stack versions lines 121-162; Pattern 1 Vite MPA lines 263-289; Pitfall 7 lines 623-627)
  </read_first>
  <action>
    Create `client/package.json` with `"name": "immersivert-client"`, `"private": true`,
    `"type": "module"`, scripts `dev` = `vite`, `build` = `vite build`, `test` = `vitest run`,
    `typecheck` = `tsc --noEmit`. Dependencies: `ahrs` pinned to `1.3.3` and `@petamoriken/float16`
    pinned to `3.9.3`. devDependencies: `vite` `8.1.4`, `typescript` `^5.0.0`, `vitest` `^3.0.0`.

    Create `client/tsconfig.json` with compilerOptions `target: ES2020`, `module: ESNext`,
    `moduleResolution: bundler`, `strict: true`, `lib: ["ES2020", "DOM", "DOM.Iterable"]`,
    `noEmit: true`, `skipLibCheck: true`, `types: ["vite/client", "vitest/globals"]`, and
    `include: ["src/**/*", "tests/**/*"]`. Add `DOM.Iterable` so WebTransport
    `incomingBidirectionalStreams` iteration type-checks.

    Create `client/vite.config.ts` per Pattern 1 but with a SINGLE entry for now: `root: __dirname`,
    `build.outDir: 'dist'`, `build.emptyOutDir: true`, `build.rollupOptions.input = { room: resolve(__dirname, 'index.html') }`,
    and `test: { environment: 'jsdom' }`. Do NOT add a `phone` entry yet — Plan 02 adds it. Import
    `defineConfig` from `vite` and `resolve` from `path`.

    Bridge the phone page so the build stays whole: `git mv client/dist/phone.html client/public/phone.html`
    and `git mv client/dist/phone.js client/public/phone.js` (Vite copies `public/` verbatim to dist
    root, so `/phone` and `/phone.js` keep working while phone.js is still plain JS). Leave the
    `<script src="/phone.js" defer>` tag in the public copy unchanged.

    Move the desktop entry: `git mv client/dist/index.html client/index.html`, then change the
    `<script src="/room.js"></script>` tag to `<script type="module" src="./src/room.ts"></script>`.
    Keep the qrcode CDN `<script>` in the head exactly as-is (external dependency, unchanged).
    Delete the now-stale `client/dist/room.js` (it becomes source at src/room.ts in Task 3;
    `git rm client/dist/room.js`).

    Run `cd client && npm install`. Confirm node_modules contains ahrs, @petamoriken/float16, vite,
    vitest.
  </action>
  <verify>
    <automated>cd /home/ivancist/Documents/immersiveRT/client && npm install >/tmp/npm-install.log 2>&1 && test -d node_modules/ahrs && test -d node_modules/@petamoriken/float16 && test -d node_modules/vite && test -d node_modules/vitest && test -f public/phone.html && test -f public/phone.js && grep -q './src/room.ts' index.html && grep -q '"ahrs": "1.3.3"' package.json && echo OK-SCAFFOLD</automated>
  </verify>
  <acceptance_criteria>
    - `cd client && npm install` exits 0 and node_modules/ahrs, node_modules/@petamoriken/float16, node_modules/vite, node_modules/vitest all exist
    - `client/package.json` pins `ahrs` to `1.3.3` and `@petamoriken/float16` to `3.9.3`; scripts include `build` (`vite build`) and `test` (`vitest run`)
    - `client/tsconfig.json` has `"strict": true`, `"noEmit": true`, and `lib` includes `DOM`
    - `client/vite.config.ts` declares `rollupOptions.input.room` = `index.html` and `test.environment` = `jsdom`; no `phone` input yet
    - `client/index.html` loads `<script type="module" src="./src/room.ts">` and still loads the qrcode CDN script; `client/public/phone.html` + `client/public/phone.js` exist; `client/dist/room.js` is deleted
    - Automated check prints `OK-SCAFFOLD`
  </acceptance_criteria>
  <done>The client is an installable Vite project; ahrs + float16 + vite + vitest are present; the desktop entry points at ./src/room.ts and the phone page is bridged via public/ so nothing is broken yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Define shared sensor types and migrate room.js to strict TypeScript</name>
  <files>client/src/types.ts, client/src/room.ts</files>
  <read_first>
    - client/dist/room.js (full file — the 931-line desktop SPA being migrated; note every top-level `var`, the `QRCode` global usage from the CDN, the WebTransport/WebSocket signaling, and any DOM `getElementById` sites that need strict null handling)
    - .planning/phases/05-sensor-fusion-and-packet-encoding/05-PATTERNS.md (types.ts pattern lines 152-177; room.ts migration approach lines 322-327 — var→const/let, remove 'use strict', no new logic)
    - .planning/phases/05-sensor-fusion-and-packet-encoding/05-CONTEXT.md (D-02 — types defined once and shared; D-07 — deadReckoningPosition naming, driftConfidence 0–1; D-13 — touch is normalized x/y + active flag)
  </read_first>
  <behavior>
    - types.ts exports a `SensorPacket` interface whose fields exactly cover schema v1 (D-14): `seq: number`, `timestamp: number`, `qw/qx/qy/qz: number`, `dx/dy/dz: number`, `px/py/pz: number`, `driftConfidence: number`, `touchActive: boolean`, `touchX: number`, `touchY: number`.
    - types.ts exports `Quaternion { w, x, y, z }`, `Vector3 { x, y, z }`, and `TouchState { active: boolean; x: number; y: number }` (all number-valued).
    - room.ts compiles under `tsc --noEmit` with `strict: true` and preserves the desktop SPA behavior (lobby, room page, QR rendering, signaling) with no logic changes.
  </behavior>
  <action>
    Create `client/src/types.ts` exporting four interfaces exactly as the shared contract: `Quaternion`
    (`w, x, y, z: number`), `Vector3` (`x, y, z: number`), `TouchState` (`active: boolean; x: number;
    y: number` — x/y normalized 0–1), and `SensorPacket` with the schema-v1 fields listed in
    &lt;behavior&gt;. Add a short doc comment above `SensorPacket` citing D-14 and stating that field
    ORDER here documents intent but the wire layout is fixed by `encode.ts` byte offsets, not by this
    interface. Do not import anything into types.ts (leaf module).

    Migrate `client/dist/room.js` into `client/src/room.ts`: copy the file, remove the `'use strict'`
    pragma (TS modules are strict), convert every top-level `var` to `const` (never reassigned) or
    `let` (reassigned), and add explicit types to module state and function signatures. For the
    `QRCode` CDN global, add an ambient declaration at the top of room.ts (`declare const QRCode: {
    toCanvas: (...args: any[]) => void }` — or the minimal shape room.ts actually calls) so tsc does
    not error on the undeclared global. For strict-null DOM access, narrow `document.getElementById(...)`
    results before use (guard with `if (!el) return;` or non-null assertion only where the element is
    guaranteed present in index.html). Do NOT change any runtime behavior, message shapes, or control
    flow — this is a mechanical, behavior-preserving migration (D-02). Sensor/decoding logic is NOT
    added here (that is Phase 6).

    After migration, `client/dist/room.js` must already be deleted (Task 2). Run
    `npm run build` and confirm it emits `dist/index.html`, `dist/phone.html`, `dist/phone.js`, and a
    hashed room bundle under `dist/assets/`.
  </action>
  <verify>
    <automated>cd /home/ivancist/Documents/immersiveRT/client && npx tsc --noEmit 2>&1 | tail -20 && npm run build >/tmp/vite-build.log 2>&1 && test -f dist/index.html && test -f dist/phone.html && test -f dist/phone.js && ls dist/assets/room-*.js >/dev/null 2>&1 && grep -q 'SensorPacket' src/types.ts && grep -qE 'export interface (Quaternion|Vector3|TouchState)' src/types.ts && echo OK-TYPES-ROOM</automated>
  </verify>
  <acceptance_criteria>
    - `cd client && npx tsc --noEmit` reports zero errors (strict mode)
    - `client/src/types.ts` exports `Quaternion`, `Vector3`, `TouchState`, and `SensorPacket`; `SensorPacket` contains fields `seq`, `timestamp`, `qw`, `qx`, `qy`, `qz`, `dx`, `dy`, `dz`, `px`, `py`, `pz`, `driftConfidence`, `touchActive`, `touchX`, `touchY`
    - `client/src/room.ts` contains no `'use strict'` pragma and no top-level `var` (grep confirms), and declares the `QRCode` global
    - `npm run build` emits `dist/index.html`, `dist/phone.html`, `dist/phone.js`, and `dist/assets/room-*.js`
    - Automated check prints `OK-TYPES-ROOM`
  </acceptance_criteria>
  <done>The shared SensorPacket contract exists and the desktop SPA is a strict-TypeScript module that builds through Vite, with the phone page still served verbatim from public/.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → build machine | Third-party package code (ahrs, @petamoriken/float16, vite, vitest) is fetched and executed at build time |
| qrcode CDN (jsdelivr) → desktop browser | Existing external script (unchanged; SRI integrity hash already present in index.html) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-SC | Tampering | `npm install ahrs` (SUS, 337/wk) + other installs | high | mitigate | Task 1 `checkpoint:human-verify` (blocking-human, never auto-approved) verifies ahrs on npmjs.com before install; ahrs has no postinstall script; versions pinned exactly in package.json; @petamoriken/float16 + vite + vitest are OK-verdict |
| T-05-08 | Tampering | qrcode CDN script in index.html | low | accept | Existing `<script>` retains its SRI `integrity` + `crossorigin` attributes; migration does not touch the CDN tag |
</threat_model>

<verification>
- `cd client && npm install` — installs cleanly (only after Task 1 approval)
- `cd client && npx tsc --noEmit` — strict TypeScript passes for types.ts + room.ts
- `cd client && npm run build` — emits dist/index.html + dist/phone.html + dist/phone.js + dist/assets/room-*.js
- Manual (optional): serve dist/ and confirm the desktop lobby + QR still render and the phone page still loads
</verification>

<success_criteria>
- Vite + TypeScript + vitest project exists under client/ with ahrs + @petamoriken/float16 installed (D-01)
- Shared SensorPacket / Quaternion / Vector3 / TouchState types defined once (D-02, D-14)
- room.js migrated to strict-TS src/room.ts with no behavior change (D-02)
- `npm run build` produces a working dist/ where both the desktop and phone pages function
</success_criteria>

<output>
Create `.planning/phases/05-sensor-fusion-and-packet-encoding/05-01-SUMMARY.md` when done.
</output>
