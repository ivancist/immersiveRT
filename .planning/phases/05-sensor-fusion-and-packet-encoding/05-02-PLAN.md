---
phase: 05-sensor-fusion-and-packet-encoding
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - client/src/phone.ts
  - client/phone.html
  - client/vite.config.ts
  - client/public/phone.html
  - client/public/phone.js
autonomous: true
requirements: [PHONE-04]
user_setup: []
must_haves:
  truths:
    - "client/src/phone.ts is the strict-TypeScript migration of the former phone.js — the permission gate, WebTransport/WebSocket signaling, WebRTC unreliable data-channel fan-out, Wake Lock, heartbeat, motion indicator, and on-screen debug log are all preserved with identical behavior"
    - "phone.html is a Vite entry (module script → ./src/phone.ts); the temporary client/public/phone.* bridge from Plan 01 is removed"
    - "vite.config.ts declares BOTH phone and room inputs, and npm run build emits dist/phone.html + dist/index.html with hashed JS bundles for each page"
    - "The WebRTC data channel is still created with { ordered: false, maxRetransmits: 0 } (Phase 4 D-05) — the sensor hot path in later plans reuses this exact channel (PHONE-04)"
  artifacts:
    - path: client/src/phone.ts
      provides: "TypeScript phone client (migrated from phone.js, behavior-preserving) ready for sensor-pipeline hooks"
      min_lines: 800
    - path: client/phone.html
      provides: "Vite phone entry (module script → ./src/phone.ts)"
      contains: "./src/phone.ts"
    - path: client/vite.config.ts
      provides: "Vite MPA config with phone + room inputs"
      contains: "phone"
  key_links:
    - from: "client/vite.config.ts"
      to: "client/phone.html"
      via: "rollupOptions.input.phone resolves phone.html which loads ./src/phone.ts"
      pattern: "phone"
    - from: "client/src/phone.ts openChannelToPeer"
      to: "RTCPeerConnection.createDataChannel"
      via: "data channel created { ordered: false, maxRetransmits: 0 } — the sensor packet transport (Phase 4 D-05, unchanged)"
      pattern: "maxRetransmits"
---

<objective>
Migrate the phone client `phone.js` into strict-TypeScript `client/src/phone.ts` and promote
`phone.html` from the temporary `public/` bridge to a real Vite entry. This is a mechanical,
behavior-preserving migration — no sensor pipeline yet — that turns the phone page into a typed
ES-module so Plans 06/07 can import the sensor modules and wire the packet loop.

Purpose: Complete the D-01/D-02 build migration for the phone side so typed sensor code can be
added. The migrated `phone.ts` keeps the exact WebRTC data-channel fan-out (`ordered: false,
maxRetransmits: 0`) that the sensor hot path (PHONE-04) will send over.
Output: `client/src/phone.ts`, a Vite-entry `client/phone.html`, an updated `vite.config.ts` with
both page inputs, and removal of the `public/phone.*` bridge.
</objective>

## Phase Goal (user story)

**As a** phone player in an active session, **I want to** have my phone's motion and touch fused
on-device (OS-fused orientation, dead-reckoned position, ZUPT-gated gesture displacement) and
streamed to every desktop as a compact 36-byte binary packet at my device's maximum sample rate,
**so that** each desktop can render my movements with low-latency, drift-honest fidelity.

> This plan migrates the phone client to TypeScript — the required foundation for wiring the sensor
> loop into the existing data channels. Behavior is unchanged; the slice deepens in Plan 06.

## Artifacts this phase produces

New symbols introduced across Phase 5 (this plan creates the ★ items; other plans' items listed
for drift-verification exclusion):

- **Migrated source:** ★`client/src/phone.ts` (from client/public/phone.js), ★`client/phone.html` (Vite entry)
- **Removed bridge:** ★`client/public/phone.html`, ★`client/public/phone.js` (deleted this plan)
- **Config:** ★`client/vite.config.ts` gains the `phone` input
- **Preserved phone.js symbols now typed:** `showView`, `attachGrantButton`, `signalSend`, `startPhoneClient`, `sendWtMessage`/`sendWtRequest`, `listenForServerPushes`, `handleServerPush`, `openChannelToPeer`, `closePeer`, `onPlayerReady`, `requestWakeLock`, `startHeartbeat`, `sendPhoneState`, `startMotionIndicator`, `initOnScreenLog`, `phoneLog`
- **Added later:** `broadcastPacket` + `startSensorPipeline` + `currentTouch` + `getGestureDisplacement` (Plans 06-07), sensor modules (Plans 03-05), `devOverlay.ts` (Plan 07)

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-sensor-fusion-and-packet-encoding/05-CONTEXT.md
@.planning/phases/05-sensor-fusion-and-packet-encoding/05-PATTERNS.md
@.planning/phases/04-phone-bootstrap-and-webrtc-channels/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the phone entry to Vite and promote phone.html out of public/</name>
  <files>client/vite.config.ts, client/phone.html, client/public/phone.html, client/public/phone.js</files>
  <read_first>
    - client/vite.config.ts (the single-room input created in Plan 01 — add a phone input alongside it)
    - client/public/phone.html (the bridged phone page from Plan 01 — its six views and the `<script src="/phone.js" defer>` tag at the bottom)
    - .planning/phases/05-sensor-fusion-and-packet-encoding/05-PATTERNS.md (phone.html script-tag replacement lines 130-134; Pitfall 7 — output HTML filename mirrors source filename)
  </read_first>
  <action>
    In `client/vite.config.ts`, extend `build.rollupOptions.input` to include BOTH entries:
    `phone: resolve(__dirname, 'phone.html')` and `room: resolve(__dirname, 'index.html')`. Keep the
    output filename identical to the source (Pitfall 7: `phone.html` → `dist/phone.html`).

    Promote the phone page: `git mv client/public/phone.html client/phone.html`. In the moved
    `client/phone.html`, replace the bottom `<script src="/phone.js" defer></script>` tag with
    `<script type="module" src="./src/phone.ts"></script>`. Delete the now-orphan
    `git rm client/public/phone.js` (its content moves to src/phone.ts in Task 2). After this,
    `client/public/` should contain no phone files.
  </action>
  <verify>
    <automated>cd /home/ivancist/Documents/immersiveRT/client && grep -q "phone: resolve" vite.config.ts && grep -q "room: resolve" vite.config.ts && test -f phone.html && grep -q './src/phone.ts' phone.html && ! test -f public/phone.js && ! test -f public/phone.html && echo OK-PHONE-ENTRY</automated>
  </verify>
  <acceptance_criteria>
    - `client/vite.config.ts` `rollupOptions.input` has both `phone` (→ phone.html) and `room` (→ index.html)
    - `client/phone.html` exists at the project root and its script tag is `<script type="module" src="./src/phone.ts">` (the old `/phone.js` defer tag is gone)
    - `client/public/phone.html` and `client/public/phone.js` no longer exist
    - Automated check prints `OK-PHONE-ENTRY`
  </acceptance_criteria>
  <done>phone.html is a first-class Vite entry pointing at ./src/phone.ts, and the temporary public/ bridge is fully removed.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate phone.js to strict-TypeScript src/phone.ts (behavior-preserving)</name>
  <files>client/src/phone.ts</files>
  <read_first>
    - client/public/phone.js — WAIT: this file was moved to src in Task 1's git history. Read its content from git (`git show HEAD:client/public/phone.js`) OR read the working copy BEFORE deletion. The executor should read the original phone.js content in full — all 868 lines: transport state (lines 15-32), showView (41-49), permission gate (55-88), signalSend (92-98), WebTransport helpers, WebRTC openChannelToPeer + data channel creation, onPlayerReady (655-677), handleServerPush (680-732), Wake Lock / heartbeat / sendPhoneState (736-760), startMotionIndicator (762-779), visibilitychange (791-814), on-screen log (816-860), bootstrap (862-868)
    - .planning/phases/05-sensor-fusion-and-packet-encoding/05-PATTERNS.md (phone.ts migration lines 182-240 — typed transport state, signalSend signature, peerConnections Map typing, data channel { ordered: false, maxRetransmits: 0 } must not change, onPlayerReady hook)
    - client/src/types.ts (import types if any signaling shapes benefit; not required for this mechanical pass)
    - client/tsconfig.json (strict settings that govern the migration)
  </read_first>
  <action>
    Create `client/src/phone.ts` from the original phone.js content. Perform a mechanical,
    behavior-preserving migration:

    Remove the `'use strict'` pragma. Convert top-level `var` declarations to `const` (never
    reassigned) or `let` (reassigned) and add explicit types per PATTERNS: `transport: WebTransport |
    null`, `ws: WebSocket | null`, `useWt: boolean`, `myId: string | null`, `roomCode`/`mySlot`/
    `myUsername`/`reconnectToken` typed, `iceServers: RTCIceServer[]`, `peers: Array<{ id: string;
    slot: number; username: string }>`, `peerConnections: Map<string, { pc: RTCPeerConnection; dc:
    RTCDataChannel; channelOpen: boolean; flagClose: () => void }>`, `openChannelCount: number`,
    `wakeLockSentinel: WakeLockSentinel | null`, `heartbeatInterval: ReturnType<typeof setInterval> |
    null`.

    Type every function signature (params + return). For DOM access under strict null checks, narrow
    `document.getElementById(id)` results (guard `if (!el) return;`, or `const el = document.getElementById(id)!`
    only where index/phone HTML guarantees presence). For the iOS permission gate, keep
    `DeviceMotionEvent.requestPermission()` as the FIRST statement inside the synchronous click handler
    — do NOT introduce any `await`/`.then`/`setTimeout` before it during migration (Phase 4 D-12 /
    Pitfall 1). Preserve the WebTransport `.getReader()` iteration and the concurrent
    `listenForServerPushes` start (Phase 4 Pitfall 2).

    CRITICAL: the data channel creation options `{ ordered: false, maxRetransmits: 0 }` must remain
    byte-identical (Phase 4 D-05) — this is the sensor transport. Keep `handleServerPush` cases
    (`pair-error`, `player-ready`, `answer`, `ice-candidate`, `peer-joined`, `peer-left`,
    `session-ended`) with the same behavior. Do NOT add any sensor pipeline, encoding, or overlay
    code in this plan — those are Plans 06/07. Leave a single `// Phase 5: sensor pipeline hooks
    added in Plan 06 (calibration + startSensorPipeline)` marker comment inside `onPlayerReady` where
    the hook will land, but change no behavior.

    Add ambient types for any browser API TypeScript's lib.dom lacks (WebTransport is in recent
    lib.dom; if tsc errors on `WebTransport`/`WebTransportBidirectionalStream`, add a minimal
    `declare` in a local `src/webtransport.d.ts` or inline `declare`). Run `npx tsc --noEmit` and fix
    all strict errors without changing runtime behavior.
  </action>
  <verify>
    <automated>cd /home/ivancist/Documents/immersiveRT/client && npx tsc --noEmit 2>&1 | tail -20 && npm run build >/tmp/vite-build-phone.log 2>&1 && test -f dist/phone.html && ls dist/assets/phone-*.js >/dev/null 2>&1 && grep -q 'maxRetransmits: 0' src/phone.ts && grep -q 'ordered: false' src/phone.ts && ! grep -q "'use strict'" src/phone.ts && ! grep -nE '^\s*var ' src/phone.ts && echo OK-PHONE-TS</automated>
  </verify>
  <acceptance_criteria>
    - `cd client && npx tsc --noEmit` reports zero errors (strict)
    - `src/phone.ts` contains `{ ordered: false, maxRetransmits: 0 }` on the data channel (unchanged from Phase 4 D-05)
    - `src/phone.ts` has no `'use strict'` pragma and no top-level `var` (grep confirms)
    - The iOS permission gate still calls `DeviceMotionEvent.requestPermission()` as the first statement in the click handler with no preceding await/then/setTimeout
    - `npm run build` emits `dist/phone.html` and `dist/assets/phone-*.js`
    - Automated check prints `OK-PHONE-TS`
  </acceptance_criteria>
  <done>The phone client is a strict-TypeScript ES module that builds through Vite with identical runtime behavior, ready for sensor-pipeline wiring in Plan 06.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| phone browser → Rust server (WebTransport :4433 / WSS :9090) | Signaling envelopes cross here (unchanged from Phase 4) |
| phone → desktop (WebRTC data channel) | The `{ ordered: false, maxRetransmits: 0 }` channel that will carry sensor packets (Phase 4 D-05) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-SC | Tampering | npm installs | low | accept | No new packages installed in this plan (all deps installed in Plan 01 under the ahrs legitimacy gate); mechanical migration only |
| T-05-09 | Tampering | data channel reliability downgrade during migration | medium | mitigate | Verify gate asserts `{ ordered: false, maxRetransmits: 0 }` remains byte-identical — an accidental reliability/ordering change would break the unreliable hot-path contract |
| T-05-10 | Information disclosure | migration accidentally logs sensitive signaling (tokens) | low | accept | Migration preserves existing `phoneLog`/`console` calls; no new logging of tokens is added; Phase 4 already keeps tokens out of the on-screen log |
</threat_model>

<verification>
- `cd client && npx tsc --noEmit` — strict TypeScript passes for phone.ts
- `cd client && npm run build` — emits dist/phone.html + dist/assets/phone-*.js (plus room bundle)
- Grep gate confirms the unreliable data-channel options are unchanged and no top-level var / use-strict remains
- Manual (device, captured in phase verification): reload the phone page from a QR link and confirm the permission gate → pair → channel-open flow behaves exactly as in Phase 4
</verification>

<success_criteria>
- phone.js fully migrated to strict-TS src/phone.ts with identical behavior (D-01, D-02)
- phone.html is a Vite entry; public/ bridge removed; both pages build
- Data channel unreliable options preserved (Phase 4 D-05) — the PHONE-04 transport is intact
</success_criteria>

<output>
Create `.planning/phases/05-sensor-fusion-and-packet-encoding/05-02-SUMMARY.md` when done.
</output>
