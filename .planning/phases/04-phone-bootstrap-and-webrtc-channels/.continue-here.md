---
context: phase
phase: 04-phone-bootstrap-and-webrtc-channels
task: 5
total_tasks: 5
status: in_progress
last_updated: 2026-07-08T19:48:22.062Z
---

<current_state>
UAT 4/5 passed. Test 5 (heartbeat + slot disconnect + reconnect) awaiting user test.
All code committed at 6ae5c85.
</current_state>

<completed_work>
- Test 1-4: PASS
- Reconnect loop implemented: WT-first per iteration, WS fallback, 3s startup delay, _reconnecting flag suppresses spurious view-ended, showReconnecting/showReconnected UI
</completed_work>

<remaining_work>
- Run test 5: background phone 65+ s → watch for reconnect-ok in debug log
- After pass: update 04-UAT.md test 5 result=pass, mark phase complete
</remaining_work>

<decisions_made>
- incomingBidirectionalStreams.getReader() replaces for-await-of (iOS WebKit pre-26.4 fix)
- WT primary signaling, WS auto-fallback via signalSend() abstraction
- Reconnect: WT first → WS fallback; slot_not_held retried every 10s up to 130s
- 3s startup delay (iOS network unstable first ~30s on resume)
- listenForServerPushes starts AFTER reconnect success (no noisy premature logs)
- a=setup:passive munge in room.js handleOffer (speculative, harmless)
</decisions_made>

<blockers>
None. Manual test pending.
</blockers>

## Server timing (measured)
- WT relay exits ~19s after client closure; slot stays Connected (no on_client_disconnect)
- Heartbeat-miss fires at last_heartbeat.elapsed() > 65s; monitor polls every 10s
- Hold timer: 60s from heartbeat-miss
- Previous test: reconnect-ok at attempt 6 (~60s into retry loop)

## Test 5 procedure
1. Hard-refresh desktop + phone
2. Pair → active view (DC-OPEN + player-ready confirmed)
3. Background phone (press Home)
4. Desktop: expect `[Server] room-event player-disconnected slot=1`
5. Wait 65+ seconds — desktop: expect `[Server] heartbeat-miss slot=1`
6. Foreground phone
7. Phone log: reconnect-try → reconnect-WT-try → reconnect-wait:N slot_not_held → reconnect-ok slot=1
8. Phone UI: dot amber → green, channels text "N/M connected"
9. Desktop: `[Server] room-event player-reconnected slot=1`

## Infrastructure State
- coturn: LAN relay allowed (denied-peer-ip 192.168/16 commented out)
- mkcert CA installed on iPhone

<next_action>
Start with: /gsd-verify-work 4 to reload test 5 context, then run test procedure above.
</next_action>
