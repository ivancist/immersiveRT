---
phase: 03-session-and-pairing
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - server/src/signaling.rs
  - server/src/ws_server.rs
  - server/src/wt_server.rs
  - server/src/main.rs
  - server/src/lib.rs
autonomous: true
requirements:
  - SESS-01
  - SESS-02
  - SESS-04
  - SESS-06

must_haves:
  truths:
    - "ws_server.rs and wt_server.rs route join-room messages to room_registry.handle_join and write the join-ack/join-error back on the same connection (per D-09, D-10)"
    - "ws_server.rs and wt_server.rs route reconnect messages to room_registry.handle_reconnect (per D-17)"
    - "ws_server.rs and wt_server.rs route pair messages to room_registry.handle_pair (per D-09)"
    - "all other message types continue to use broker.route() unchanged"
    - "on WS/WT disconnect, room_registry.on_client_disconnect is called after broker.unregister"
    - "server refuses to start if PAIRING_TOKEN_SECRET or BASE_URL env vars are missing (per Claude's discretion recommendation in RESEARCH.md OQ4+OQ2)"
    - "cargo build -p immersive-rt-server succeeds with no warnings"
  artifacts:
    - "server/src/signaling.rs contains JoinRoomPayload, JoinAckPayload, JoinErrorPayload, RoomEventPayload, PairPayload, PairAckPayload structs"
    - "server/src/lib.rs exposes pub mod room_registry and pub mod pairing_token"
    - "server/src/main.rs constructs Arc<RoomRegistry> with all four constructor args and passes it to both wt_server::run and ws_server::run"
  key_links:
    - "ws_server relay_ws and wt_server handle_wt_connection both receive Arc<RoomRegistry> — must be threaded through all intermediate function signatures"
    - "wt_server join-room ack is written to the inbound bidirectional stream send half before finish(), not via broker (different from broker push path)"
    - "ws_server join-room ack is written directly to write (SplitSink) in the inbound arm of the select! loop"
---

<objective>
Wire the WS and WT connection handlers to the RoomRegistry created in Plan 03-01. After this plan, the running server correctly handles join-room, reconnect, and pair message types, emits proper join-ack/join-error/pair-ack responses, and triggers lifecycle events. All other signaling messages continue through broker.route() unchanged.

Purpose: Activates SESS-01 (desktop joins room via WS/WT), SESS-02 (pairing URL embedded in join-ack), SESS-04 (reconnect path), SESS-06 (lifecycle events broadcast on connect/disconnect).

Output: Five server source files modified; cargo build and cargo test both clean.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-session-and-pairing/03-CONTEXT.md
@.planning/phases/03-session-and-pairing/03-RESEARCH.md
@.planning/phases/03-session-and-pairing/03-PATTERNS.md
@server/src/signaling.rs
@server/src/ws_server.rs
@server/src/wt_server.rs
@server/src/main.rs
@server/src/lib.rs
@.planning/phases/03-session-and-pairing/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: signaling.rs — add typed payload structs for Phase 3 message types</name>
  <files>server/src/signaling.rs</files>
  <read_first>
    - server/src/signaling.rs — existing SignalingEnvelope and parse_envelope; confirm msg_type is String (no struct change needed, just additions)
    - .planning/phases/03-session-and-pairing/03-CONTEXT.md D-10 — exact field names for join-room, join-ack, join-error, room-event payloads
    - .planning/phases/03-session-and-pairing/03-PATTERNS.md §signaling.rs — optional typed payload structs example
  </read_first>
  <action>
    Append six new typed payload structs to signaling.rs after the existing parse_envelope function. All derive Debug, Serialize, Deserialize (serde already imported with features = ["derive"] in Cargo.toml):

    JoinRoomPayload: fields username: String, room_code: String (empty string means create new room per D-04), game_type: String.

    JoinAckPayload: fields slot: u8, room_code: String, reconnect_token: String, pairing_url: String (per D-10 join-ack format).

    JoinErrorPayload: field reason: String (values: "room_full", "room_not_found", "invalid_payload", "invalid_username" per D-10).

    RoomEventPayload: fields event: String (values: "player-joined", "player-disconnected", "player-left", "player-reconnected", "room-full" — note "player-disconnected" extends D-21 for hold-started state per UI-SPEC requirement), slot: u8, username: String.

    PairPayload: field token: String.

    PairAckPayload: field desktop_id: String (the client_id of the desktop that owns this slot).

    Do NOT modify SignalingEnvelope or parse_envelope. Existing code using these structs is unaffected.
  </action>
  <verify>
    <automated>cargo check -p immersive-rt-server 2>&1 | tail -10</automated>
  </verify>
  <done>cargo check succeeds; signaling.rs contains the six new payload structs with correct field types matching D-10.</done>
</task>

<task type="auto">
  <name>Task 2: ws_server.rs + wt_server.rs — inject RoomRegistry and add join-room/reconnect/pair dispatch</name>
  <files>server/src/ws_server.rs, server/src/wt_server.rs</files>
  <read_first>
    - server/src/ws_server.rs — current relay_ws signature (line ~136), run signature (line ~49), run_with_listener (line ~70), the else-branch routing block (lines ~211–237), cleanup block (lines ~269–272)
    - server/src/wt_server.rs — current run signature (line ~18), handle_wt_connection signature (line ~59), routing block (lines ~215–222), cleanup at lines ~292–293
    - .planning/phases/03-session-and-pairing/03-PATTERNS.md §ws_server.rs — exact dispatch match block and function signature extension
    - .planning/phases/03-session-and-pairing/03-01-SUMMARY.md — RoomRegistry method signatures: handle_join, handle_reconnect, handle_pair, on_client_disconnect
  </read_first>
  <action>
    WS SERVER (ws_server.rs) changes — make all changes in one pass; do not leave the file in a partially-compiling state:

    1. Add import at top: use crate::room_registry::RoomRegistry;

    2. Extend run() signature with three new trailing parameters: room_registry: Arc<RoomRegistry>, base_url: String, pairing_secret: String. Thread them through to run_with_listener.

    3. Extend run_with_listener() the same way (room_registry, base_url, pairing_secret). Thread them to relay_ws.

    4. Extend relay_ws<S>() the same way. Inside the relay loop, replace the final else-branch (the single broker.route() call) with a match on envelope.msg_type.as_str():
       - "join-room": call room_registry.handle_join(&envelope.from, &envelope.payload, &broker).await; serialize result as JSON string; send via write.send(Message::Text(...)).await.
       - "reconnect": call room_registry.handle_reconnect(&envelope.from, &envelope.payload, &broker).await; serialize and send the same way.
       - "pair": call room_registry.handle_pair(&envelope.payload, &broker).await; serialize and send.
       - _: keep the existing broker.route(&envelope.to, payload) call with its existing warn! log.
       Wrap serde_json::to_string errors with unwrap_or_else(|e| { tracing::warn!(...); String::new() }).

    5. In the cleanup block (after the select! loop): after broker.unregister(id), add room_registry.on_client_disconnect(id, &broker).await;

    WT SERVER (wt_server.rs) changes:

    1. Add import: use crate::room_registry::RoomRegistry;

    2. Extend run() with room_registry: Arc<RoomRegistry>, base_url: String, pairing_secret: String. Thread through to handle_wt_connection.

    3. Extend handle_wt_connection() the same way. Inside the accept_bi() arm, replace the single broker.route() call (lines ~216–222) with a match block identical in structure to ws_server.rs. Key difference: in WT, each inbound message arrives on its own bidirectional stream with its own (send, recv). For join-room, reconnect, and pair responses: write the ack bytes to send using send.write_all(&ack_bytes).await then send.finish().await BEFORE the match arm ends. For the _ default: call broker.route (existing logic) then the existing send.finish().await.

    4. In the cleanup block at the end of handle_wt_connection (after broker.unregister, before Ok(())): add room_registry.on_client_disconnect(&my_id, &broker).await;
  </action>
  <verify>
    <automated>cargo check -p immersive-rt-server 2>&1 | tail -15</automated>
  </verify>
  <done>cargo check passes with no errors. All four function signatures in ws_server.rs and wt_server.rs include the three new params. on_client_disconnect is called in cleanup for both transport types.</done>
</task>

<task type="auto">
  <name>Task 3: main.rs + lib.rs — env vars, RoomRegistry construction, updated try_join! call</name>
  <files>server/src/main.rs, server/src/lib.rs</files>
  <read_first>
    - server/src/main.rs — existing env var pattern (lines 58–87), try_join! call (lines 105–109), mod declarations (lines 1–5)
    - server/src/lib.rs — current pub mod list; confirm ws_server and wt_server are already exported
    - .planning/phases/03-session-and-pairing/03-PATTERNS.md §main.rs — env var pattern, Arc injection pattern
    - .planning/phases/03-session-and-pairing/03-01-SUMMARY.md — RoomRegistry::new() constructor signature
  </read_first>
  <action>
    main.rs changes (apply in a single Edit pass to avoid temporarily broken state):

    1. Add two mod declarations after the existing five: mod room_registry; and mod pairing_token;

    2. After the api_token env var block (around line 83), add the two required env vars following the exact same pattern as TURN_SHARED_SECRET — no default, descriptive error message:
       - pairing_token_secret from "PAIRING_TOKEN_SECRET" with error "PAIRING_TOKEN_SECRET environment variable not set — generate a random 32+ char secret and set it"
       - base_url from "BASE_URL" with error "BASE_URL environment variable not set — set BASE_URL=https://<your-ip>:8443 before starting the server"

    3. Add two optional env vars with defaults (pattern: unwrap_or_else then parse):
       - hold_ttl_secs: u64 from "HOLD_TTL_SECS" defaulting to "60"
       - pairing_ttl_secs: u64 from "PAIRING_TOKEN_TTL_SECS" defaulting to "90"
       Both must parse to u64 with map_err wrapping anyhow::anyhow!.

    4. After the broker construction line, add: let room_registry = Arc::new(room_registry::RoomRegistry::new(pairing_token_secret, base_url, hold_ttl_secs, pairing_ttl_secs));

    5. Update the try_join! call — pass room_registry.clone(), base_url (already consumed by RoomRegistry::new, so clone it before new() above), and pairing_token_secret similarly cloned before new(). If String values are consumed by RoomRegistry::new, clone them before passing to new() so main.rs can also log them. Update both wt_server::run and ws_server::run call sites to include the three new arguments. Keep the argument order matching the new function signatures from Task 2.

    lib.rs changes:

    Add two lines in alphabetical position: pub mod pairing_token; and pub mod room_registry; so integration tests can reference these modules.
  </action>
  <verify>
    <automated>cargo test -p immersive-rt-server 2>&1 | tail -20</automated>
  </verify>
  <done>cargo test passes with zero failures. All prior Phase 1 and Phase 2 tests continue to pass. Running `PAIRING_TOKEN_SECRET=x BASE_URL=y TURN_SHARED_SECRET=z API_TOKEN=t cargo run -p immersive-rt-server 2>&1 | head -5` shows "Server starting" log line (server starts and logs env var values). Running without PAIRING_TOKEN_SECRET or BASE_URL exits immediately with the descriptive error message.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→ws_server relay | Any WS/WT client can send arbitrary message types including join-room and pair |
| PAIRING_TOKEN_SECRET env | Secret used for HMAC token signing; must never appear in logs or error messages |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-04 | Tampering | ws/wt server join-room username field | medium | mitigate | Validation in room_registry.handle_join (1–64 chars, printable ASCII). The handler's return of join-error surfaces no internal state. |
| T-03-06 | Tampering | ws/wt server malformed payload | low | mitigate | serde_json::from_value on typed payload returns None → join-error "invalid_payload" returned; connection remains open for subsequent valid messages. This mirrors the existing envelope drop pattern (T-01-06). |
| T-03-07 | Information Disclosure | PAIRING_TOKEN_SECRET in logs | medium | mitigate | Secret passed to RoomRegistry::new() and never logged. tracing::info! startup log must NOT include pairing_token_secret value — only log that it is set. Review the startup info! call. |
</threat_model>

<verification>
Full workspace test:
```
cargo test -p immersive-rt-server
```
Must pass all tests from Phases 1, 2, and 3-01. Then verify required env vars are enforced:
```
# Missing PAIRING_TOKEN_SECRET — expect descriptive error:
TURN_SHARED_SECRET=test API_TOKEN=test cargo run -p immersive-rt-server 2>&1 | head -3
```
</verification>

<success_criteria>
- cargo test -p immersive-rt-server exits 0 with no failures
- ws_server.rs and wt_server.rs both dispatch join-room, reconnect, and pair to room_registry methods
- Missing PAIRING_TOKEN_SECRET or BASE_URL causes the server binary to exit with a descriptive error, not a panic
- PAIRING_TOKEN_SECRET value never appears in any log line (check with grep)
- lib.rs exports pub mod room_registry and pub mod pairing_token
</success_criteria>

<output>
Create `.planning/phases/03-session-and-pairing/03-02-SUMMARY.md` when done.

Include:
- Updated ws_server::run and wt_server::run function signatures (full parameter list)
- Confirmation that cargo test passes
- The exact env vars now required at server startup
- Note of any compile errors encountered and how resolved
</output>
