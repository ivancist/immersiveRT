---
quick_id: 260710-w83
slug: fix-inverted-position-axes-negate-all-th
description: Fix inverted position axes: negate all three components in both dead-reckoning and gesture position paths in client/src/scene.ts
date: 2026-07-10
status: planning
---

# Quick Task 260710-w83: Fix Inverted Position Axes

## Goal

User verified all three position axes are inverted (East/West, North/South, Up/Down all move cube opposite direction). Fix: negate all three components in both position paths in scene.ts.

## Tasks

### Task 1: Negate position components in scene.ts

**File:** `client/src/scene.ts`

**Change 1 — dead-reckoning path (line ~207):**
```
obj.mesh.position.set(rdx, rdz, -rdy)
→
obj.mesh.position.set(-rdx, -rdz, rdy)
```

**Change 2 — gesture/absolute path (line ~212):**
```
obj.mesh.position.set(rpx, rpz, -rpy)
→
obj.mesh.position.set(-rpx, -rpz, rpy)
```

**Update comment (line ~205):** Correct the mapping comment to reflect negation.

**Commit:** `fix(06-05): invert all position axes — user verified all three backwards`
