---
phase: quick-260711-0lh
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/phone.ts
  - client/tests/phone-rotate.test.ts
autonomous: true
requirements: [QUICK-0lh]

must_haves:
  truths:
    - "rotateDeviceToWorld() rotates device-frame acceleration into world frame by applying primaryQuat DIRECTLY (standard active rotation v' = q·v·q*), not its conjugate."
    - "Under eulerToQuat(90,0,0) (pure +90° yaw about Z), device-frame (1,0,0) maps to world-frame (0,1,0) — NOT (0,-1,0)."
    - "The comment above rotateDeviceToWorld describes primaryQuat as device→world (matching scene.ts unconjugated use), not world→device."
    - "The full client test suite passes."
  artifacts:
    - client/src/phone.ts
    - client/tests/phone-rotate.test.ts
  key_links:
    - "Call site phone.ts:907 (const wa = rotateDeviceToWorld(ax, ay, az, primaryQuat)) feeds the corrected world-frame accel into the Kalman integrators."
    - "phone-rotate.test.ts imports the now-exported rotateDeviceToWorld and eulerToQuat and locks the +90° yaw convention."
---

<objective>
Fix the huge position drift that makes the phone motion controller unusable. The
drift is isolated to the acceleration-integration path: `rotateDeviceToWorld()` in
`client/src/phone.ts` rotates device-frame acceleration into world frame using the
**conjugate** of `primaryQuat`, but `primaryQuat` (from `eulerToQuat()`) is already
device→world (confirmed by `scene.ts:169-170` using it unconjugated). Applying the
conjugate flips rotated acceleration on every tilt, injecting orientation-dependent
spurious acceleration into the Kalman integrator; ZUPT never catches it (it only
detects translational stillness). Orientation rendering is unaffected — only
position drifts.

Purpose: Restore correct world-frame acceleration so dead-reckoning position tracks
real phone motion instead of running away to the POSITION_MAX clamp.
Output: Corrected `rotateDeviceToWorld()` + fixed comment in `phone.ts`, a new unit
test locking the rotation convention, and a green test suite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260711-0lh-fix-huge-position-drift-making-phone-mot/260711-0lh-CONTEXT.md

# The function to fix and its call site (do NOT change the call site — the fix is
# entirely inside rotateDeviceToWorld and its comment):
@client/src/phone.ts

# The quaternion convention source (device→world, W3C Z-X-Y) that the fix must honor:
@client/src/sensor/orientation.ts

# Existing test-file conventions to follow (vitest, jsdom, import from ../src, toBeCloseTo):
@client/tests/orientation.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix rotation direction and comment in rotateDeviceToWorld, and export it</name>
  <files>client/src/phone.ts</files>
  <action>
In `client/src/phone.ts`, fix `rotateDeviceToWorld()` (currently a private function
at ~line 777) so it applies `primaryQuat` DIRECTLY as a standard active quaternion
rotation (v' = q·v·q*), not its conjugate.

The current code computes the conjugate because BOTH cross products use reversed
operand order. Flip both to standard cross-product order so the `t` vector becomes
t = 2·(q_vec × v), i.e. tx = 2·(qy·vz − qz·vy), ty = 2·(qz·vx − qx·vz),
tz = 2·(qx·vy − qy·vx); and the additive q_vec × t term for the returned x/y/z
becomes (qy·tz − qz·ty), (qz·tx − qx·tz), (qx·ty − qy·tx) respectively. Result:
x = vx + w·tx + (qy·tz − qz·ty), y = vy + w·ty + (qz·tx − qx·tz),
z = vz + w·tz + (qx·ty − qy·tx). Keep the same function signature and destructuring
of `{ w, x: qx, y: qy, z: qz }`.

Replace the stale comment block directly above the function (currently claims
primaryQuat is world→device and that the conjugate rotates device→world). The new
comment must state that `primaryQuat` (from `eulerToQuat`, W3C Z-X-Y) is device→world
— the same convention `scene.ts` uses unconjugated to orient the mesh — so the vector
is rotated by applying the quaternion directly. Do NOT reintroduce the words
"conjugate" as the operation being applied; the operation is direct rotation.

Export the function so it is unit-testable: change `function rotateDeviceToWorld`
to `export function rotateDeviceToWorld`. Do NOT change the call site at ~line 907
(`const wa = rotateDeviceToWorld(ax, ay, az, primaryQuat)`) and do NOT extract the
function into a separate module — export-in-place is sufficient (phone.ts's only
top-level statements are two `document.addEventListener` calls, which are harmless
to run under the jsdom test environment).

Out of scope (do NOT touch, per CONTEXT): rotational/gyro ZUPT, the POSITION_MAX
clamp, and the axis-mapping/negation logic in scene.ts.
  </action>
  <verify>
    <automated>cd client && grep -c "export function rotateDeviceToWorld" src/phone.ts</automated>
  </verify>
  <done>rotateDeviceToWorld applies primaryQuat directly (standard active rotation), is exported, and its comment correctly describes primaryQuat as device→world. Call site unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add unit test locking the device→world rotation convention</name>
  <files>client/tests/phone-rotate.test.ts</files>
  <action>
Create `client/tests/phone-rotate.test.ts` following the conventions in
`client/tests/orientation.test.ts` (vitest, jsdom env, `import { ... } from '../src/...'`,
`describe`/`it`, `toBeCloseTo`). Import `rotateDeviceToWorld` from `../src/phone` and
`eulerToQuat` from `../src/sensor/orientation`.

Assert the correct convention with a known quaternion. Build q = eulerToQuat(90, 0, 0)
(a pure +90° yaw about Z):

- Device-frame (1,0,0) must map to world-frame (0,1,0): assert result.x ≈ 0,
  result.y ≈ 1, result.z ≈ 0 (toBeCloseTo, 6 decimals). This is the regression catch —
  the old conjugate bug produced (0,-1,0). Include an explicit assertion that
  result.y is positive (≈ +1), NOT -1.
- Device-frame (0,1,0) must map to world-frame (-1,0,0): assert result.x ≈ -1,
  result.y ≈ 0, result.z ≈ 0. (The old conjugate would have produced (1,0,0).)
- Identity quaternion {w:1, x:0, y:0, z:0}: an arbitrary vector (e.g. 1,2,3) is
  returned unchanged.

Add a brief file header comment explaining the test locks the device→world rotation
convention (the fix for quick task 260711-0lh) and would have caught the conjugate
regression.
  </action>
  <verify>
    <automated>cd client && npx vitest run tests/phone-rotate.test.ts</automated>
  </verify>
  <done>tests/phone-rotate.test.ts exists and passes: +90° yaw maps device (1,0,0)→world (0,1,0) and (0,1,0)→(-1,0,0); identity leaves vectors unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Run full client test suite to confirm no regressions</name>
  <files>client/tests/phone-rotate.test.ts, client/src/phone.ts</files>
  <action>
Run the project's full client test suite (`npm test`, which runs `vitest run`) from
the `client/` directory. Confirm the new phone-rotate test passes and every existing
suite (orientation, encode, decode, kalman, zupt, seq-drop, target-state) still
passes. If any pre-existing test fails due to this change, stop and report — the fix
should be isolated to the acceleration-rotation path and must not affect any other
suite. Do not modify unrelated tests to make them pass.
  </action>
  <verify>
    <automated>cd client && npm test</automated>
  </verify>
  <done>Full vitest suite is green — new phone-rotate test passes and no existing test regressed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| phone sensors → phone client math | Device Motion readings are already safeFloat/NaN-guarded upstream; this change is pure math on already-sanitized values. No new boundary introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-0lh-01 | Tampering | rotateDeviceToWorld math change | low | accept | Client-only, pure-function change with no I/O and no new inputs; behavior locked by the new unit test. No untrusted data crosses a new boundary. |

No package installs in this task — no supply-chain (T-*-SC) threat applies.
</threat_model>

<verification>
- `grep -c "export function rotateDeviceToWorld" client/src/phone.ts` returns 1.
- `cd client && npx vitest run tests/phone-rotate.test.ts` passes (+90° yaw: device (1,0,0)→world (0,1,0), device (0,1,0)→world (-1,0,0); identity unchanged).
- `cd client && npm test` passes the entire suite with zero regressions.
</verification>

<success_criteria>
- `rotateDeviceToWorld()` applies `primaryQuat` directly (standard active rotation), exported, with a corrected device→world comment; call site unchanged.
- New `client/tests/phone-rotate.test.ts` locks the +90° yaw convention and would fail against the old conjugate bug.
- Full client test suite is green.
</success_criteria>

<output>
Create `.planning/quick/260711-0lh-fix-huge-position-drift-making-phone-mot/260711-0lh-SUMMARY.md` when done.
</output>
