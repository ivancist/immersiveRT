# Copy to .env and fill in real values before docker compose up — .env is gitignored
TURN_SHARED_SECRET=change-me-use-a-random-32-char-secret
COTURN_EXTERNAL_IP=127.0.0.1
CERT_PATH=certs/localhost+2.pem
KEY_PATH=certs/localhost+2-key.pem
WT_PORT=4433
WS_PORT=9090
HTTP_PORT=8081
